{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "ignoreCommand": "[ \"$VERCEL_ENV\" = \"preview\" ] && [ -z \"$VERCEL_GIT_PULL_REQUEST_ID\" ] && exit 0; exit 1",
  "crons": []
}
