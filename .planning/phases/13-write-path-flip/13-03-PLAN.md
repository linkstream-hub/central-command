---
phase: "13"
plan: "03"
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - artifacts/ag_test_results.txt
autonomous: false
requirements:
  - WPATH-01
  - WPATH-02
  - WPATH-03
---

<objective>
Production verification of the write path flip. This is the test sprint for Phase 13.

Three requirements must be demonstrated with specific observed evidence:
- WPATH-01: A new email triggers addToDispatchQueue → job appears in Neon (/live) + NO new row in Dispatch Queue Sheets tab
- WPATH-02: A dispatcher manually creates a job from the dashboard → job appears in /live (Neon read path, end-to-end)
- WPATH-03: No regressions — /live, job assignment, scheduling, Tech PWA views all behave identically to pre-flip

Evidence is written to artifacts/ag_test_results.txt with specific observed values (not "PASS" labels).
Playwright baseline must hold: 0 new failures vs ~40 passed / 32 skipped.

Pre-condition: Plan 2 complete. WRITE_PATH_NEON_ONLY = true confirmed by Brandon.
</objective>

<execution_context>
@A:/PTOW/1_APT_Central_Command/.claude/get-shit-done/workflows/execute-plan.md
@A:/PTOW/1_APT_Central_Command/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@A:/PTOW/1_APT_Central_Command/.planning/REQUIREMENTS.md
@A:/PTOW/1_APT_Central_Command/.planning/phases/13-write-path-flip/13-CONTEXT.md

<interfaces>
<!-- Production URLs -->
Dashboard (production): https://dispatch.aptmaintenanceinc.com/live
Test credentials: badge T01, PIN 1234

<!-- Code.js trigger schedule -->
checkNewLeadEmails() runs every 15 minutes on weekdays 6:30am–7pm PT.
If testing outside that window: trigger it manually via Apps Script "Run" button.
Or: send the test email and wait up to 15 min if within the schedule window.

<!-- Google Sheets verification -->
Dispatch Queue tab: https://docs.google.com/spreadsheets/d/1eCSHpj5381R7Hhloe718r1n8fEfesY2tjYOuOLxTSd4
Sort by column A (Timestamp) descending to see the latest row.
Expected after WPATH-01 test: NO new row with a timestamp matching the test email send time.

<!-- Test email spec for WPATH-01 -->
Send to: workorder@aptmaintenanceinc.com
Subject: "TEST Phase13 Flip Verification — [address]"
Body: Include a plausible Lapham-style maintenance request with a street address.
The subject "TEST Phase13 Flip" should make it easy to identify in Sheets and Neon.
After Code.js processes it: search Neon (via /live or by checking the database) for the job ID.

<!-- Playwright baseline -->
Regression ceiling: 0 new failures vs ~40 passed, ~32 skipped.
Command: cd tech-pwa && npx playwright test
Kill dev server after: Get-NetTCPConnection -LocalPort 3000,3001,3010 -State Listen -ErrorAction SilentlyContinue | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force -ErrorAction SilentlyContinue }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — confirm WRITE_PATH_NEON_ONLY is active and deploys are correct version</name>
  <read_first>none</read_first>
  <action>
    Verification only. No code or config changes.

    Check 1 — Confirm Script Property is set:
    Open Apps Script console for root project → Project Settings → Script Properties.
    Confirm: WRITE_PATH_NEON_ONLY = true
    If not present or set to false: STOP. Do not proceed. Report to Claude Code.

    Check 2 — Confirm deploy versions:
    Run: clasp deployments (from A:/PTOW/1_APT_Central_Command/)
    Confirm Code.js deployment shows description "v96"
    Confirm TechPWA deployment shows description "v91"
    Run: clasp deployments (from A:/PTOW/1_APT_Central_Command/dashboard-api/)
    Confirm DashboardAPI deployment shows description "v43"

    All three must match. Any mismatch = STOP, investigate.
  </action>
  <verify>
    <automated>clasp deployments 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - WRITE_PATH_NEON_ONLY = true confirmed in Script Properties
    - Code.js v96, TechPWA v91, DashboardAPI v43 confirmed via clasp deployments
  </acceptance_criteria>
  <done>Pre-flight passed — system is live on Neon-only write path</done>
</task>

<task type="auto">
  <name>Task 2: WPATH-01 — send test email and verify job in Neon + no Sheets row added</name>
  <read_first>none</read_first>
  <action>
    This task demonstrates WPATH-01: "Code.js processes a new inbound email and writes to Neon only."

    Step 1 — Send test email:
    Send an email to workorder@aptmaintenanceinc.com with:
    - Subject: "TEST Phase13 Flip — 1234 Test Street Unit 5, San Jose CA"
    - Body: A plausible maintenance request. Example:
      "Hi, we have a leaking faucet in unit 5 at 1234 Test Street, San Jose CA 95101.
       Please contact the tenant at test@example.com. Thanks, Property Manager"
    Record the exact send time: ______

    Step 2 — Trigger Code.js (if outside 6:30am–7pm PT schedule window):
    Open Apps Script editor for root project → select function "checkNewLeadEmails" → click Run.
    Wait for execution to complete.

    Step 3 — Verify job in Neon via /live:
    Open https://dispatch.aptmaintenanceinc.com/live
    Find the job for "1234 Test Street". Record:
    - Job ID (Lead ID): ______
    - Status shown: ______
    - Address shown: ______

    Step 4 — Verify NO new row in Dispatch Queue Sheets tab:
    Open: https://docs.google.com/spreadsheets/d/1eCSHpj5381R7Hhloe718r1n8fEfesY2tjYOuOLxTSd4
    Open the "Dispatch Queue" tab. Sort by column A (Timestamp) descending.
    Check the most recent row. Paste the timestamp and Lead ID of the most recent row: ______
    Confirm: the timestamp does NOT match the test email send time from Step 1.
    If a new row DID appear with the test email content: STOP. WRITE_PATH_NEON_ONLY is not active. Report to Claude Code immediately.

    Record in ag_test_results.txt (use the template in Task 9).
  </action>
  <verify>
    <automated>echo "Manual browser verification required"</automated>
  </verify>
  <acceptance_criteria>
    - Job for "1234 Test Street" appears in /live at https://dispatch.aptmaintenanceinc.com/live
    - Job ID recorded: non-empty string starting with the Lead ID pattern
    - Dispatch Queue Sheets tab: most recent row timestamp does NOT match test email send time
    - No new Sheets row with "1234 Test Street" or "TEST Phase13" in it
  </acceptance_criteria>
  <done>WPATH-01 verified: job in Neon at /live; no Sheets row added</done>
</task>

<task type="auto">
  <name>Task 3: WPATH-02 — dispatcher creates manual job via dashboard; verify in /live</name>
  <read_first>none</read_first>
  <action>
    This task demonstrates WPATH-02: "dispatcher creates/updates a job from the dashboard and it appears correctly in /live."

    Step 1 — Create manual job:
    Open https://dispatch.aptmaintenanceinc.com/live (log in as office staff with Google OAuth).
    Use the "Create Manual Job" button or equivalent UI element.
    Fill in:
    - Address: "9999 Manual Test Ave, San Jose CA" (memorable, won't conflict with real jobs)
    - Category: General Maintenance
    - Description: "Phase 13 manual job creation test"
    - Leave tech/schedule blank (or assign T01 if required by the form)
    Submit the form.
    Record the new job ID returned by the dashboard: ______

    Step 2 — Verify in /live:
    Refresh https://dispatch.aptmaintenanceinc.com/live
    Find the job for "9999 Manual Test Ave".
    Record:
    - Job appears in queue: yes/no ______
    - Job ID shown: ______
    - Status shown: ______
    - Address shown matches exactly: yes/no ______

    Step 3 — Verify NO new row in Dispatch Queue Sheets tab:
    Open the Dispatch Queue Sheets tab (same URL as Task 2).
    Sort by Timestamp descending. Check for a row with "9999 Manual Test Ave".
    Note: A row WILL appear in Sheets because createManualJobDA still appends to Sheets first (Sheets remains as backup write until Phase 14). This is expected and correct.
    Confirm: the job ALSO appears in /live (Neon read path). Both Sheets + Neon have the row.

    Record in ag_test_results.txt.
  </action>
  <verify>
    <automated>echo "Manual browser verification required"</automated>
  </verify>
  <acceptance_criteria>
    - Manual job created via dashboard form
    - Job appears in /live within 30 seconds of creation (no page reload required, or after one reload)
    - Job ID, address, and status are correct in /live
    - Job also appears in Sheets (createManualJobDA dual-write confirmed) — this is expected
  </acceptance_criteria>
  <done>WPATH-02 verified: manually-created job appears in /live via Neon read path</done>
</task>

<task type="auto">
  <name>Task 4: WPATH-02 continued — dispatcher updates existing job; verify change in /live</name>
  <read_first>none</read_first>
  <action>
    Verify that dashboard-initiated job updates still appear correctly in /live.
    (Dashboard updateJob routes to Next.js PATCH → Neon — this should be unchanged by the flip.)

    Step 1 — Update the manual job from Task 3:
    Find "9999 Manual Test Ave" in /live.
    Open the job detail modal.
    Assign a tech (e.g., T01) and set a scheduled date (any future date).
    Save/update.

    Step 2 — Verify in /live:
    Observe that the job shows the new tech assignment and scheduled date.
    Record:
    - Tech assigned: ______
    - Scheduled date: ______
    - Status changed to "Scheduled": yes/no ______

    Step 3 — Optionally check Neon state directly:
    If time allows, confirm via the neon_audit.py tool or /api/jobs/[jobId] endpoint
    that the updated fields are in the Neon jobs row.

    Record in ag_test_results.txt.
  </action>
  <verify>
    <automated>echo "Manual browser verification required"</automated>
  </verify>
  <acceptance_criteria>
    - Job update (tech + date) applied and visible in /live
    - No error displayed in the dashboard
    - Status transitions correctly (becomes "Scheduled" when tech + date set)
  </acceptance_criteria>
  <done>WPATH-02 update path verified: dashboard PATCH → Neon → /live shows updated fields</done>
</task>

<task type="auto">
  <name>Task 5: WPATH-03 — regression check on core flows</name>
  <read_first>none</read_first>
  <action>
    Verify that the write path flip has not broken any observable behavior.
    WPATH-03: "/live, job assignment, scheduling, and Tech PWA clock-in/out show no regressions."

    Check each item and record observed result in ag_test_results.txt:

    A. /live page loads at https://dispatch.aptmaintenanceinc.com/live — job queue visible, no console errors: ______

    B. Schedule grid (/schedule) loads and shows today's schedule correctly: ______

    C. Tech PWA (/jobs) — log in as T01 (badge T01, PIN 1234). Job list loads. Record job count shown: ______

    D. Clock-in flow — on the Tech PWA, navigate to any scheduled job and attempt clock-in.
       Observe: clock-in button responds without error.
       Record: clock-in response (success or specific error): ______
       Note: Do NOT clock out — this is a live production system. Clock in and immediately clock out if T01 has no real assignment. Or skip if no job assigned to T01.

    E. Comms tab on any active WO — open a WO, go to the Comms tab, confirm thread history loads (no "Failed to load" error): ______

    F. Browser console — open the /live page. Check the browser developer console for any new RED errors (errors that did not exist pre-flip). Record count of new console errors: ______

    Record in ag_test_results.txt.
  </action>
  <verify>
    <automated>echo "Manual browser verification required"</automated>
  </verify>
  <acceptance_criteria>
    - /live loads with job queue visible
    - /schedule loads without error
    - Tech PWA /jobs loads and shows job list
    - No new RED console errors on /live
    - No reported errors from dispatchers or techs (none observed during test)
  </acceptance_criteria>
  <done>WPATH-03 regression check complete — no regressions observed</done>
</task>

<task type="auto">
  <name>Task 6: Playwright regression suite</name>
  <read_first>none</read_first>
  <action>
    Run the full Playwright test suite to confirm 0 new failures.

    Working directory: A:/PTOW/1_APT_Central_Command/tech-pwa/

    Ensure dev server is running first (if tests run against local):
      npm run dev &

    Run tests:
      npx playwright test

    Paste the summary line here: ______
    Expected: approximately "40 passed, 32 skipped, 0 failed" (exact count may vary slightly).
    Regression ceiling: 0 new failures vs baseline.

    After tests complete — kill dev server:
      Get-NetTCPConnection -LocalPort 3000,3001,3010 -State Listen -ErrorAction SilentlyContinue | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force -ErrorAction SilentlyContinue }
  </action>
  <verify>
    <automated>cd tech-pwa && npx playwright test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Test summary shows 0 failed tests
    - Skipped count is not significantly higher than baseline (~32)
    - No new test failures introduced by Phase 13 changes
  </acceptance_criteria>
  <done>Playwright suite: [paste summary line] — 0 new failures confirmed</done>
</task>

<task type="auto">
  <name>Task 7: Clean up test jobs from production</name>
  <read_first>none</read_first>
  <action>
    Remove the test jobs created during verification from the production system.
    These are jobs that don't correspond to real work orders.

    Test jobs created:
    1. "1234 Test Street Unit 5, San Jose CA" — from WPATH-01 test (Task 2)
    2. "9999 Manual Test Ave, San Jose CA" — from WPATH-02 test (Task 3)

    For each: open the job in /live → mark as Archived (status → Archive action).
    This routes through Next.js PATCH {status: 'Archived'} → Neon.

    Record: both jobs confirmed archived in /live: ______

    Note: The Sheets row for "9999 Manual Test Ave" (created by createManualJobDA) will remain
    in Sheets as Archived. That is acceptable — Phase 14 will protect the Sheets tab.
    The WPATH-01 job will NOT have a Sheets row (that's the whole point).
  </action>
  <verify>
    <automated>echo "Manual browser action required"</automated>
  </verify>
  <acceptance_criteria>
    - Both test jobs archived in /live
    - No "TEST Phase13" jobs showing as active in the production queue
  </acceptance_criteria>
  <done>Test jobs archived — production queue clean</done>
</task>

<task type="auto">
  <name>Task 8: Write ag_test_results.txt with specific observed evidence</name>
  <read_first>none</read_first>
  <action>
    Create artifacts/ag_test_results.txt with the following template filled in
    with SPECIFIC observed values from Tasks 2–7. Do NOT write "PASS" labels —
    write what was actually observed.

    Template:

      # Phase 13: Write Path Flip — Test Results
      # Date: [today's date]
      # Tester: AG
      # Branch: feat/phase-13-write-path-flip

      ## WPATH-01: Email-inbound job → Neon only (no Sheets write)

      Test email sent at: [timestamp]
      Job ID found in /live: [actual job ID]
      Job address in /live: 1234 Test Street Unit 5, San Jose CA
      Job status in /live: [actual status]
      Dispatch Queue Sheets most-recent-row timestamp: [timestamp of most recent row before test]
      New Sheets row with test email content: NOT FOUND
      WPATH-01 verdict: [PASS/FAIL + specific evidence]

      ## WPATH-02: Dashboard creates job → appears in /live (Neon read path)

      Manual job created: 9999 Manual Test Ave, San Jose CA
      Job ID returned by dashboard: [actual job ID]
      Job visible in /live after creation: [yes/no + any delay]
      Address shown in /live: [exact text]
      Status shown in /live: [status]
      Job also in Sheets (expected): [yes/no — should be YES, dual-write preserved]
      WPATH-02 create verdict: [PASS/FAIL + specific evidence]

      Dashboard update: assigned T01, scheduled [date]
      Status became Scheduled: [yes/no]
      WPATH-02 update verdict: [PASS/FAIL + specific evidence]

      ## WPATH-03: No regressions

      /live page loads: [yes/no + any errors]
      /schedule loads: [yes/no]
      Tech PWA /jobs — job count: [number]
      Clock-in response: [observed result or "skipped — no job assigned to T01"]
      Comms tab: [loads/error]
      New browser console RED errors: [count or "none"]
      WPATH-03 verdict: [PASS/FAIL + specific evidence]

      ## Playwright baseline

      Command: npx playwright test
      Result: [paste full summary line, e.g. "42 passed, 32 skipped, 0 failed (2m 31s)"]
      New failures vs baseline (~40p/32s): [count — must be 0]
      Playwright verdict: [PASS/FAIL]

      ## Overall phase verdict

      WPATH-01: [PASS/FAIL]
      WPATH-02: [PASS/FAIL]
      WPATH-03: [PASS/FAIL]
      Playwright: [PASS/FAIL]
      Phase 13 verdict: [PASS if all PASS / FAIL if any FAIL]
  </action>
  <verify>
    <automated>
      powershell -Command "
        if (Test-Path 'A:\PTOW\1_APT_Central_Command\artifacts\ag_test_results.txt') {
          $content = Get-Content 'A:\PTOW\1_APT_Central_Command\artifacts\ag_test_results.txt' -Raw
          if ($content -match 'WPATH-01' -and $content -match 'WPATH-02' -and $content -match 'WPATH-03') {
            Write-Host 'ag_test_results.txt contains all three requirement sections: PASS'
          } else { Write-Host 'Missing sections in ag_test_results.txt: FAIL' }
        } else { Write-Host 'ag_test_results.txt does not exist: FAIL' }
      "
    </automated>
  </verify>
  <acceptance_criteria>
    - artifacts/ag_test_results.txt exists
    - Contains WPATH-01, WPATH-02, WPATH-03 sections with specific observed evidence
    - Contains Playwright summary line (not a label — the actual test output line)
    - Overall verdict is PASS (or FAIL with specific details if something failed)
  </acceptance_criteria>
  <done>ag_test_results.txt written with specific observed evidence for all three requirements</done>
</task>

<task type="terminal">
  <name>Task 9 (N-2): tsc + diff + post to Claude Code, stop. Wait for PASS.</name>
  <read_first>none</read_first>
  <action>
    Note: Phase 13 has no TypeScript changes. Skip npx tsc --noEmit (GAS-only sprint).

    Run:
    1. git push origin HEAD  (if any new commits from test artifacts)
    2. git diff main...HEAD > artifacts/ag_diff.txt
    3. git add artifacts/ag_diff.txt artifacts/ag_test_results.txt
    4. git commit -m "chore(phase-13): final diff + test results artifact"
    5. git push origin HEAD

    Post to Claude Code:
    "Phase 13 test sprint complete. ag_test_results.txt at artifacts/ag_test_results.txt.
    [Paste WPATH-01/02/03 verdicts and Playwright summary line from ag_test_results.txt here.]
    All three WPATH requirements verified. Phase 13 ready for clear-to-merge."

    STOP. Wait for Claude Code clear-to-merge.
  </action>
  <verify>
    <automated>git log --oneline -3</automated>
  </verify>
  <acceptance_criteria>
    - Commits contain Phase 13 test artifacts
    - ag_test_results.txt and ag_diff.txt both committed and pushed
    - Message posted to Claude Code — waiting for clear-to-merge
  </acceptance_criteria>
  <done>Test results and diff posted — awaiting clear-to-merge from Claude Code</done>
</task>

<task type="terminal">
  <name>Task 10 (N-1): Merge only after Claude Code issues "Clear to merge." Not before.</name>
  <read_first>none</read_first>
  <action>
    After Claude Code issues "Clear to merge":

    1. Open the PR for feat/phase-13-write-path-flip on GitHub.
    2. Ensure CI has passed (if applicable — no TypeScript changes so CI may be skipped).
    3. Merge via GitHub PR (squash or merge commit — match repo convention).
    4. Delete the feature branch.
    5. Run: git checkout main && git pull origin main
    6. Confirm: SESSION_STATE.md and ROADMAP.md Phase 13 status updated to COMPLETE (or do it as a chore commit on main if needed).

    Do NOT merge before "Clear to merge" is received from Claude Code.
  </action>
  <verify>
    <automated>git log --oneline -3</automated>
  </verify>
  <acceptance_criteria>
    - PR merged after explicit clear-to-merge from Claude Code
    - main is clean and up to date
    - Phase 13 marked COMPLETE in ROADMAP.md progress table
  </acceptance_criteria>
  <done>Phase 13 merged — write path flip complete. Neon is now the sole write path for jobs.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-V-01 | Tampering | Test emails remaining in production queue | mitigate | Task 7 archives them; they do not affect real job count |
| T-13-V-02 | Information Disclosure | ag_test_results.txt contains no secrets | accept | Test results contain only job IDs, timestamps, and status values — no credentials, DSNs, or tokens |
| T-13-V-03 | Denial of Service | Playwright test against production | accept | Playwright runs against local dev server (localhost:3000), not production URL, per current config |
</threat_model>

<verification>
Phase 13 is complete when:
1. WPATH-01 PASS: test email processed by Code.js → job in /live, no Sheets row added
2. WPATH-02 PASS: manual job created via dashboard → visible in /live; existing job updated → change visible in /live
3. WPATH-03 PASS: all core flows unchanged; Playwright 0 new failures
4. Claude Code clear-to-merge received
5. PR merged; main updated; ROADMAP.md Phase 13 → COMPLETE
</verification>

<success_criteria>
Wave 3 is complete when:
1. ag_test_results.txt contains specific observed evidence for WPATH-01, WPATH-02, WPATH-03
2. Playwright summary shows 0 new failures
3. All three phase requirements verified in production
4. PR merged after clear-to-merge from Claude Code
5. Phase 13 status → COMPLETE in ROADMAP.md
</success_criteria>

<output>
Create `.planning/phases/13-write-path-flip/13-03-SUMMARY.md` when done.
</output>
