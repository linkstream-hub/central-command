---
phase: "13"
plan: "02"
type: execute
wave: 2
depends_on: ["13-01"]
files_modified: []
autonomous: false
requirements:
  - WPATH-01
  - WPATH-02
  - WPATH-03

user_setup:
  - service: Google Apps Script — Root Project
    why: "Set the WRITE_PATH_NEON_ONLY = true Script Property to activate the Neon-only write path"
    dashboard_config:
      - task: "Set WRITE_PATH_NEON_ONLY Script Property in root Apps Script project"
        location: >
          Open Apps Script console for the root project (Code.js / TechPWA.gs).
          Script editor → Project Settings (gear icon) → Script Properties → Add property:
            Property: WRITE_PATH_NEON_ONLY
            Value:    true
          Save. Do NOT set this on the dashboard-api project — it belongs on the root project only.
---

<objective>
Deploy the code changes from Plan 1 to all three GAS endpoints, then have Brandon activate the flip
via Script Properties. No code edits in this plan — it is purely deploy + activation.

Three deploys required (separate clasp projects):
1. Root project (Code.js + TechPWA.gs) — `clasp push` + TechPWA deploy + Code.js deploy
2. DashboardAPI project — `clasp push` + DashboardAPI deploy

Then Brandon sets `WRITE_PATH_NEON_ONLY = true` in the Apps Script console.
After this plan completes, the production system is live on the new write path.
Plan 3 verifies it.

Pre-condition: Claude Code has issued PASS on the Plan 1 diff. Do not start this plan without PASS.
</objective>

<execution_context>
@A:/PTOW/1_APT_Central_Command/.claude/get-shit-done/workflows/execute-plan.md
@A:/PTOW/1_APT_Central_Command/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@A:/PTOW/1_APT_Central_Command/.planning/phases/13-write-path-flip/13-CONTEXT.md
@A:/PTOW/1_APT_Central_Command/CLAUDE.md

<interfaces>
<!-- Deploy commands — exact from CLAUDE.md -->
Root project working directory: A:/PTOW/1_APT_Central_Command/

  clasp push --force
  clasp deploy --deploymentId AKfycbySG8tbAaXyIRFXnq7x-Fp5Gvs7uG8RmAyBB_wSFcGmScbhI3SHSq2HoznowBcsi3mM9Q --description "v91"
  [TechPWA v91 — Phase 13 write path flip: WRITE_PATH_NEON_ONLY gate + syncJobToNeon hardening]

  clasp deploy --deploymentId AKfycbyFgHHDrZm1NZBG2iQ3czdeRxSGuvjkqyLkx7OCjdkA5vRBCm3IQ1RAEyrbgVp-Y4xs1g --description "v96"
  [Code.js v96 — Phase 13 write path flip: addToDispatchQueue/updateJob/archiveJob gated]

DashboardAPI working directory: A:/PTOW/1_APT_Central_Command/dashboard-api/

  clasp push --force
  clasp deploy --deploymentId AKfycbyum_KLprgPh51GxFiwhsoNHScc4TqIBrzZS0GPfHsnhrc9hAtp03AciyiydhfyJyxCCQ --description "v43"
  [DashboardAPI v43 — Phase 13: createManualJobDA Neon sync]

NOTE: All three deploy commands are run by AG in the terminal (from repo root or dashboard-api/).
Code.js deploy is "manual only — never automate" per CLAUDE.md — this means it is never triggered
by CI/CD; AG running it explicitly in the terminal IS the correct manual procedure.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate check — confirm Plan 1 PASS received before deploying</name>
  <read_first>none</read_first>
  <action>
    Verify pre-conditions before touching any deploy commands:

    1. Confirm artifacts/ag_diff.txt exists and is non-empty:
       Test-Path 'A:\PTOW\1_APT_Central_Command\artifacts\ag_diff.txt'

    2. Confirm current branch is feat/phase-13-write-path-flip:
       git branch --show-current

    3. Confirm Plan 1 PASS was received from Claude Code.
       If no PASS has been issued: STOP. Do not run any clasp commands. Report status to Claude Code.

    This task has no file modifications. Gate only.
  </action>
  <verify>
    <automated>
      git branch --show-current
      Test-Path 'A:\PTOW\1_APT_Central_Command\artifacts\ag_diff.txt'
    </automated>
  </verify>
  <acceptance_criteria>
    - Branch is feat/phase-13-write-path-flip
    - artifacts/ag_diff.txt exists
    - Plan 1 PASS confirmed
  </acceptance_criteria>
  <done>Gate check passed — proceeding to deploys</done>
</task>

<task type="auto">
  <name>Task 2: Root project — clasp push (pushes Code.js + TechPWA.gs + SuggestTechs.js)</name>
  <read_first>none</read_first>
  <action>
    Working directory: A:/PTOW/1_APT_Central_Command/
    (All clasp commands in Tasks 2–4 run from this directory.)

    Run:
      clasp push --force

    Expected output: lines like "└─ Code.js" and "└─ TechPWA.gs" listed as pushed.
    Any error or "No files to push" = STOP, investigate, report to Claude Code.

    Note: clasp push pushes ALL .js and .gs files in the root project. This is correct.
  </action>
  <verify>
    <automated>clasp push --force 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - clasp push completes without error
    - Output confirms Code.js and TechPWA.gs were included in the push
  </acceptance_criteria>
  <done>Root project pushed — Code.js + TechPWA.gs live in Apps Script editor</done>
</task>

<task type="auto">
  <name>Task 3: Root project — deploy TechPWA v91</name>
  <read_first>none</read_first>
  <action>
    Working directory: A:/PTOW/1_APT_Central_Command/

    Run:
      clasp deploy --deploymentId AKfycbySG8tbAaXyIRFXnq7x-Fp5Gvs7uG8RmAyBB_wSFcGmScbhI3SHSq2HoznowBcsi3mM9Q --description "v91"

    Expected output: "Updated deployment." and the deployment URL.
    Any error = STOP, report to Claude Code.
  </action>
  <verify>
    <automated>clasp deploy --deploymentId AKfycbySG8tbAaXyIRFXnq7x-Fp5Gvs7uG8RmAyBB_wSFcGmScbhI3SHSq2HoznowBcsi3mM9Q --description "v91" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - Deploy command returns "Updated deployment."
    - No errors
  </acceptance_criteria>
  <done>TechPWA v91 deployed — syncJobToNeon hardening live</done>
</task>

<task type="auto">
  <name>Task 4: Root project — deploy Code.js v96 (manual-only deploy)</name>
  <read_first>none</read_first>
  <action>
    Working directory: A:/PTOW/1_APT_Central_Command/

    IMPORTANT: Code.js deployment is "manual only — never automate" per CLAUDE.md.
    This task is the manual terminal step. AG runs this explicitly; it is NOT run by CI/CD.

    Run:
      clasp deploy --deploymentId AKfycbyFgHHDrZm1NZBG2iQ3czdeRxSGuvjkqyLkx7OCjdkA5vRBCm3IQ1RAEyrbgVp-Y4xs1g --description "v96"

    Expected output: "Updated deployment." and the deployment URL.
    Any error = STOP, report to Claude Code.
  </action>
  <verify>
    <automated>clasp deploy --deploymentId AKfycbyFgHHDrZm1NZBG2iQ3czdeRxSGuvjkqyLkx7OCjdkA5vRBCm3IQ1RAEyrbgVp-Y4xs1g --description "v96" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - Deploy command returns "Updated deployment."
    - No errors
  </acceptance_criteria>
  <done>Code.js v96 deployed — addToDispatchQueue/updateJob/archiveJob gates live</done>
</task>

<task type="auto">
  <name>Task 5: DashboardAPI project — clasp push + deploy v43</name>
  <read_first>none</read_first>
  <action>
    Working directory: A:/PTOW/1_APT_Central_Command/dashboard-api/

    Run in sequence:

    Step 1 — Push:
      cd dashboard-api && clasp push --force
    (Or from repo root: clasp push --force with working dir set to dashboard-api/)

    Expected output: DashboardAPI.gs listed as pushed.

    Step 2 — Deploy:
      clasp deploy --deploymentId AKfycbyum_KLprgPh51GxFiwhsoNHScc4TqIBrzZS0GPfHsnhrc9hAtp03AciyiydhfyJyxCCQ --description "v43"

    Expected output: "Updated deployment."
    Any error = STOP, report to Claude Code.
  </action>
  <verify>
    <automated>echo "Verify via clasp deploy output above"</automated>
  </verify>
  <acceptance_criteria>
    - clasp push succeeds for dashboard-api/ project
    - Deploy returns "Updated deployment."
    - No errors in either step
  </acceptance_criteria>
  <done>DashboardAPI v43 deployed — createManualJobDA Neon sync live</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: Brandon — set WRITE_PATH_NEON_ONLY = true in Apps Script Script Properties</name>
  <what-built>
    AG cannot do this — it requires the Apps Script console UI (web dashboard).
    This is the live production flip. Once set, the next Code.js trigger will write to Neon only.
    Brandon is the account holder and this is classified as a dashboard-only action per the Brandon Rule.
  </what-built>
  <how-to-verify>
    1. Open the Apps Script editor for the root project (the one containing Code.js and TechPWA.gs).
       URL: https://script.google.com → find the project named for the Lead Parsing / TechPWA deployment.

    2. Click the gear icon (Project Settings) in the left sidebar.

    3. Scroll to "Script Properties" section.

    4. Click "Add script property".

    5. Property name: WRITE_PATH_NEON_ONLY
       Value: true

    6. Click "Save script properties".

    7. Verify: the property appears in the list as WRITE_PATH_NEON_ONLY = true.

    ROLLBACK: If anything goes wrong after the flip, return here and change the value to "false"
    (or delete the property entirely). This takes effect on the next trigger run — no code deploy needed.
  </how-to-verify>
  <resume-signal>
    Type "property set" after Step 7 is confirmed.
    Paste the Script Properties panel screenshot or confirmation: "WRITE_PATH_NEON_ONLY = true confirmed"
  </resume-signal>
</task>

<task type="terminal">
  <name>Task 7: Terminal — confirm all deploys and property set; post status to Claude Code, stop</name>
  <read_first>none</read_first>
  <action>
    Run deploy version confirmations:

    From A:/PTOW/1_APT_Central_Command/:
      clasp deployments

    From A:/PTOW/1_APT_Central_Command/dashboard-api/:
      clasp deployments

    Post to Claude Code:
    "Phase 13 Wave 2 deploys complete. All three GAS endpoints updated:
    - Code.js v96: appendRow/setValue gated by WRITE_PATH_NEON_ONLY
    - TechPWA v91: syncJobToNeon hardened with HTTP status check + re-throw
    - DashboardAPI v43: createManualJobDA Neon sync added
    Brandon confirmed: WRITE_PATH_NEON_ONLY = true set in Script Properties.
    Production write path is now Neon-only for inbound email jobs.
    Proceeding to Plan 3: production verification."

    STOP. Wait for acknowledgement before Plan 3.
    (Plan 3 sends a real test email — confirm with Claude Code first.)
  </action>
  <verify>
    <automated>echo "Verification via clasp deployments output above"</automated>
  </verify>
  <acceptance_criteria>
    - All three deploys confirmed via clasp deployments output
    - WRITE_PATH_NEON_ONLY = true confirmed by Brandon
    - Status posted to Claude Code
  </acceptance_criteria>
  <done>All deploys live; property set; status posted. Awaiting Plan 3 acknowledgement.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-D-01 | Tampering | clasp push overwrites ALL files in project | mitigate | Plan 1 diff review by Claude Code ensures only the three targeted functions are changed; clasp push applies exactly what is in the local working tree |
| T-13-D-02 | Denial of Service | WRITE_PATH_NEON_ONLY=true + Neon outage | mitigate | Rollback is instant: set property to false in Apps Script UI. No clasp deploy needed. RTO < 2 minutes. |
| T-13-D-03 | Spoofing | Someone else sets WRITE_PATH_NEON_ONLY | accept | Only Brandon has GAS console access. Risk is negligible. |
</threat_model>

<verification>
After all 7 tasks complete:
- clasp deployments confirms Code.js v96, TechPWA v91, DashboardAPI v43 are the active versions
- WRITE_PATH_NEON_ONLY = true is set in root project Script Properties (confirmed by Brandon)
- No new error alerts in GAS execution log
</verification>

<success_criteria>
Wave 2 is complete when:
1. All three GAS projects deployed at the expected version numbers
2. WRITE_PATH_NEON_ONLY = true confirmed by Brandon
3. Status posted to Claude Code — Plan 3 acknowledged
</success_criteria>

<output>
Create `.planning/phases/13-write-path-flip/13-02-SUMMARY.md` when done.
</output>
