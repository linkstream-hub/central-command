---
phase: "13"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Code.js
  - TechPWA.gs
  - dashboard-api/DashboardAPI.gs
autonomous: false
requirements:
  - WPATH-01
  - WPATH-02
  - WPATH-03
---

<objective>
Make all code changes required for the write path flip. Three files, four concerns:

1. **Code.js** — gate `addToDispatchQueue()`, `updateJob()`, and `archiveJob()` behind the `WRITE_PATH_NEON_ONLY` Script Property. When the property is `true`, Sheets writes are skipped and the Neon sync call continues.
2. **TechPWA.gs** — harden `syncJobToNeon()` error handling. After the flip, a silent sync failure is data loss — the function must throw when `WRITE_PATH_NEON_ONLY` is `true` and the HTTP call returns a non-2xx response.
3. **DashboardAPI.gs** — add an inline Neon sync call to `createManualJobDA()`. Manually-created jobs currently never reach Neon, which would break WPATH-02 after the flip.

No schema changes. No new Next.js routes. No deploy in this plan — deploy is Plan 2.

Output: all three files modified, diff posted to Claude Code for PTOW gate review.
</objective>

<execution_context>
@A:/PTOW/1_APT_Central_Command/.claude/get-shit-done/workflows/execute-plan.md
@A:/PTOW/1_APT_Central_Command/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@A:/PTOW/1_APT_Central_Command/.planning/ROADMAP.md
@A:/PTOW/1_APT_Central_Command/.planning/phases/13-write-path-flip/13-CONTEXT.md
@A:/PTOW/1_APT_Central_Command/docs/SHADOW_WRITES.md
@A:/PTOW/1_APT_Central_Command/docs/GAS_MIGRATION_SCOPE.md

<interfaces>
<!-- syncJobToNeon auth — confirmed pre-flight result (Task 2 verifies live) -->
TechPWA.gs:1629   headers: { 'DASHBOARD_API_KEY': apiKey }  ← header name is the key name
route.ts:10       const apiKey = req.headers.get('DASHBOARD_API_KEY');  ← matches
Status: NO MISMATCH. Auth is correct. Do not change headers.

<!-- addToDispatchQueue structure (Code.js:810–924) -->
LOOKUP_BY_SENDER branch (lines 817–862):
  line 849: dispatchSheet.appendRow(rowData);          ← GATE with !neonOnly
  lines 852–858: syncJobToNeon call                    ← KEEP (always run)
  line 860: Logger.log(...)                            ← keep
  line 861: return;                                    ← keep

Standard branch (lines 870–924):
  line 908: dispatchSheet.appendRow(rowData);          ← GATE with !neonOnly
  lines 910–917: syncJobToNeon call                    ← KEEP (always run)
  line 919: var newRow = dispatchSheet.getLastRow();   ← GATE with !neonOnly
  line 920: var range = dispatchSheet.getRange(...)    ← GATE with !neonOnly
  lines 921–923: setBackground calls                   ← GATE with !neonOnly

WRITE_PATH_NEON_ONLY must be read ONCE at the top of addToDispatchQueue(), before either branch.
Row-color block (919–923) MUST also be gated — getLastRow() returns wrong row if appendRow was skipped.

<!-- updateJob() and archiveJob() — confirmed dead code -->
Code.js grep result: updateJob has zero callers in Code.js.
Dashboard routes archiveJob via Next.js PATCH /api/jobs/[jobId] (status=Archived) — not via GAS.
Action: gate both with early-return stub when neonOnly=true. Log clearly.

<!-- syncJobToNeon hardening (TechPWA.gs:1616–1638) -->
Current: try/catch logs failure silently; muteHttpExceptions:true masks HTTP errors.
Required: check response.getResponseCode(). If not 200 or 201 AND WRITE_PATH_NEON_ONLY=true → throw.
Pattern for re-throw:
  if (statusCode !== 200 && statusCode !== 201) {
    var neonOnly = PropertiesService.getScriptProperties().getProperty('WRITE_PATH_NEON_ONLY') === 'true';
    if (neonOnly) throw new Error('Neon sync HTTP ' + statusCode + ': ' + responseText);
    return { success: false, statusCode: statusCode };
  }

<!-- createManualJobDA gap (DashboardAPI.gs:1350–1480) -->
Problem: createManualJobDA writes to Sheets (appendRow) but never calls syncJobToNeon.
This is a SEPARATE clasp project (dashboard-api/) — cannot call TechPWA.gs syncJobToNeon directly.
Fix: inline UrlFetchApp.fetch call to /api/jobs/sync AFTER sheet.appendRow(newRow).
Variables available in scope at that point: leadId, address, unit, category, description,
  assignedTech, status, notes, rmName, rmEmail, tenantName, tenantPhone, tenantEmail,
  accessInfo, wcCode, entityId, body.scheduledDate, body.scheduledTime, estHours, now, priority.
Auth pattern: read DASHBOARD_API_URL + DASHBOARD_API_KEY from Script Properties (same as push notification block above it).
Header: { 'DASHBOARD_API_KEY': apiKey } — must match /api/jobs/sync route.
muteHttpExceptions: true — log only, never throw (Sheets write already succeeded).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Branch setup — MANDATORY FIRST TASK</name>
  <read_first>none</read_first>
  <action>
    Run these three commands and paste output for each:

    1. git branch --show-current
       Expected: feat/phase-13-write-path-flip
       Mismatch = STOP. Report to Claude Code. Do not proceed.

    2. git ls-remote --heads origin feat/phase-13-write-path-flip
       Expected: non-empty (branch exists on remote).
       Empty = run `git push -u origin feat/phase-13-write-path-flip` first.

    3. git log main..HEAD --oneline
       Expected: shows any commits if branch has prior work, or empty for a fresh branch.
       If empty on a pre-existing branch (not freshly created): rebase from main before starting.
  </action>
  <verify>
    <automated>git branch --show-current</automated>
  </verify>
  <acceptance_criteria>
    - Current branch is feat/phase-13-write-path-flip
    - Branch exists on remote
  </acceptance_criteria>
  <done>Branch confirmed: feat/phase-13-write-path-flip on remote</done>
</task>

<task type="auto">
  <name>Task 2: Auth pre-flight — verify syncJobToNeon header matches /api/jobs/sync before any Sheets write is removed</name>
  <read_first>
    - A:/PTOW/1_APT_Central_Command/TechPWA.gs lines 1616–1640 (syncJobToNeon function)
    - A:/PTOW/1_APT_Central_Command/tech-pwa/src/app/api/jobs/sync/route.ts lines 1–15
  </read_first>
  <action>
    READ-ONLY verification. No code changes in this task.

    Check 1: TechPWA.gs syncJobToNeon() — confirm the `headers` object key name.
    - Expected: `{ 'DASHBOARD_API_KEY': apiKey }` (key name is the literal string "DASHBOARD_API_KEY")

    Check 2: /api/jobs/sync route.ts — confirm what header it reads.
    - Expected: `req.headers.get('DASHBOARD_API_KEY')`

    If both match: log "Auth pre-flight PASS: header name 'DASHBOARD_API_KEY' matches on both sides."
    If mismatch found: STOP. Report exact mismatch to Claude Code. Do not proceed to any code changes.

    This task has no file modifications. Its only output is a logged verification result.
  </action>
  <verify>
    <automated>
      powershell -Command "
        $tpwa = Select-String -Path 'A:\PTOW\1_APT_Central_Command\TechPWA.gs' -Pattern 'DASHBOARD_API_KEY'
        $route = Select-String -Path 'A:\PTOW\1_APT_Central_Command\tech-pwa\src\app\api\jobs\sync\route.ts' -Pattern 'DASHBOARD_API_KEY'
        Write-Host 'TechPWA.gs:' $tpwa.Line.Trim()
        Write-Host 'route.ts:' $route.Line.Trim()
      "
    </automated>
  </verify>
  <acceptance_criteria>
    - TechPWA.gs syncJobToNeon() sends header with key name `DASHBOARD_API_KEY`
    - /api/jobs/sync reads `req.headers.get('DASHBOARD_API_KEY')`
    - Both strings are identical
    - No mismatch detected
  </acceptance_criteria>
  <done>Auth pre-flight PASS logged — DASHBOARD_API_KEY header name matches on both sides</done>
</task>

<task type="auto">
  <name>Task 3: Code.js — gate addToDispatchQueue() with WRITE_PATH_NEON_ONLY</name>
  <read_first>
    - A:/PTOW/1_APT_Central_Command/Code.js lines 810–924 (full addToDispatchQueue function)
  </read_first>
  <action>
    Modify Code.js:addToDispatchQueue().

    Step 1 — Add toggle read at the very top of the function, immediately after the existing APT Internal skip check (line 812). Insert before the LOOKUP_BY_SENDER block:

      var neonOnly = PropertiesService.getScriptProperties().getProperty('WRITE_PATH_NEON_ONLY') === 'true';

    Step 2 — LOOKUP_BY_SENDER branch: gate the appendRow call (line 849). Replace:
      dispatchSheet.appendRow(rowData);
    With:
      if (!neonOnly) { dispatchSheet.appendRow(rowData); }

    The syncJobToNeon try/catch block immediately after MUST remain ungated (lines 852–858).
    The Logger.log and return at the end of this branch remain ungated.

    Step 3 — Standard branch: gate the appendRow call (line 908). Replace:
      dispatchSheet.appendRow(rowData);
    With:
      if (!neonOnly) { dispatchSheet.appendRow(rowData); }

    The syncJobToNeon try/catch block immediately after MUST remain ungated (lines 910–917).

    Step 4 — Standard branch: gate the row-color block (lines 919–923). Replace:
      var newRow = dispatchSheet.getLastRow();
      var range  = dispatchSheet.getRange(newRow, 1, 1, 26);
      if      (priority === "1-URGENT")      range.setBackground("#FFE0CC");
      else if (priority === "3-PTE-PENDING") range.setBackground("#FFF3CD");
      else if (priority === "2-TURNOVER")    range.setBackground("#D4EDDA");
    With:
      if (!neonOnly) {
        var newRow = dispatchSheet.getLastRow();
        var range  = dispatchSheet.getRange(newRow, 1, 1, 26);
        if      (priority === "1-URGENT")      range.setBackground("#FFE0CC");
        else if (priority === "3-PTE-PENDING") range.setBackground("#FFF3CD");
        else if (priority === "2-TURNOVER")    range.setBackground("#D4EDDA");
      }

    IMPORTANT: `rowData` is built before the appendRow call in both branches and is passed to
    `getJobDataFromRow(rowData)` — this works correctly whether or not appendRow ran.
    Do NOT change the syncJobToNeon call or the rowData construction.
  </action>
  <verify>
    <automated>
      powershell -Command "
        $code = Get-Content 'A:\PTOW\1_APT_Central_Command\Code.js' -Raw
        if ($code -match 'WRITE_PATH_NEON_ONLY') { Write-Host 'Toggle present: PASS' } else { Write-Host 'Toggle MISSING: FAIL' }
        if ($code -match 'if \(!neonOnly\).*appendRow') { Write-Host 'appendRow gate: PASS' } else { Write-Host 'appendRow gate pattern not found — verify manually' }
      "
    </automated>
  </verify>
  <acceptance_criteria>
    - `WRITE_PATH_NEON_ONLY` is read once at the top of addToDispatchQueue(), before either branch
    - LOOKUP_BY_SENDER branch: appendRow is inside `if (!neonOnly)` block
    - LOOKUP_BY_SENDER branch: syncJobToNeon call is NOT inside any neonOnly guard
    - Standard branch: appendRow is inside `if (!neonOnly)` block
    - Standard branch: syncJobToNeon call is NOT inside any neonOnly guard
    - Standard branch: row-color block (getLastRow + getRange + setBackground) is inside `if (!neonOnly)` block
    - No other logic in addToDispatchQueue is changed
  </acceptance_criteria>
  <done>addToDispatchQueue gated: appendRow + row-color skip when WRITE_PATH_NEON_ONLY=true; syncJobToNeon always runs</done>
</task>

<task type="auto">
  <name>Task 4: Code.js — gate updateJob() and archiveJob() (dead-code stub + safety gate)</name>
  <read_first>
    - A:/PTOW/1_APT_Central_Command/Code.js lines 1737–1804 (updateJob and archiveJob functions)
  </read_first>
  <action>
    Both functions are confirmed dead code for active paths:
    - updateJob() has zero callers in Code.js
    - archiveJob() is superseded by dashboard → Next.js PATCH /api/jobs/[jobId] {status:'Archived'}
    Gate both with early-return stubs for safety (prevents any Sheets write if someone calls them directly).

    **updateJob()**: Inside the try block, immediately after the `if (!dispatchSheet || !job.rowIndex) return false;` guard (line 1741), insert:

      var neonOnly = PropertiesService.getScriptProperties().getProperty('WRITE_PATH_NEON_ONLY') === 'true';
      if (neonOnly) {
        Logger.log('updateJob skipped: WRITE_PATH_NEON_ONLY=true — dashboard updates route via Next.js PATCH.');
        return true;
      }

    **archiveJob()**: Inside the try block, immediately after the `if (!dispatchSheet || !rowIndex) return false;` guard (line 1790), insert:

      var neonOnly = PropertiesService.getScriptProperties().getProperty('WRITE_PATH_NEON_ONLY') === 'true';
      if (neonOnly) {
        Logger.log('archiveJob skipped: WRITE_PATH_NEON_ONLY=true — archive routes via Next.js PATCH.');
        return true;
      }

    No other changes to these functions.
  </action>
  <verify>
    <automated>
      powershell -Command "
        $code = Get-Content 'A:\PTOW\1_APT_Central_Command\Code.js' -Raw
        $updateGated = $code -match 'updateJob skipped.*WRITE_PATH_NEON_ONLY'
        $archiveGated = $code -match 'archiveJob skipped.*WRITE_PATH_NEON_ONLY'
        Write-Host ('updateJob gate: ' + $(if ($updateGated) { 'PASS' } else { 'FAIL' }))
        Write-Host ('archiveJob gate: ' + $(if ($archiveGated) { 'PASS' } else { 'FAIL' }))
      "
    </automated>
  </verify>
  <acceptance_criteria>
    - updateJob() returns true immediately when WRITE_PATH_NEON_ONLY=true, logging the reason
    - archiveJob() returns true immediately when WRITE_PATH_NEON_ONLY=true, logging the reason
    - No other logic in either function is modified
  </acceptance_criteria>
  <done>updateJob and archiveJob gated with early-return stubs when WRITE_PATH_NEON_ONLY=true</done>
</task>

<task type="auto">
  <name>Task 5: TechPWA.gs — harden syncJobToNeon() error handling for neon-only mode</name>
  <read_first>
    - A:/PTOW/1_APT_Central_Command/TechPWA.gs lines 1616–1640 (syncJobToNeon function)
  </read_first>
  <action>
    Modify TechPWA.gs:syncJobToNeon().

    Current code in the try block:
      var response = UrlFetchApp.fetch(url, {
        method: 'post',
        contentType: 'application/json',
        headers: { 'DASHBOARD_API_KEY': apiKey },
        payload: JSON.stringify(jobData),
        muteHttpExceptions: true
      });
      Logger.log('Sync Job Result: ' + response.getContentText());
      return JSON.parse(response.getContentText());

    Replace with:
      var response = UrlFetchApp.fetch(url, {
        method: 'post',
        contentType: 'application/json',
        headers: { 'DASHBOARD_API_KEY': apiKey },
        payload: JSON.stringify(jobData),
        muteHttpExceptions: true
      });
      var statusCode   = response.getResponseCode();
      var responseText = response.getContentText();
      Logger.log('Sync Job Result (' + statusCode + '): ' + responseText);
      if (statusCode !== 200 && statusCode !== 201) {
        var neonOnly = PropertiesService.getScriptProperties().getProperty('WRITE_PATH_NEON_ONLY') === 'true';
        if (neonOnly) {
          throw new Error('Neon sync failed HTTP ' + statusCode + ' (WRITE_PATH_NEON_ONLY=true): ' + responseText);
        }
        return { success: false, statusCode: statusCode, error: responseText };
      }
      return JSON.parse(responseText);

    Current catch block:
      } catch (e) {
        Logger.log('Sync Job Error: ' + e.message);
        return { success: false, error: e.message };
      }

    Replace with:
      } catch (e) {
        Logger.log('Sync Job Error: ' + e.message);
        var neonOnlyErr = PropertiesService.getScriptProperties().getProperty('WRITE_PATH_NEON_ONLY') === 'true';
        if (neonOnlyErr) { throw e; }
        return { success: false, error: e.message };
      }

    This ensures that when WRITE_PATH_NEON_ONLY=true, any network failure or non-2xx response
    propagates as an exception to the caller in addToDispatchQueue(), making the failure visible
    in GAS execution logs rather than silently swallowed.
  </action>
  <verify>
    <automated>
      powershell -Command "
        $tpwa = Get-Content 'A:\PTOW\1_APT_Central_Command\TechPWA.gs' -Raw
        $hasStatusCheck = $tpwa -match 'getResponseCode'
        $hasReThrow     = $tpwa -match 'neonOnlyErr.*throw e'
        Write-Host ('HTTP status check: ' + $(if ($hasStatusCheck) { 'PASS' } else { 'FAIL' }))
        Write-Host ('catch re-throw:    ' + $(if ($hasReThrow) { 'PASS' } else { 'FAIL' }))
      "
    </automated>
  </verify>
  <acceptance_criteria>
    - syncJobToNeon() calls response.getResponseCode() and logs the status code
    - Non-2xx response when WRITE_PATH_NEON_ONLY=true throws an Error (not returns silently)
    - Network/catch error when WRITE_PATH_NEON_ONLY=true re-throws the exception
    - When WRITE_PATH_NEON_ONLY=false (or unset), existing graceful error return behavior is preserved
    - Do not change: URL construction, DASHBOARD_API_URL property read, header name, payload serialization
  </acceptance_criteria>
  <done>syncJobToNeon hardened: HTTP status checked; throws on failure when neon-only mode is active</done>
</task>

<task type="auto">
  <name>Task 6: DashboardAPI.gs — add inline Neon sync to createManualJobDA() for WPATH-02</name>
  <read_first>
    - A:/PTOW/1_APT_Central_Command/dashboard-api/DashboardAPI.gs lines 1350–1480 (createManualJobDA function)
    - A:/PTOW/1_APT_Central_Command/tech-pwa/src/app/api/jobs/sync/route.ts (verify expected field names)
  </read_first>
  <action>
    Add a Neon sync block immediately after `sheet.appendRow(newRow);` (line ~1427) and before the
    row-color block that follows it.

    Insert this block:

      // Sync to Neon — added Phase 13: ensures manually-created jobs appear in /live after write-path flip
      try {
        var baseUrl = PropertiesService.getScriptProperties().getProperty('DASHBOARD_API_URL');
        var apiKey  = PropertiesService.getScriptProperties().getProperty('DASHBOARD_API_KEY');
        if (baseUrl && apiKey) {
          var jobData = {
            jobId:        leadId,
            timestamp:    now,
            priority:     priority,
            emailType:    'manual_entry',
            category:     category,
            address:      address,
            unit:         unit,
            description:  description,
            accessInfo:   accessInfo,
            rmName:       rmName,
            rmEmail:      rmEmail,
            tenantName:   tenantName,
            tenantPhone:  tenantPhone,
            tenantEmail:  tenantEmail,
            tech:         assignedTech,
            scheduledDate: body.scheduledDate || '',
            scheduledTime: body.scheduledTime || '',
            estHours:     parseFloat(estHours) || 0,
            status:       status,
            notes:        notes,
            wcCode:       wcCode,
            entityId:     entityId
          };
          UrlFetchApp.fetch(baseUrl + '/api/jobs/sync', {
            method:          'post',
            contentType:     'application/json',
            headers:         { 'DASHBOARD_API_KEY': apiKey },
            payload:         JSON.stringify(jobData),
            muteHttpExceptions: true
          });
        }
      } catch (e) {
        Logger.log('createManualJobDA Neon sync failed: ' + e.message);
      }

    IMPORTANT:
    - This block goes AFTER appendRow (Sheets write already succeeded — log only, never throw on sync failure)
    - DASHBOARD_API_URL must be set in DashboardAPI.gs Script Properties (same Script Properties panel as DASHBOARD_API_KEY)
    - `now` is defined earlier in createManualJobDA as the timestamp variable — verify before insertion
    - `estHours` is defined earlier as `body.estHours || ''` — verify this variable name is in scope
    - `body.scheduledDate` and `body.scheduledTime` are the original request body fields before they were combined into `scheduledValue`
    - Do NOT use `scheduledValue` (it's the combined "YYYY-MM-DD|HH:MM" format used for Sheets only)
  </action>
  <verify>
    <automated>
      powershell -Command "
        $da = Get-Content 'A:\PTOW\1_APT_Central_Command\dashboard-api\DashboardAPI.gs' -Raw
        $hasSyncBlock  = $da -match 'createManualJobDA Neon sync'
        $hasApiJobSync = $da -match '/api/jobs/sync'
        Write-Host ('createManualJobDA sync block: ' + $(if ($hasSyncBlock) { 'PASS' } else { 'FAIL' }))
        Write-Host ('/api/jobs/sync call in DashboardAPI: ' + $(if ($hasApiJobSync) { 'PASS' } else { 'FAIL' }))
      "
    </automated>
  </verify>
  <acceptance_criteria>
    - Neon sync block appears in createManualJobDA() after the sheet.appendRow(newRow) call
    - The block reads DASHBOARD_API_URL and DASHBOARD_API_KEY from Script Properties
    - The block builds jobData with at minimum: jobId (leadId), timestamp, priority, emailType, category, address, status, tech, entityId
    - Auth header is `{ 'DASHBOARD_API_KEY': apiKey }` — matches /api/jobs/sync expectation
    - muteHttpExceptions: true — Sheets write already committed; sync failure is logged only
    - No other functions in DashboardAPI.gs are modified
  </acceptance_criteria>
  <done>createManualJobDA: Neon sync block added after appendRow — manual jobs will now appear in /live</done>
</task>

<task type="auto">
  <name>Task 7: Contradiction check — verify all changes before first commit</name>
  <read_first>
    - A:/PTOW/1_APT_Central_Command/Code.js lines 810–924 (addToDispatchQueue in full)
    - A:/PTOW/1_APT_Central_Command/Code.js lines 1737–1804 (updateJob + archiveJob)
    - A:/PTOW/1_APT_Central_Command/TechPWA.gs lines 1616–1645 (syncJobToNeon in full)
    - A:/PTOW/1_APT_Central_Command/dashboard-api/DashboardAPI.gs lines 1426–1440 (createManualJobDA sync block insertion point)
  </read_first>
  <action>
    READ-ONLY audit. No code changes. Verify:

    1. addToDispatchQueue() — LOOKUP_BY_SENDER branch:
       - `neonOnly` variable exists before the branch
       - `if (!neonOnly)` wraps ONLY the `appendRow` call
       - syncJobToNeon call is OUTSIDE any neonOnly guard
       - `return;` at end of branch is outside any guard

    2. addToDispatchQueue() — Standard branch:
       - `if (!neonOnly)` wraps the `appendRow` call
       - syncJobToNeon call is OUTSIDE any neonOnly guard
       - Row-color block (getLastRow + getRange + setBackground trio) is INSIDE a separate `if (!neonOnly)` guard
       - No other logic is gated

    3. updateJob() — first lines inside try block:
       - `neonOnly` is read from Script Properties
       - `if (neonOnly) { Logger.log(...); return true; }` precedes all setValue calls

    4. archiveJob() — first lines inside try block:
       - `neonOnly` is read from Script Properties
       - `if (neonOnly) { Logger.log(...); return true; }` precedes all setValue calls

    5. syncJobToNeon() — inside try:
       - `getResponseCode()` is called on the response
       - Non-2xx response with neonOnly=true → throws
       - catch block re-throws when neonOnly=true

    6. createManualJobDA() — after appendRow:
       - Neon sync block exists
       - Uses `DASHBOARD_API_KEY` header name (not `x-api-key`)
       - Uses `body.scheduledDate` and `body.scheduledTime` separately (not `scheduledValue`)

    If any check fails: fix it before proceeding to the terminal task.
    Log each check as PASS or FAIL. All 6 must PASS.
  </action>
  <verify>
    <automated>echo "Manual read required — no automated check for logic structure"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 contradiction checks pass
    - No appendRow calls in addToDispatchQueue remain ungated
    - No row-color setBackground calls remain ungated in the standard branch
    - syncJobToNeon calls in addToDispatchQueue are NOT inside any neonOnly guard
  </acceptance_criteria>
  <done>Contradiction check complete — all 6 checks PASS</done>
</task>

<task type="terminal">
  <name>Task 8: Terminal — git push + diff + post to Claude Code, stop</name>
  <read_first>none</read_first>
  <action>
    Run in order:

    1. git add Code.js TechPWA.gs dashboard-api/DashboardAPI.gs
    2. git commit -m "feat(phase-13): gate GAS write path + harden syncJobToNeon + DashboardAPI manual job sync"
    3. git push origin HEAD
    4. git diff main...HEAD > artifacts/ag_diff.txt
    5. git add artifacts/ag_diff.txt
    6. git commit -m "chore(phase-13): update diff artifact"
    7. git push origin HEAD

    Post to Claude Code:
    "Phase 13 Wave 1 code changes complete. diff at artifacts/ag_diff.txt.
    Files changed: Code.js (addToDispatchQueue gated, updateJob/archiveJob stubs), TechPWA.gs (syncJobToNeon hardened), DashboardAPI.gs (createManualJobDA Neon sync added).
    No TypeScript changes. No schema changes. Awaiting PTOW gate PASS."

    STOP. Wait for Claude Code PASS before Plan 2.
  </action>
  <verify>
    <automated>git log --oneline -3</automated>
  </verify>
  <acceptance_criteria>
    - Commits on feat/phase-13-write-path-flip
    - artifacts/ag_diff.txt exists and is non-empty
    - Message posted to Claude Code; waiting for PASS
  </acceptance_criteria>
  <done>Diff posted to Claude Code — waiting for PASS before Plan 2</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script Property → GAS | WRITE_PATH_NEON_ONLY is read from Script Properties — only Brandon can set/unset it in the GAS console |
| GAS → Next.js | syncJobToNeon HTTP call — auth via DASHBOARD_API_KEY header; route validates against env var |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | WRITE_PATH_NEON_ONLY property | mitigate | Property read-only from within GAS; only GAS console (Brandon) can set it |
| T-13-02 | Denial of Service | syncJobToNeon throw on failure | accept | If Neon is unavailable when neonOnly=true, inbound lead is logged to GAS execution log but not queued anywhere. Accept: retry via manual trigger is faster than risking data loss. |
| T-13-03 | Repudiation | createManualJobDA sync failure | accept | muteHttpExceptions=true — sync failure logged but does not surface to dispatcher. Acceptable: Sheets row still created; Neon will be caught by next audit run. |
</threat_model>

<verification>
After all 8 tasks complete:
- Code.js: WRITE_PATH_NEON_ONLY toggle present in addToDispatchQueue; appendRow and row-color block gated; syncJobToNeon ungated
- Code.js: updateJob and archiveJob return early with Logger.log when neonOnly=true
- TechPWA.gs: syncJobToNeon calls getResponseCode; throws on non-2xx when neonOnly=true; re-throws in catch when neonOnly=true
- DashboardAPI.gs: createManualJobDA has Neon sync block after appendRow using DASHBOARD_API_KEY header
- artifacts/ag_diff.txt committed and pushed
- Message posted to Claude Code — waiting for PASS
</verification>

<success_criteria>
Wave 1 is complete when:
1. All three GAS files modified per task specs
2. Contradiction check passes (all 6 checks green)
3. Diff artifact committed and pushed
4. Claude Code PASS received on the diff
</success_criteria>

<output>
Create `.planning/phases/13-write-path-flip/13-01-SUMMARY.md` when done.
</output>
