---
phase: 25-parsing-intake-quality
plan: 04
type: execute
wave: 3
depends_on: ["25-03"]
files_modified:
  - Code.js
autonomous: false
requirements: [INTAKE-01, INTAKE-02, INTAKE-03, INTAKE-04, INTAKE-05, INTAKE-06, INTAKE-07]
user_setup:
  - service: gas
    why: "Disable the legacy GAS email-polling trigger so it does not double-process with n8n"
    dashboard_config:
      - task: "Delete the checkNewLeadEmails time-based trigger after n8n workflow is activated"
        location: "Google Apps Script → root project → Triggers panel"
must_haves:
  truths:
    - "checkNewLeadEmails returns immediately (migrated-to-n8n stub) and is clasp-pushed to production"
    - "The GAS time-based trigger for checkNewLeadEmails is deleted (no concurrent polling)"
    - "The n8n workflow is activated only AFTER the GAS stub is live — never concurrent"
    - "A manual end-to-end run against a test inbox produces a correctly-parsed WO with access info, requester auto-reply, and tenant coordination email"
    - "No duplicate WOs appear for a single inbound email (dedup + single-poller verified)"
  artifacts:
    - path: "Code.js"
      provides: "checkNewLeadEmails stubbed with early return — migration cutover"
      contains: "Migrated to n8n"
  key_links:
    - from: "Code.js checkNewLeadEmails"
      to: "n8n phase-19-email-polling workflow"
      via: "polling handoff — GAS stub then n8n owns intake"
      pattern: "Migrated to n8n"
---

<objective>
Execute the intake cutover: stub the legacy GAS `checkNewLeadEmails` so it no longer polls, deploy that change, then activate the n8n workflow — strictly in that order to avoid double-processing. Finish with a manual end-to-end verification against a test inbox covering every INTAKE requirement on the live path.

Purpose: The n8n workflow (Plan 25-03) cannot go live while GAS is still polling the same inbox — both would process the same email and create duplicate WOs (Research Pitfall 1). This plan is the controlled switchover plus the only verification that actually proves INTAKE-01..07 on real inbound mail (unit tests verify logic; this verifies the live pipeline). This plan is deliberately small and high-stakes — it is the merge-gate moment for the phase.

Output: stubbed `Code.js` (clasp-pushed), activated n8n workflow, deleted GAS trigger, and a manual verification record.
</objective>

<execution_context>
@C:/PTOW/1_APT_Central_Command/.claude/get-shit-done/workflows/execute-plan.md
@C:/PTOW/1_APT_Central_Command/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25-parsing-intake-quality/25-CONTEXT.md
@.planning/phases/25-parsing-intake-quality/25-RESEARCH.md
@.planning/phases/25-parsing-intake-quality/25-03-SUMMARY.md

# Cutover source:
@Code.js

<interfaces>
<!-- Cutover ordering invariant (Research Pitfall 1) — NEVER reorder. Matches Task 2 exactly: -->
#   1. Insert `return;` stub at top of checkNewLeadEmails  →  clasp push --force  (GAS stops polling)
#   2. Delete the GAS time-based trigger (manual, Triggers panel — kill it while GAS is stubbed)
#   3. Activate n8n phase-19-email-polling workflow         (n8n starts polling — only poller left)
#
# Concurrency = duplicate WOs. Warning sign: two WOs with same Gmail msgId in Neon jobs table.
#
# CLAUDE.md invariants: no NEW GAS code (a stub/early-return is migration, allowed);
#   clasp deploy is MANUAL ONLY — never automate; dev write guard stays on for any test sends.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stub checkNewLeadEmails and prepare the cutover diff</name>
  <files>Code.js</files>
  <read_first>
    - Code.js — `checkNewLeadEmails` (and `setupTrigger`) full body; identify the exact entry point to short-circuit
    - 25-RESEARCH.md Runtime State Inventory ("Live service config" + "OS-registered state" rows) and Pitfall 1
    - CLAUDE.md — "no new GAS code" (early-return stub is migration, not a feature) + "clasp deploy MANUAL ONLY"
  </read_first>
  <action>
    Insert at the very top of `checkNewLeadEmails()` an early `return; // Migrated to n8n (Phase 25 cutover) — polling owned by phase-19-email-polling workflow`. Do not delete the function body (keep it as dormant reference, consistent with the sheets-client tombstone pattern). Make no other GAS changes. This is a code edit only — do NOT run clasp here (deploy is manual and gated in Task 2). Stage the change and produce a diff artifact at `artifacts/phase25_gas_cutover_diff.txt` via `git diff Code.js`.
  </action>
  <verify>
    <automated>cd "C:/PTOW/1_APT_Central_Command" && node -e "const c=require('fs').readFileSync('Code.js','utf8'); const i=c.indexOf('function checkNewLeadEmails'); if(i<0)throw new Error('checkNewLeadEmails not found'); const seg=c.slice(i,i+400); if(!/return;\s*\/\/\s*Migrated to n8n/.test(seg))throw new Error('stub return not at top of checkNewLeadEmails'); console.log('GAS stub OK');"</automated>
  </verify>
  <acceptance_criteria>checkNewLeadEmails begins with the migrated-to-n8n early return; no other GAS edits; diff artifact written.</acceptance_criteria>
  <done>Stub in place and verified; ready for manual clasp push at the cutover checkpoint.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Execute the ordered cutover (GAS push then n8n activate then trigger delete)</name>
  <what-built>Code.js stub is staged (Task 1) and the n8n workflow is committed active:false (Plan 25-03). This step performs the live switchover, which requires GAS deploy + n8n activation that only a human operator performs.</what-built>
  <how-to-verify>
    Perform IN THIS EXACT ORDER (reordering creates duplicate WOs):
    1. Push the GAS stub: `clasp push --force` from the GAS root project. Confirm checkNewLeadEmails now returns immediately.
    2. In Google Apps Script Triggers panel, DELETE the time-based trigger that fires checkNewLeadEmails (every 15 min). Confirm it is gone.
    3. In n8n (Railway), open `Phase 19 — Email Polling & WO Intake` and set it Active. Confirm the Gmail trigger is listening.
    4. Confirm no other workflow or GAS path is still polling workorder@.
  </how-to-verify>
  <resume-signal>Reply "cutover done — GAS stub pushed, trigger deleted, n8n active". If anything blocks, describe what.</resume-signal>
  <acceptance_criteria>GAS stub deployed, GAS trigger deleted, n8n workflow active — confirmed in the stated order; exactly one poller live.</acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual end-to-end verification on the live intake path</name>
  <what-built>The n8n workflow is now the sole poller. This verifies INTAKE-01..07 end-to-end on real mail — the only verification that exercises Gemini extraction quality, Neon lookup, and outbound comms on the live path.</what-built>
  <how-to-verify>
    Run these against the production/test inbox (dev write guard must remain ON for any send you do not want delivered; for the real send tests, send to your own controlled addresses):
    1. INTAKE-05 (paramount): Submit a real Lapham webform (or forward a captured one) to workorder@. Confirm the resulting WO has address, requester, tenant name+contact, and a clean description — lossless. Repeat with an Apple-Mail-forwarded copy (two-line format).
    2. INTAKE-01/02/03: Forward a free-text RM email with an address. Confirm address, requester/tenant, and description extract correctly; confirm a turnover/inspection email does NOT populate tenant contact.
    3. INTAKE-04: Send a WO whose access info contains a code not in the property's Neon record. Confirm the properties row gains the new code (merge, not replace — existing codes still present).
    4. INTAKE-06: Confirm the requester receives the "request received" auto-reply; confirm an APT-internal forward does NOT get one.
    5. INTAKE-07: Confirm tenant gets the coordination email when email present. (Tenant SMS deferred to Phase 23 — no SMS check in this phase.)
    6. Duplicate guard: confirm a single inbound email produces exactly ONE WO (no Gmail msgId appears twice in Neon jobs).
  </how-to-verify>
  <resume-signal>Report pass/fail per item 1-6. Any fail → describe the gap (routes to gap-closure planning). All pass → "intake E2E verified".</resume-signal>
  <acceptance_criteria>Items 1-6 verified on the live path; INTAKE-05 lossless confirmed both formats; no duplicate WOs; auto-reply + tenant coordination email confirmed.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GAS poller ↔ n8n poller | Two systems that must never poll the same inbox concurrently |
| live inbound mail → production WO creation + outbound sends | The real, unfiltered path — dev guard is the only thing between a test and a real customer send |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-13 | Tampering | Duplicate WO creation from concurrent GAS+n8n polling | mitigate | Strict cutover order (stub+push → trigger delete → activate); isDuplicateJob + Gmail msgId dedup; Task 3 item 6 explicitly checks for double-processing. |
| T-25-14 | Information Disclosure | Accidental real customer send during E2E test (Lapham incident precedent) | mitigate | Dev write guard stays ON; real-send tests target operator-controlled addresses only; SMS tested to Brandon's phone first. |
| T-25-15 | Repudiation | No record of cutover ordering | accept | Cutover steps + verification captured in the checkpoint replies and SUMMARY; sufficient for a solo-operator workflow. |
| T-25-SC | Tampering | npm/clasp installs | accept | No npm installs; clasp is the existing project toolchain, deploy is manual-only per CLAUDE.md. |
</threat_model>

<verification>
- Task 1 stub verify script passes; diff artifact exists at artifacts/phase25_gas_cutover_diff.txt
- Cutover performed in the mandated order (checkpoint confirmation)
- E2E checklist items 1-6 reported pass
- No duplicate WOs for a single inbound email
</verification>

<success_criteria>
- checkNewLeadEmails stubbed (migrated-to-n8n) and clasp-pushed to production
- GAS trigger deleted; n8n workflow active; exactly one poller
- INTAKE-01..07 verified end-to-end on the live path (INTAKE-07 = email channel; SMS deferred to Phase 23)
- Zero duplicate WOs; auto-reply correctly skips APT-internal senders
</success_criteria>

<output>
Create `.planning/phases/25-parsing-intake-quality/25-04-SUMMARY.md` when done. Record: cutover timestamp + order, GAS trigger deletion confirmation, E2E results per INTAKE requirement, and any gaps to route into gap-closure planning.
</output>
