---
phase: 25-parsing-intake-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tech-pwa/src/lib/normalizeAddressKey.ts
  - tech-pwa/src/lib/detectLaphamForm.ts
  - tech-pwa/src/lib/__tests__/normalizeAddressKey.test.ts
  - tech-pwa/src/lib/__tests__/detectLaphamForm.test.ts
  - tech-pwa/src/lib/__tests__/fixtures/intake/lapham-sameline.txt
  - tech-pwa/src/lib/__tests__/fixtures/intake/lapham-forwarded.txt
  - tech-pwa/src/lib/__tests__/fixtures/intake/lapham-turnover.txt
  - tech-pwa/vitest.config.ts
autonomous: true
requirements: [INTAKE-01, INTAKE-02, INTAKE-03, INTAKE-05]
must_haves:
  truths:
    - "normalizeAddressKey produces the same dedup key as the GAS Code.js source for every fixture address"
    - "detectLaphamForm extracts every dispatch-needed field from both same-line and Apple-Mail-forwarded (two-line) form bodies"
    - "detectLaphamForm returns null for non-Lapham emails (caller falls through to Gemini)"
    - "Turnover/inspection forms do not populate tenant contact fields"
    - "Both functions have unit tests covering every algorithm step and pass under npm run test:unit"
  artifacts:
    - path: "tech-pwa/src/lib/normalizeAddressKey.ts"
      provides: "Deterministic address dedup key, verbatim port of GAS normalizeAddressKey"
      exports: ["normalizeAddressKey"]
    - path: "tech-pwa/src/lib/detectLaphamForm.ts"
      provides: "Deterministic Lapham webform parser, verbatim port of GAS detectLaphamForm"
      exports: ["detectLaphamForm", "LaphamParseResult"]
    - path: "tech-pwa/src/lib/__tests__/normalizeAddressKey.test.ts"
      provides: "Unit coverage for every normalization step (INTAKE-01)"
    - path: "tech-pwa/src/lib/__tests__/detectLaphamForm.test.ts"
      provides: "Unit coverage for detection, field extraction, two-line format, emailType, PTE, tenant exemption (INTAKE-02/03/05)"
  key_links:
    - from: "tech-pwa/src/lib/detectLaphamForm.ts"
      to: "tech-pwa/src/lib/normalizeAddressKey.ts"
      via: "import for senderLookupNeeded address normalization"
      pattern: "import.*normalizeAddressKey"
    - from: "tech-pwa/vitest.config.ts"
      to: "src/lib/normalizeAddressKey.ts"
      via: "coverage.include entry"
      pattern: "normalizeAddressKey"
---

<objective>
Port the two production-tested deterministic parsing primitives out of GAS `Code.js` into testable TypeScript modules with full unit coverage. These are the dedup key (`normalizeAddressKey`) and the lossless Lapham form parser (`detectLaphamForm`). This plan also establishes the intake fixture set and test scaffolding that downstream plans verify against.

Purpose: INTAKE-05 (Lapham lossless extraction) is the paramount requirement of this phase. The GAS parsing logic contains >1 year of production bug fixes (Apple-Mail forwarded format, embedded units, street-type normalization). Porting it verbatim — with tests proving parity — is the only safe path. These TS modules become the source of truth that the n8n Code nodes (Plan 25-03) reproduce.

Output: `normalizeAddressKey.ts`, `detectLaphamForm.ts`, their test suites, an intake fixture directory, and updated vitest coverage config.
</objective>

<execution_context>
@C:/PTOW/1_APT_Central_Command/.claude/get-shit-done/workflows/execute-plan.md
@C:/PTOW/1_APT_Central_Command/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/25-parsing-intake-quality/25-CONTEXT.md
@.planning/phases/25-parsing-intake-quality/25-RESEARCH.md
@.planning/phases/25-parsing-intake-quality/25-PATTERNS.md

# Port source (read in full before writing):
@Code.js

# Analog files to copy structure from:
@tech-pwa/src/lib/wc-codes.ts
@tech-pwa/src/lib/__tests__/wc-codes.test.ts
@tech-pwa/src/lib/__tests__/compliance.test.ts
@tech-pwa/vitest.config.ts

<interfaces>
<!-- Contract produced by this plan, consumed by Plan 25-03 (n8n Code nodes) and Plan 25-02. -->
<!-- detectLaphamForm returns this shape; n8n must reproduce it identically. -->

export interface LaphamParseResult {
  isLaphamForm: boolean;
  confidence: 'High' | 'Medium' | 'Low';
  emailType: 'adhoc_workorder' | 'turnover' | 'inspection' | 'unknown';
  address: string;
  unit: string;
  rmName: string;
  rmEmail: string;
  tenantName: string;
  tenantPhone: string;
  tenantEmail: string;
  tenantPreferredContact: string;
  tenantHasPets: string;
  pteGranted: 'Yes' | 'No' | 'Not Applicable';
  pteNotes: string;
  description: string;
  senderLookupNeeded: boolean;
}

export function normalizeAddressKey(address: string, unit?: string): string;
export function detectLaphamForm(senderEmail: string, subject: string, body: string): LaphamParseResult | null;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port normalizeAddressKey with full unit coverage</name>
  <files>tech-pwa/src/lib/normalizeAddressKey.ts, tech-pwa/src/lib/__tests__/normalizeAddressKey.test.ts, tech-pwa/vitest.config.ts</files>
  <read_first>
    - Code.js — locate `normalizeAddressKey` and read it in full; this is the verbatim port source
    - tech-pwa/src/lib/wc-codes.ts — single-export utility structure to mirror (no framework imports, JSDoc on the exported function)
    - tech-pwa/src/lib/__tests__/wc-codes.test.ts — vitest test structure to mirror
    - 25-RESEARCH.md Pattern 2 — the 7-step algorithm in order
    - 25-PATTERNS.md "normalizeAddressKey.ts" section — exact signature and coverage scope
    - tech-pwa/vitest.config.ts — existing coverage.include list to extend
  </read_first>
  <behavior>
    - Test 1: strips city/state suffix at first comma boundary — `normalizeAddressKey('123 Main St, Oakland, CA')` → `'123 main st||'`
    - Test 2: extracts embedded unit when unit param empty — `normalizeAddressKey('456 Elm Ave #2B')` → `'456 elm ave||2b'`
    - Test 3: explicit unit param overrides absence of embedded unit
    - Test 4: normalizes `##` to `#` before unit extraction
    - Test 5: street-type tokens lowercased and trailing-period-stripped (`Avenue`, `Ave.`, `St.`)
    - Test 6: empty/null address returns `'||'`
    - Test 7: collapses multiple spaces and strips non-alphanumeric
  </behavior>
  <action>
    Create `normalizeAddressKey.ts` as a verbatim TypeScript port of the GAS function. Signature: `export function normalizeAddressKey(address: string, unit?: string): string`. No framework imports — pure transform with a JSDoc block (mirror wc-codes.ts). Preserve the GAS algorithm step-for-step: `##`→`#`, embedded `#\w+` extraction into unit when unit empty, strip `#\w+`, split on first comma, street-type regex lowercase + trailing-period strip, lowercase + strip non-alphanumeric + collapse spaces, return `addr + '||' + unit`. Handle null/empty via `String(value || '')` at entry — no try/catch. Then create the test file mirroring wc-codes.test.ts structure with the seven behaviors above. Finally add `'src/lib/normalizeAddressKey.ts'` to the `coverage.include` array in vitest.config.ts.
  </action>
  <verify>
    <automated>cd tech-pwa && npm run test:unit -- normalizeAddressKey</automated>
  </verify>
  <acceptance_criteria>normalizeAddressKey.ts exports the function; test file has 7+ cases all passing; vitest.config.ts coverage.include lists the new file; no tsc errors introduced.</acceptance_criteria>
  <done>All normalizeAddressKey tests pass; output matches GAS source for every fixture address.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port detectLaphamForm with fixtures and full unit coverage</name>
  <files>tech-pwa/src/lib/detectLaphamForm.ts, tech-pwa/src/lib/__tests__/detectLaphamForm.test.ts, tech-pwa/src/lib/__tests__/fixtures/intake/lapham-sameline.txt, tech-pwa/src/lib/__tests__/fixtures/intake/lapham-forwarded.txt, tech-pwa/src/lib/__tests__/fixtures/intake/lapham-turnover.txt, tech-pwa/vitest.config.ts</files>
  <read_first>
    - Code.js — `detectLaphamForm` and its inner `field()` helper (the two-line/same-line regex strategy); read both in full
    - Code.js — emailType detection from description (turnover/inspection keyword scan) and PTE value mapping
    - 25-RESEARCH.md Pattern 3 — detection guard, two-line regex, PTE mapping, emailType detection, tenant-context note
    - 25-PATTERNS.md "detectLaphamForm.ts" section — LaphamParseResult interface, field() port, detection guard, error handling
    - 25-PATTERNS.md "detectLaphamForm.test.ts" section — nested describe-block organization
  </read_first>
  <behavior>
    - Detection: returns non-null for sender `website@laphamcompany.com`, OR body containing `'Submitted values are:'`, OR `'Webform submission from: Maintenance Request'`; returns null otherwise
    - Same-line extraction: `'Name: Jane Smith'` → `tenantName` populated (or rmName per field mapping)
    - Two-line (forwarded) extraction: `'Address\n123 Elm St'` with `>` quote-prefix stripped → address populated — every field must work in BOTH formats
    - emailType: description containing `turnover`/`move out`/`move-out` → `turnover`; `inspection` → `inspection`; else `adhoc_workorder`
    - PTE mapping: "permission to enter my dwelling while I am not there" → `Yes`; "do not give permission"/"without my presence" → `No`; else `Not Applicable`
    - Tenant exemption: turnover/inspection fixture does not produce tenant contact when form has none — no hallucination
    - senderLookupNeeded: true when address resolves empty or `LOOKUP_BY_SENDER`
  </behavior>
  <action>
    Create three fixture files under `__tests__/fixtures/intake/`: a same-line Lapham webform body, an Apple-Mail-forwarded version of the same data (fields rendered as `Field\nValue` with `>` quote prefixes and a `---------- Forwarded message ---------` header), and a turnover-type form with no tenant contact. Use sanitized synthetic data (no real PII). Then create `detectLaphamForm.ts` porting the GAS function verbatim: the `LaphamParseResult` interface from the interfaces block, the `field()` helper with BOTH same-line and two-line regex (CRITICAL — research Pitfall 3), the detection guard, PTE mapping, emailType keyword scan, and `senderLookupNeeded` computation. Import `normalizeAddressKey` for any address-key derivation. Return `null` for non-Lapham emails. No try/catch — missing fields return empty string. Then create the test file with nested describe blocks (detection, same-line extraction, forwarded/two-line extraction, emailType, PTE mapping, tenant exemption) loading the fixtures via `readFileSync`. Add `'src/lib/detectLaphamForm.ts'` to vitest.config.ts coverage.include.
  </action>
  <verify>
    <automated>cd tech-pwa && npm run test:unit -- detectLaphamForm</automated>
  </verify>
  <acceptance_criteria>detectLaphamForm.ts exports function + LaphamParseResult; both same-line and forwarded fixtures extract all fields; turnover fixture leaves tenant fields empty; non-Lapham input returns null; all tests pass; coverage.include updated.</acceptance_criteria>
  <done>Every dispatch-needed field extracts losslessly from both fixture formats; tenant exemption proven; tests green.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: Terminal gate — tsc, diff artifact, push, STOP for Claude Code review</name>
  <what-built>Parsing utility ports (normalizeAddressKey, detectLaphamForm) with fixtures and unit tests.</what-built>
  <how-to-verify>
    1. `cd tech-pwa && npx tsc --noEmit` → zero errors (fill in observed output)
    2. `git diff main...HEAD > artifacts/phase25_01_diff.txt` → commit the artifact
    3. `git push` → branch on origin
    4. STOP — post diff to Claude Code for review. Do not start Wave 2 without explicit PASS.
  </how-to-verify>
  <resume-signal>Claude Code replies PASS / clear for this plan's diff</resume-signal>
  <acceptance_criteria>tsc zero errors (evidence pasted); artifacts/phase25_01_diff.txt committed; branch pushed; Claude Code PASS recorded in this plan's SUMMARY.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inbound email body → parser | Untrusted external content (email body) enters deterministic parsing. No code execution path — pure string transform. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | detectLaphamForm regex on attacker-controlled body | accept | Pure string extraction; no eval, no injection sink. Output is data fields consumed downstream with their own validation. ReDoS risk low — regexes are anchored, bounded character classes. |
| T-25-02 | Information Disclosure | test fixtures committed to git | mitigate | Fixtures use synthetic sanitized data only — no real tenant PII, no real addresses, no real access codes. |
| T-25-SC | Tampering | npm installs | accept | No new packages introduced this plan (verbatim port using existing stack). |
</threat_model>

<verification>
- `cd tech-pwa && npm run test:unit` runs the full unit suite green (no regressions to compliance/job-transitions/wc-codes)
- `cd tech-pwa && npx tsc --noEmit` reports zero errors
- Fixtures contain no real PII (manual grep for real domains/phone patterns)
</verification>

<success_criteria>
- normalizeAddressKey.ts and detectLaphamForm.ts exist with the exact exported signatures from the interfaces block
- Both functions are byte-for-byte behavioral ports of the GAS source (verified by tests against fixtures)
- INTAKE-01 (address), INTAKE-02 (requester/tenant + exemption), INTAKE-03 (description), INTAKE-05 (Lapham lossless) have passing unit coverage
- vitest.config.ts coverage.include lists both new modules
- Full unit suite green; tsc clean
</success_criteria>

<output>
Create `.planning/phases/25-parsing-intake-quality/25-01-SUMMARY.md` when done. List: exported symbols (normalizeAddressKey, detectLaphamForm, LaphamParseResult), fixture file paths, and any GAS edge cases discovered during the port that downstream n8n nodes must reproduce.
</output>
