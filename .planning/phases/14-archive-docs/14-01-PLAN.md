# Phase 14: Archive + Docs (v1.1 Neon Cut-Over)

## Goals
- ARCH-01: Lock the Dispatch Queue Google Sheets tab as a read-only archive to prevent any further writes from Code.js or users.
- ARCH-02: Update `docs/SHADOW_WRITES.md` to formally close the dual-write era and mark Sheets as an archive.
- ARCH-03: Update `SESSION_STATE.md` and `PROJECT.md` to formally declare Neon as the sole write path and source of truth.

## Tasks

### Wave 1: Sheet Protection & Documentation
1. **Brandon Action (ARCH-01):** Open the active Google Sheet, navigate to the "Dispatch Queue" tab, right-click and select "Protect sheet". Restrict edit permissions entirely to lock it as a read-only archive.
2. **AG (ARCH-02):** Update `docs/SHADOW_WRITES.md` with a dated entry stating the cut-over is complete and Sheets is now locked.
3. **AG (ARCH-03):** Update `SESSION_STATE.md` and `PROJECT.md` to declare Neon as the sole write path and single source of truth.
4. **Terminal Task:** Run `npx tsc --noEmit` -> `git push origin HEAD` -> `git diff main...HEAD > artifacts/ag_diff.txt` -> commit artifact -> submit to Claude Code.

## Verification
- Verify `docs/SHADOW_WRITES.md` reflects cutover complete status.
- Verify `SESSION_STATE.md` and `PROJECT.md` state Neon as sole write path and source of truth.
