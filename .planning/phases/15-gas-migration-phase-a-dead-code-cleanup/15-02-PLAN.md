---
phase: 15-gas-migration-phase-a-dead-code-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - Code.js
  - SuggestTechs.js
autonomous: false
requirements: []
must_haves:
  truths:
    - "Every confirmed-safe dead function in Code.js and SuggestTechs.js is removed"
    - "Dead call site for sendAutoReply inside routeLead is removed"
    - "Root clasp project (Code.js + SuggestTechs.js) pushes and deploys with a version bump"
    - "checkNewLeadEmails() trigger runs once after deploy with no reference errors"
  artifacts:
    - path: "Code.js"
      provides: "Root GAS project — email polling, lead parsing (dead code removed)"
      contains: "function checkNewLeadEmails"
    - path: "SuggestTechs.js"
      provides: "Tech suggestion module (dev/setup dead code removed)"
      contains: "function suggestTechsForJob"
  key_links:
    - from: "Code.js routeLead"
      to: "sendInspectionSummary"
      via: "live call (kept)"
      pattern: "sendInspectionSummary"
    - from: "checkNewLeadEmails"
      to: "GAS time trigger"
      via: "production polling loop (must not regress)"
      pattern: "function checkNewLeadEmails"
---

<objective>
Delete the confirmed-safe dead functions from the ROOT clasp project — `Code.js` and `SuggestTechs.js` — using the CONFIRMED SAFE list produced by 15-01. Remove the associated tombstone comments and the one dead call site (sendAutoReply inside routeLead). Then push and deploy the root GAS project once and verify a clean `checkNewLeadEmails()` trigger run.

Purpose: Code.js and SuggestTechs.js are the same root clasp project, so they push/deploy together in a single manual deploy (per D-04/D-05). This plan removes their dead code in one coherent unit and confirms the live polling loop still runs.

Output: leaner Code.js + SuggestTechs.js, root project deployed at a new version, clean execution log.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-gas-migration-phase-a-dead-code-cleanup/15-CONTEXT.md
@.planning/phases/15-gas-migration-phase-a-dead-code-cleanup/15-01-SUMMARY.md
@docs/GAS_MIGRATION_SCOPE.md
@CLAUDE.md
@RULES.md
</context>

<artifacts_this_phase_produces>
Functions REMOVED from Code.js (confirmed-safe, per 15-01):
`updateJob()`, `archiveJob()`, `getDispatchData()`, `getDispatchHeaders()`, `doGet()` (HTML-serving, ~line 1232), `sendAutoReply()` (+ its dead call site in routeLead ~line 752), `buildSig()`, `getNewContactsData()`, `getLeadsHeaders()`, `getReviewHeaders()`, `setupAuditTrigger()`, and tombstone comment blocks: `backfillDispatchMsgIds`, `catchUpMissedEmails`, `draftTenantContact`, `sendUrgentAlert`, `sendTurnoverFlag`.

Functions REMOVED from SuggestTechs.js: `setupTradeDurationSheet()`, `testSuggestTechs()`.

NOT removed (EXCLUDE — live callers): `getTodayStr()` in SuggestTechs.js (called by suggestTechsForJob, migrate). Any Code.js function not on the 15-01 CONFIRMED SAFE list.
NEVER removed: `checkNewLeadEmails`, `routeLead`, `sendInspectionSummary`, `setupTrigger`, and all keep/migrate functions.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Branch gate + delete confirmed-safe dead functions from Code.js</name>
  <files>Code.js</files>
  <read_first>
    - C:/PTOW/1_APT_Central_Command/.planning/phases/15-gas-migration-phase-a-dead-code-cleanup/15-01-SUMMARY.md (CONFIRMED SAFE + EXCLUDE lists — binding)
    - C:/PTOW/1_APT_Central_Command/Code.js (full file)
    - C:/PTOW/1_APT_Central_Command/dashboard-api/DashboardAPI.gs (cross-file caller re-check)
    - C:/PTOW/1_APT_Central_Command/SuggestTechs.js (cross-file caller re-check)
    - C:/PTOW/1_APT_Central_Command/docs/GAS_MIGRATION_SCOPE.md
    - C:/PTOW/1_APT_Central_Command/RULES.md (NEVER_RUN list)
  </read_first>
  <action>
    Branch gate first: `git branch --show-current` must equal `feat/gas-phase-a-dead-code` (create it from main if needed: `git checkout -b feat/gas-phase-a-dead-code`). `git ls-remote --heads origin feat/gas-phase-a-dead-code` — if empty, push the branch. Never work on main (branch-rule invariant).

    For EACH function on the Code.js CONFIRMED SAFE list from 15-01, BEFORE deleting, re-run the caller-graph check: `grep -rn "\b<fn>\b" Code.js SuggestTechs.js dashboard-api/DashboardAPI.gs`. If any hit is a CALL (not a comment, not the definition) inside a keep/migrate function, STOP and do not delete that function — flag the discrepancy with 15-01. Otherwise delete the entire function body.

    Functions to delete (re-verify each): updateJob, archiveJob, getDispatchData, getDispatchHeaders, doGet (the HTML-serving one near line 1232 — confirm it serves HtmlService/dashboard, NOT a TechPWA entry point), sendAutoReply, buildSig, getNewContactsData, getLeadsHeaders, getReviewHeaders, setupAuditTrigger. When deleting sendAutoReply, ALSO remove the dead guarded call site in routeLead (`if (AUTO_REPLY_ENABLED) sendAutoReply(...)` near line 752) so no dangling reference remains; leave the live `sendInspectionSummary` call in routeLead intact. Remove the five tombstone comment blocks (backfillDispatchMsgIds, catchUpMissedEmails, draftTenantContact, sendUrgentAlert, sendTurnoverFlag) per D-03.

    Do NOT touch: checkNewLeadEmails, detectLaphamForm, parseWithGemini, routeLead (except removing the dead sendAutoReply line), sendInspectionSummary, addToDispatchQueue, logToSheet, setupTrigger, or any function not on the CONFIRMED SAFE list. Do not delete any NEVER_RUN trigger setup that is still wired. Do not modify migration-status of remaining functions (D-01).
  </action>
  <verify>
    <automated>cd C:/PTOW/1_APT_Central_Command && for fn in updateJob archiveJob getDispatchData getDispatchHeaders sendAutoReply buildSig getNewContactsData getLeadsHeaders getReviewHeaders setupAuditTrigger; do if grep -qE "^function $fn\b" Code.js; then echo "FAIL: $fn still defined"; exit 1; fi; done; grep -qE "^function checkNewLeadEmails" Code.js && grep -qE "^function routeLead" Code.js && ! grep -q "sendAutoReply" Code.js && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^function (updateJob|archiveJob|getDispatchData|getDispatchHeaders|sendAutoReply|buildSig|getNewContactsData|getLeadsHeaders|getReviewHeaders|setupAuditTrigger)\b" Code.js` returns nothing.
    - `grep -n "sendAutoReply" Code.js` returns nothing (function AND the routeLead call site both gone).
    - `grep -nE "^function (checkNewLeadEmails|routeLead|sendInspectionSummary|setupTrigger)\b" Code.js` still returns all four (live functions intact).
    - The five tombstone comment lines are gone: `grep -n "function draftTenantContact\|function sendUrgentAlert\|function sendTurnoverFlag\|function backfillDispatchMsgIds\|function catchUpMissedEmails" Code.js` returns nothing.
    - Current branch is `feat/gas-phase-a-dead-code`.
  </acceptance_criteria>
  <done>All confirmed-safe Code.js dead functions and the dead sendAutoReply call site are removed; live polling functions remain.</done>
</task>

<task type="auto">
  <name>Task 2: Delete confirmed-safe dead functions from SuggestTechs.js</name>
  <files>SuggestTechs.js</files>
  <read_first>
    - C:/PTOW/1_APT_Central_Command/.planning/phases/15-gas-migration-phase-a-dead-code-cleanup/15-01-SUMMARY.md (SuggestTechs verdicts)
    - C:/PTOW/1_APT_Central_Command/SuggestTechs.js (full file)
    - C:/PTOW/1_APT_Central_Command/dashboard-api/DashboardAPI.gs (cross-file caller re-check)
    - C:/PTOW/1_APT_Central_Command/Code.js (cross-file caller re-check)
  </read_first>
  <action>
    Re-run the caller-graph check for the three SuggestTechs.js delete candidates: `grep -rn "\b<fn>\b" Code.js SuggestTechs.js dashboard-api/DashboardAPI.gs`.

    Delete `setupTradeDurationSheet()` (one-time setup) and `testSuggestTechs()` (dev harness) — both confirmed SAFE in 15-01. When removing setupTradeDurationSheet, leave the Logger string at line ~230 that merely mentions its name as guidance text, OR update that string to drop the stale reference (your discretion — do not break the surrounding live function).

    Do NOT delete `getTodayStr()` — it is EXCLUDE: called by `suggestTechsForJob` (migrate status) at line ~102. Per D-01, leave it in place. Do not touch suggestTechsForJob, buildTechScores, loadSkillRatings, or any other migrate function.
  </action>
  <verify>
    <automated>cd C:/PTOW/1_APT_Central_Command && ! grep -qE "^function setupTradeDurationSheet\b" SuggestTechs.js && ! grep -qE "^function testSuggestTechs\b" SuggestTechs.js && grep -qE "^function getTodayStr\b" SuggestTechs.js && grep -qE "^function suggestTechsForJob\b" SuggestTechs.js && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^function (setupTradeDurationSheet|testSuggestTechs)\b" SuggestTechs.js` returns nothing.
    - `grep -nE "^function getTodayStr\b" SuggestTechs.js` STILL returns a hit (EXCLUDE — kept).
    - `grep -nE "^function suggestTechsForJob\b" SuggestTechs.js` still returns a hit.
  </acceptance_criteria>
  <done>SuggestTechs.js dev/setup dead code removed; getTodayStr and all migrate functions preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Code.js + SuggestTechs.js dead code removed locally on feat/gas-phase-a-dead-code. Ready to push and MANUALLY deploy the ROOT clasp project (email triggers live here — manual deploy only, D-05).</what-built>
  <how-to-verify>
    1. Confirm local diff is deletions only (no logic changes): `git -C C:/PTOW/1_APT_Central_Command diff Code.js SuggestTechs.js` — every change should be a removed function or comment.
    2. Push the root project: from repo root run `clasp push --force`. Confirm it reports the two changed files pushed with no syntax error.
    3. Deploy with a version bump (EXACT command, manual only):
       `clasp deploy --deploymentId AKfycbyFgHHDrZm1NZBG2iQ3czdeRxSGuvjkqyLkx7OCjdkA5vRBCm3IQ1RAEyrbgVp-Y4xs1g --description "v## — Phase A dead code (Code.js + SuggestTechs.js)"` (use the next sequential v number).
    4. Per D-06: wait for / manually run one `checkNewLeadEmails()` trigger cycle, then open the GAS execution log. Confirm NO "ReferenceError" / "is not defined" / "is not a function" errors referencing any deleted symbol.
  </how-to-verify>
  <resume-signal>Type "approved" once clasp push + deploy succeeded AND the execution log shows a clean checkNewLeadEmails() run, or paste the error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GAS deploy → production trigger | The root project runs the live inbound-email polling loop as workorder@. A bad deploy could break lead intake. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02a | Denial of Service | Deleting a function still called by checkNewLeadEmails breaks lead polling | mitigate | Per-function grep re-check before delete (Task 1/2); all deletes restricted to 15-01 CONFIRMED SAFE list; post-deploy execution-log verification (D-06) is a blocking human checkpoint. |
| T-15-02b | Tampering | Dangling reference (sendAutoReply call site left in routeLead) | mitigate | Task 1 explicitly removes the routeLead call site; verify gate asserts `! grep sendAutoReply Code.js`. |
| T-15-02c | Repudiation | Wrong deployment version / wrong project | mitigate | Exact deploymentId pinned in checkpoint; manual deploy only (D-05); root project files only (Code.js, SuggestTechs.js). |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | None in this phase. No supply-chain surface. |

## Rollback if a live caller was missed
If the execution log shows a ReferenceError for a deleted symbol: `git checkout Code.js SuggestTechs.js` (or `git revert` the commit), `clasp push --force`, and redeploy the prior version with the same deploymentId. Deletions are pure removals, so reverting restores exact prior behavior. Add the missed function to the EXCLUDE list in 15-01-SUMMARY before retrying.
</threat_model>

<verification>
- All Code.js + SuggestTechs.js CONFIRMED SAFE functions removed; EXCLUDE functions (getTodayStr) retained.
- `clasp push --force` exits 0 for the root project; deploy uses the pinned root deploymentId with a bumped version.
- GAS execution log: one clean checkNewLeadEmails() run, zero reference errors (D-06).
- `git diff` shows deletions only — no logic edits to live functions beyond the removed sendAutoReply call line.
</verification>

<success_criteria>
Root clasp project deploys cleanly with all confirmed dead code removed and the live polling loop verified error-free.
</success_criteria>

<output>
Create `.planning/phases/15-gas-migration-phase-a-dead-code-cleanup/15-02-SUMMARY.md` when done. Record the deployed version number and the execution-log verification result.
</output>
