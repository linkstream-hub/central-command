---
phase: 15-gas-migration-phase-a-dead-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - artifacts/15_caller_graph.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every delete-candidate function has a documented grep-based caller check across all 3 GAS files"
    - "Functions with a caller in a keep/migrate function are explicitly excluded from deletion"
    - "The confirmed-safe delete list is recorded as a reusable artifact for 15-02 and 15-03"
  artifacts:
    - path: "artifacts/15_caller_graph.md"
      provides: "Per-function caller evidence + confirmed-safe delete list + exclusion list"
      contains: "EXCLUDE"
  key_links:
    - from: "artifacts/15_caller_graph.md"
      to: "docs/GAS_MIGRATION_SCOPE.md"
      via: "delete-status function list"
      pattern: "delete"
---

<objective>
Build a grep-verified caller graph for every function marked `delete` in `docs/GAS_MIGRATION_SCOPE.md` across the three in-scope GAS files (`Code.js`, `SuggestTechs.js`, `dashboard-api/DashboardAPI.gs`). Produce a single artifact that splits the delete candidates into a CONFIRMED-SAFE-TO-DELETE list and an EXCLUDE list (candidates that still have a caller inside a `keep` or `migrate` function and therefore must NOT be deleted in Phase A).

Purpose: Phase A is zero-risk dead-code removal. The only way to guarantee zero behavior change is to prove, per function, that no live (`keep`/`migrate`) function calls it. Several delete candidates are shared helpers with live callers — deleting them would break production. This plan front-loads that verification so the deletion plans (15-02, 15-03) only remove confirmed-dead code.

Output: `artifacts/15_caller_graph.md` — the authoritative confirmed-safe delete list consumed by 15-02 and 15-03.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-gas-migration-phase-a-dead-code-cleanup/15-CONTEXT.md
@docs/GAS_MIGRATION_SCOPE.md
@CLAUDE.md
@RULES.md
</context>

<artifacts_this_phase_produces>
This plan REMOVES nothing. It produces one analysis artifact: `artifacts/15_caller_graph.md`. The functions slated for removal by later plans (15-02, 15-03) are catalogued here with caller evidence. See the EXCLUDE list below for shared helpers that delete candidates depend on but which keep/migrate functions ALSO depend on — those are NOT removed in Phase A.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Grep caller graph for all Code.js + SuggestTechs.js delete candidates</name>
  <files>artifacts/15_caller_graph.md</files>
  <read_first>
    - C:/PTOW/1_APT_Central_Command/docs/GAS_MIGRATION_SCOPE.md (delete-status rows — authoritative candidate list)
    - C:/PTOW/1_APT_Central_Command/Code.js (full file — definitions + call sites)
    - C:/PTOW/1_APT_Central_Command/SuggestTechs.js (full file)
    - C:/PTOW/1_APT_Central_Command/dashboard-api/DashboardAPI.gs (full file — cross-file caller check)
    - C:/PTOW/1_APT_Central_Command/.planning/phases/15-gas-migration-phase-a-dead-code-cleanup/15-CONTEXT.md (D-01 scope, exclusion rules)
  </read_first>
  <action>
    For each Code.js delete candidate (updateJob, archiveJob, getDispatchData, getDispatchHeaders, doGet [HTML serving, ~line 1232], sendAutoReply, getNewContactsData, backfillDispatchMsgIds, catchUpMissedEmails, draftTenantContact [tombstone], sendUrgentAlert [tombstone], sendTurnoverFlag [tombstone], getLeadsHeaders, getReviewHeaders, buildSig, setupAuditTrigger) and each SuggestTechs.js candidate (getTodayStr, setupTradeDurationSheet, testSuggestTechs): run `grep -rn "\b<fn>\b" Code.js SuggestTechs.js dashboard-api/DashboardAPI.gs` from the repo root.

    For every reference found, classify it as: (a) the function definition, (b) a comment/Logger-string mention (not a call), (c) a call site inside a `delete` function, or (d) a call site inside a `keep`/`migrate` function. Use docs/GAS_MIGRATION_SCOPE.md to resolve the migration status of each enclosing function.

    Record the verdict per function in artifacts/15_caller_graph.md: SAFE (no caller in any keep/migrate function) or EXCLUDE (has a live caller). Known traps to verify and document explicitly: (1) sendAutoReply is called at Code.js line ~752 inside routeLead (keep) but gated by `AUTO_REPLY_ENABLED = false` — the call site is dead and must be removed together with sendAutoReply in 15-02; mark sendAutoReply SAFE with the note "remove dead call site at routeLead line ~752 too". (2) buildSig is called only inside sendAutoReply — SAFE once sendAutoReply goes. (3) getTodayStr in SuggestTechs.js is called at line ~102 inside suggestTechsForJob (migrate, NOT delete) — mark EXCLUDE. Per D-01, do not delete EXCLUDE functions in this phase.

    Do NOT modify any GAS file in this task. Analysis only.
  </action>
  <verify>
    <automated>cd C:/PTOW/1_APT_Central_Command && grep -q "EXCLUDE" artifacts/15_caller_graph.md && grep -q "getTodayStr" artifacts/15_caller_graph.md && grep -q "sendAutoReply" artifacts/15_caller_graph.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - artifacts/15_caller_graph.md exists and lists every Code.js and SuggestTechs.js delete candidate with a SAFE or EXCLUDE verdict.
    - getTodayStr is marked EXCLUDE with evidence "called by suggestTechsForJob (migrate) at SuggestTechs.js line ~102".
    - sendAutoReply is marked SAFE with the note to also remove the dead call site in routeLead.
    - `grep -n "function getTodayStr" SuggestTechs.js` confirms the definition still exists (not yet deleted).
  </acceptance_criteria>
  <done>Code.js + SuggestTechs.js candidates each have a documented SAFE/EXCLUDE verdict backed by grep evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Grep caller graph for all DashboardAPI.gs delete candidates + finalize exclusion list</name>
  <files>artifacts/15_caller_graph.md</files>
  <read_first>
    - C:/PTOW/1_APT_Central_Command/dashboard-api/DashboardAPI.gs (full file)
    - C:/PTOW/1_APT_Central_Command/docs/GAS_MIGRATION_SCOPE.md (DashboardAPI.gs delete rows + migration status of all DA functions)
    - C:/PTOW/1_APT_Central_Command/.planning/phases/15-gas-migration-phase-a-dead-code-cleanup/15-CONTEXT.md (daResponse / STATUS_TO_* / doGet / doPost / validateApiKey rules)
  </read_first>
  <action>
    For each DashboardAPI.gs delete candidate (getDispatchDataDA, getJobByIdDA, getTodaySchedule, getWeekSchedule, getLiveFieldStatus, getComplianceStatus, getTechListDA, getJobHistory, getNotificationsDA, updateJobDA, archiveJobDA, validatePasscode, getDQSheet, getTRSheet, getTMSheet, daResponse, backfillScheduledDates, weekDateRange, parseScheduledDate, normalizeStatusForFrontend, normalizeStatusForSheet, rowToJob, sendSmsDA, padDA, fuzzyAddressScoreDA, extractAddressWordsDA, parseTechCellDA, detectColumnsDA, todayStr, setScriptProperties, getCalendarDataDA, STATUS_TO_FRONTEND, STATUS_TO_SHEET): run `grep -rn "\b<fn>\b" dashboard-api/DashboardAPI.gs Code.js SuggestTechs.js` and classify each reference as definition / comment / routing line in doGet|doPost / call inside a delete function / call inside a keep|migrate function.

    Apply these mandatory rules from 15-CONTEXT.md and confirm each against actual grep output:
    - doGet(), doPost(), validateApiKey() in DashboardAPI.gs: NEVER delete — mark EXCLUDE (HTTP entry points / live auth). The 11 migrated read/write action functions are deleted along with ONLY their routing lines inside doGet/doPost, not the entry points themselves.
    - daResponse(): EXCLUDE in Phase A — it is referenced by ~53 sites including many keep/migrate action functions (createManualJobDA, suggestTechsDA, getGmailThreadDA, feedback/comments handlers, tenant scheduling, sentinel write-backs). It can only be deleted after ALL action functions are gone, which is Phase D-onward, not Phase A.
    - getDQSheet / getTRSheet / getTMSheet: EXCLUDE in Phase A — heavily used (17 / 10 / 5 refs) by keep/migrate functions. Verify each has at least one caller in a non-delete function.
    - rowToJob, parseScheduledDate, todayStr, normalizeStatusForFrontend, normalizeStatusForSheet, STATUS_TO_FRONTEND, STATUS_TO_SHEET: check whether their remaining callers (after the 11 read actions are removed) are all delete functions. If ANY caller is keep/migrate (e.g. rowToJob at line ~3562 in tenant scheduling, normalizeStatusForSheet at line ~1159 in createManualJobDA), mark EXCLUDE. Per the specifics in 15-CONTEXT.md, STATUS_TO_* maps delete ONLY if no remaining caller.
    - Scheduling-sheet helpers (padDA, fuzzyAddressScoreDA, extractAddressWordsDA, parseTechCellDA, detectColumnsDA, getCalendarDataDA): these are only used by the dead scheduling-sheet miner (trigger deleted S95). Verify each reference is inside another scheduling-sheet delete function before marking SAFE.

    Append all DA verdicts to artifacts/15_caller_graph.md. Finish the artifact with two clearly headed sections: `## CONFIRMED SAFE TO DELETE` (grouped by file) and `## EXCLUDE (live callers / entry points — do NOT delete in Phase A)` with the reason for each exclusion. This artifact is the binding input for 15-02 and 15-03.
  </action>
  <verify>
    <automated>cd C:/PTOW/1_APT_Central_Command && grep -q "## CONFIRMED SAFE TO DELETE" artifacts/15_caller_graph.md && grep -q "## EXCLUDE" artifacts/15_caller_graph.md && grep -q "daResponse" artifacts/15_caller_graph.md && grep -q "validateApiKey" artifacts/15_caller_graph.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - artifacts/15_caller_graph.md ends with a `## CONFIRMED SAFE TO DELETE` section and a `## EXCLUDE` section.
    - daResponse, getDQSheet, getTRSheet, getTMSheet, doGet, doPost, validateApiKey all appear in the EXCLUDE section with a stated reason.
    - Every function in the CONFIRMED SAFE list has zero callers inside a keep/migrate function, evidenced by the recorded grep output.
    - `grep -c "function " dashboard-api/DashboardAPI.gs` is recorded as the pre-deletion baseline in the artifact.
  </acceptance_criteria>
  <done>The caller-graph artifact cleanly separates safe deletions from must-keep functions, with grep evidence and reasons for every exclusion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan reads source and writes one markdown artifact. No code is executed, no network calls, no GAS deploy, no auth/input handling changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | Incorrect caller-graph leading to a wrong "SAFE" verdict | mitigate | Every SAFE verdict requires recorded grep output proving zero keep/migrate callers; the deletion plans (15-02/15-03) re-run the same grep before deleting (defense in depth). |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — analysis + GAS edits only. No supply-chain surface. |

**Why risk is zero for this plan:** No files are modified except a new analysis artifact. No deploy, no behavior change. The artifact is the safety control for the deletion plans.
</threat_model>

<verification>
- artifacts/15_caller_graph.md exists with both CONFIRMED SAFE and EXCLUDE sections.
- Spot-check: every function in EXCLUDE has at least one grep hit inside a keep/migrate function recorded in the artifact.
- No GAS file modified in this plan: `git diff --name-only` shows only artifacts/15_caller_graph.md (and SUMMARY).
</verification>

<success_criteria>
The confirmed-safe delete list is complete, grep-backed, and ready to drive zero-risk deletions in 15-02 (root project) and 15-03 (dashboard-api project).
</success_criteria>

<output>
Create `.planning/phases/15-gas-migration-phase-a-dead-code-cleanup/15-01-SUMMARY.md` when done. Include the final CONFIRMED SAFE and EXCLUDE lists inline so downstream plans can consume them.
</output>
