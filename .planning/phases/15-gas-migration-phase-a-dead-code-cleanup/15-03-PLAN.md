---
phase: 15-gas-migration-phase-a-dead-code-cleanup
plan: 03
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - dashboard-api/DashboardAPI.gs
autonomous: false
requirements: []
must_haves:
  truths:
    - "Every confirmed-safe dead function in DashboardAPI.gs is removed"
    - "Routing lines for deleted action functions are removed from doGet/doPost, but doGet/doPost/validateApiKey themselves remain"
    - "daResponse, getDQSheet/getTRSheet/getTMSheet, and any helper with a keep/migrate caller are NOT deleted"
    - "dashboard-api clasp project pushes and deploys with a version bump"
  artifacts:
    - path: "dashboard-api/DashboardAPI.gs"
      provides: "Dashboard API GAS project (migrated read-action dead code removed)"
      contains: "function doPost"
  key_links:
    - from: "doPost"
      to: "validateApiKey"
      via: "live auth gate (kept)"
      pattern: "validateApiKey"
    - from: "doPost"
      to: "daResponse"
      via: "live response builder used by remaining keep/migrate actions (kept)"
      pattern: "daResponse"
---

<objective>
Delete the confirmed-safe dead functions from the `dashboard-api/` clasp project (`DashboardAPI.gs`) using the CONFIRMED SAFE list from 15-01, remove only the routing lines for those deleted actions inside doGet/doPost, then push and deploy the dashboard-api project with a version bump. The HTTP entry points (doGet, doPost, validateApiKey) and the shared helpers still used by keep/migrate functions (daResponse, getDQSheet, getTRSheet, getTMSheet, and any rowToJob/status helpers with live callers) are preserved.

Purpose: DashboardAPI.gs is a separate clasp project from the root, so it deploys independently (D-04). The 11 migrated read/write actions plus the scheduling-sheet helpers, legacy passcode auth, stubs, and one-time utilities are dead and removable; the shared response/sheet helpers are NOT, because keep/migrate actions still call them in Phase A.

Output: leaner DashboardAPI.gs, dashboard-api project deployed at a new version.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-gas-migration-phase-a-dead-code-cleanup/15-CONTEXT.md
@.planning/phases/15-gas-migration-phase-a-dead-code-cleanup/15-01-SUMMARY.md
@docs/GAS_MIGRATION_SCOPE.md
@CLAUDE.md
@RULES.md
</context>

<artifacts_this_phase_produces>
Functions REMOVED from DashboardAPI.gs (confirmed-safe, per 15-01):
Migrated read/write actions: `getDispatchDataDA()`, `getJobByIdDA()`, `getTodaySchedule()`, `getWeekSchedule()`, `getLiveFieldStatus()`, `getComplianceStatus()`, `getTechListDA()`, `getJobHistory()`, `getNotificationsDA()`, `updateJobDA()`, `archiveJobDA()`.
Legacy/stub/one-time: `validatePasscode()`, `sendSmsDA()` (stub), `backfillScheduledDates()`, `setScriptProperties()`.
Scheduling-sheet helpers (miner dead since S95): `getCalendarDataDA()`, `padDA()`, `fuzzyAddressScoreDA()`, `extractAddressWordsDA()`, `parseTechCellDA()`, `detectColumnsDA()`.
Action-only helpers IF (and only if) 15-01 confirmed zero keep/migrate callers remain: `weekDateRange()`, `parseScheduledDate()`, `normalizeStatusForFrontend()`, `normalizeStatusForSheet()`, `rowToJob()`, `todayStr()`, `STATUS_TO_FRONTEND`, `STATUS_TO_SHEET`.
Plus the routing lines inside doGet/doPost that dispatch to any deleted action.

NOT removed (EXCLUDE per 15-01): `doGet()`, `doPost()`, `validateApiKey()` (entry points / live auth), `daResponse()` (~53 callers including keep/migrate), `getDQSheet()`/`getTRSheet()`/`getTMSheet()` (live callers), and any of the action-only helpers above that 15-01 found still has a keep/migrate caller.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Delete confirmed-safe migrated actions + legacy/stub/setup functions from DashboardAPI.gs</name>
  <files>dashboard-api/DashboardAPI.gs</files>
  <read_first>
    - C:/PTOW/1_APT_Central_Command/.planning/phases/15-gas-migration-phase-a-dead-code-cleanup/15-01-SUMMARY.md (CONFIRMED SAFE + EXCLUDE — binding)
    - C:/PTOW/1_APT_Central_Command/dashboard-api/DashboardAPI.gs (full file)
    - C:/PTOW/1_APT_Central_Command/Code.js (cross-file caller re-check)
    - C:/PTOW/1_APT_Central_Command/SuggestTechs.js (cross-file caller re-check)
    - C:/PTOW/1_APT_Central_Command/docs/GAS_MIGRATION_SCOPE.md
  </read_first>
  <action>
    Branch gate: ensure current branch is `feat/gas-phase-a-dead-code` (same branch as 15-02 if running after, or create from main). Never commit to main.

    For EACH function below, re-run the caller-graph check before deleting: `grep -rn "\b<fn>\b" dashboard-api/DashboardAPI.gs Code.js SuggestTechs.js`. Delete only if the sole remaining references are the definition, comments, and the routing line(s) inside doGet/doPost. If a CALL exists inside a keep/migrate function, STOP and flag against 15-01.

    Delete these function bodies (all on the CONFIRMED SAFE list): getDispatchDataDA, getJobByIdDA, getTodaySchedule, getWeekSchedule, getLiveFieldStatus, getComplianceStatus, getTechListDA, getJobHistory, getNotificationsDA, updateJobDA, archiveJobDA, validatePasscode, sendSmsDA (stub), backfillScheduledDates, setScriptProperties, getCalendarDataDA, padDA, fuzzyAddressScoreDA, extractAddressWordsDA, parseTechCellDA, detectColumnsDA.

    For EACH deleted action, also remove its dispatch line inside doGet and/or doPost (e.g. `if (action === "getDispatchData") return daResponse(getDispatchDataDA());`). Remove ONLY those specific routing lines — do NOT remove or restructure doGet/doPost/validateApiKey themselves (EXCLUDE — entry points and live auth). Do not remove routing lines for actions that map to keep/migrate functions.

    Do NOT delete daResponse, getDQSheet, getTRSheet, getTMSheet (EXCLUDE — live callers). Leave all keep/migrate functions untouched (createManualJobDA, getGmailThreadDA, replyToThreadDA, getDraftReplyDA, suggestTechsDA + scoring helpers, feedback/comments handlers, tenant scheduling, sentinel write-backs, timecard, TOM, push, etc.). Do not change migration status of survivors (D-01).
  </action>
  <verify>
    <automated>cd C:/PTOW/1_APT_Central_Command && for fn in getDispatchDataDA getJobByIdDA getTodaySchedule getWeekSchedule getLiveFieldStatus getComplianceStatus getTechListDA getJobHistory getNotificationsDA updateJobDA archiveJobDA validatePasscode sendSmsDA backfillScheduledDates setScriptProperties getCalendarDataDA padDA fuzzyAddressScoreDA extractAddressWordsDA parseTechCellDA detectColumnsDA; do if grep -qE "^function $fn\b" dashboard-api/DashboardAPI.gs; then echo "FAIL: $fn still defined"; exit 1; fi; done; grep -qE "^function doGet\b" dashboard-api/DashboardAPI.gs && grep -qE "^function doPost\b" dashboard-api/DashboardAPI.gs && grep -qE "^function validateApiKey\b" dashboard-api/DashboardAPI.gs && grep -qE "^function daResponse\b" dashboard-api/DashboardAPI.gs && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - None of the 21 listed functions is still defined: the verify command exits PASS.
    - doGet, doPost, validateApiKey, daResponse ALL still defined (EXCLUDE — kept).
    - getDQSheet/getTRSheet/getTMSheet still defined: `grep -nE "^function (getDQSheet|getTRSheet|getTMSheet)\b" dashboard-api/DashboardAPI.gs` returns all three.
    - No `action === "<deleted>"` routing line remains for any deleted action: `grep -n 'action === "getDispatchData"' dashboard-api/DashboardAPI.gs` and the same for updateJob/archiveJob/getTodaySchedule return nothing.
  </acceptance_criteria>
  <done>All confirmed-safe migrated/legacy/stub/scheduling-sheet functions and their routing lines are removed; entry points and shared helpers preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Conditionally remove action-only helpers + status maps (only if zero keep/migrate callers)</name>
  <files>dashboard-api/DashboardAPI.gs</files>
  <read_first>
    - C:/PTOW/1_APT_Central_Command/.planning/phases/15-gas-migration-phase-a-dead-code-cleanup/15-01-SUMMARY.md (verdicts for weekDateRange, parseScheduledDate, normalizeStatusForFrontend, normalizeStatusForSheet, rowToJob, todayStr, STATUS_TO_*)
    - C:/PTOW/1_APT_Central_Command/dashboard-api/DashboardAPI.gs (full file — post Task 1 state)
  </read_first>
  <action>
    These helpers were used by the actions deleted in Task 1 — BUT some are also called by surviving keep/migrate functions. After Task 1's deletions, re-run for each: `grep -rn "\b<helper>\b" dashboard-api/DashboardAPI.gs`. Candidates: weekDateRange, parseScheduledDate, normalizeStatusForFrontend, normalizeStatusForSheet, rowToJob, todayStr, STATUS_TO_FRONTEND, STATUS_TO_SHEET.

    For each candidate: if the ONLY remaining references are the definition itself (and definitions of other to-be-deleted helpers), delete it. If any reference is a CALL inside a surviving function, do NOT delete — leave it and record it as EXCLUDE in the summary. Known live-caller traps from 15-01 to honor: rowToJob is called inside tenant-scheduling (~line 3562, keep) and the createManualJobDA region; normalizeStatusForSheet is called by createManualJobDA (~line 1159, migrate); normalizeStatusForFrontend is called inside rowToJob. Per the 15-CONTEXT specifics, STATUS_TO_* and daResponse delete ONLY after all callers are gone — in Phase A they are NOT all gone, so default to keeping these unless grep proves zero live callers.

    This task is allowed to delete ZERO helpers if 15-01 found them all still live — that is the correct, zero-risk outcome. Do not force deletions. Record the kept-vs-removed decision per helper in the summary.
  </action>
  <verify>
    <automated>cd C:/PTOW/1_APT_Central_Command && for h in weekDateRange parseScheduledDate normalizeStatusForFrontend normalizeStatusForSheet rowToJob todayStr; do defs=$(grep -cE "^function $h\b" dashboard-api/DashboardAPI.gs 2>/dev/null || echo 0); refs=$(grep -cE "\b$h\b" dashboard-api/DashboardAPI.gs 2>/dev/null || echo 0); if [ "$defs" = "0" ] && [ "$refs" -gt "0" ]; then echo "FAIL: $h deleted but $refs dangling reference(s) remain"; exit 1; fi; echo "$h: defs=$defs refs=$refs OK"; done && echo CHECK_DONE</automated>
  </verify>
  <acceptance_criteria>
    - For every helper deleted, `grep -nE "^function <helper>\b" dashboard-api/DashboardAPI.gs` returns nothing AND there were zero call sites before deletion (recorded in summary).
    - For every helper kept, the summary records the specific surviving caller (function name + line) that justified keeping it.
    - rowToJob, if any keep/migrate caller remains, is NOT deleted: confirm by the recorded caller evidence.
    - No surviving function references a deleted helper (no dangling references): a final `grep` for each deleted helper shows zero remaining references.
  </acceptance_criteria>
  <done>Action-only helpers and status maps are removed only where grep proves zero live callers; all others kept with recorded justification; no dangling references.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>DashboardAPI.gs dead code removed locally on feat/gas-phase-a-dead-code. Ready to push and MANUALLY deploy the dashboard-api clasp project (separate project from root — its own deploymentId).</what-built>
  <how-to-verify>
    1. Confirm the diff is deletions only: `git -C C:/PTOW/1_APT_Central_Command diff dashboard-api/DashboardAPI.gs` — every change is a removed function, removed routing line, or removed helper. No edits to doGet/doPost/validateApiKey bodies beyond removed routing lines.
    2. Push the dashboard-api project: `cd C:/PTOW/1_APT_Central_Command/dashboard-api && clasp push --force`. Confirm no syntax error.
    3. Deploy with a version bump (EXACT command, manual per D-05):
       `clasp deploy --deploymentId AKfycbyum_KLprgPh51GxFiwhsoNHScc4TqIBrzZS0GPfHsnhrc9hAtp03AciyiydhfyJyxCCQ --description "v## — Phase A dead code (DashboardAPI.gs)"` (next sequential v number).
    4. Smoke test a surviving action from the live dashboard (e.g. load /live or a comms thread, which exercise keep/migrate actions through doPost). Confirm no 500 / "is not defined" error in the GAS execution log for the dashboard-api project.
  </how-to-verify>
  <resume-signal>Type "approved" once clasp push + deploy succeeded for dashboard-api AND a surviving dashboard action works with a clean execution log, or paste the error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → DashboardAPI doPost | API-key-gated HTTP endpoint serving the dispatch dashboard. validateApiKey() is the live auth gate and must remain. |
| GAS deploy → production dashboard | A bad deploy could break /live, comms, scheduling, or feedback for office staff. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03a | Denial of Service | Deleting a helper (daResponse/getDQSheet/rowToJob) still called by a keep/migrate action breaks live dashboard actions | mitigate | EXCLUDE list enforced; per-helper grep re-check in Task 2; Task 2 may delete zero helpers; blocking smoke-test checkpoint after deploy. |
| T-15-03b | Elevation of Privilege | Accidentally removing validateApiKey() opens the endpoint | mitigate | validateApiKey explicitly EXCLUDE; verify gate asserts it still defined; checkpoint diff review confirms entry points untouched. |
| T-15-03c | Tampering | Dangling routing line referencing a deleted action function | mitigate | Task 1 removes routing lines with each action; acceptance criteria grep for orphaned `action === "<deleted>"` lines. |
| T-15-03d | Repudiation | Wrong project/deploymentId (root vs dashboard-api) | mitigate | dashboard-api deploymentId pinned; push runs from dashboard-api/ dir; files_modified limited to DashboardAPI.gs. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | None in this phase. No supply-chain surface. |

## Rollback if a live caller was missed
If a dashboard action 500s with a ReferenceError after deploy: `git checkout dashboard-api/DashboardAPI.gs` (or `git revert`), `cd dashboard-api && clasp push --force`, redeploy the prior version with the same dashboard-api deploymentId. Deletions are pure removals — revert restores exact prior behavior. Move the missed symbol to EXCLUDE in 15-01-SUMMARY before retrying.
</threat_model>

<verification>
- All 21 confirmed-safe DashboardAPI.gs functions removed; doGet/doPost/validateApiKey/daResponse/getDQSheet/getTRSheet/getTMSheet retained.
- No orphaned routing lines for deleted actions; no dangling helper references.
- `cd dashboard-api && clasp push --force` exits 0; deploy uses the pinned dashboard-api deploymentId with a bumped version.
- A surviving dashboard action works post-deploy with a clean execution log.
</verification>

<success_criteria>
The dashboard-api clasp project deploys cleanly with all confirmed dead code removed, entry points and shared helpers intact, and live dashboard actions verified working.
</success_criteria>

<output>
Create `.planning/phases/15-gas-migration-phase-a-dead-code-cleanup/15-03-SUMMARY.md` when done. Record the deployed version number, the per-helper keep/remove decisions from Task 2, and the smoke-test result.
</output>
