---
phase: 15-gas-migration-phase-a-dead-code-cleanup
plan: 04
type: execute
wave: 3
depends_on:
  - 15-02
  - 15-03
files_modified:
  - artifacts/ag_diff.txt
  - artifacts/ag_test_results.txt
autonomous: false
requirements: []
must_haves:
  truths:
    - "tech-pwa typecheck is clean (proves no Next.js code was touched)"
    - "Full deletion diff is captured for Claude Code review"
    - "Both GAS projects' execution logs are confirmed error-free after deploy"
    - "PR is merged only after explicit Claude Code clear"
  artifacts:
    - path: "artifacts/ag_diff.txt"
      provides: "Complete main...HEAD diff of the deletion work"
    - path: "artifacts/ag_test_results.txt"
      provides: "Observed verification evidence per success criterion"
  key_links:
    - from: "artifacts/ag_test_results.txt"
      to: "GAS execution logs"
      via: "checkNewLeadEmails + dashboard action verification"
      pattern: "checkNewLeadEmails"
---

<objective>
Close Phase A: prove no Next.js code was touched (tsc clean), capture the full deletion diff and test evidence as artifacts for Claude Code review, and merge only after an explicit clear. These are the mandatory terminal-3 tasks from CLAUDE.md, adapted for a GAS-only deletion phase.

Purpose: The deletion work in 15-02/15-03 touches only GAS files, so the tech-pwa typecheck must remain clean (a regression there would mean scope leaked into Next.js, violating SC-4). The diff + test artifacts are the merge-gate inputs for Claude Code per the co-lead workflow.

Output: artifacts/ag_diff.txt, artifacts/ag_test_results.txt, and a merged PR after clear.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-gas-migration-phase-a-dead-code-cleanup/15-CONTEXT.md
@.planning/phases/15-gas-migration-phase-a-dead-code-cleanup/15-02-SUMMARY.md
@.planning/phases/15-gas-migration-phase-a-dead-code-cleanup/15-03-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan REMOVES no GAS code. It produces two review artifacts (ag_diff.txt, ag_test_results.txt) and merges the PR that carries all Phase A deletions from 15-02 and 15-03.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task N-2: Typecheck clean + push + capture diff (CLAUDE.md terminal task)</name>
  <files>artifacts/ag_diff.txt</files>
  <read_first>
    - C:/PTOW/1_APT_Central_Command/CLAUDE.md (terminal-3 task spec, AG SPEC REQUIREMENTS)
    - C:/PTOW/1_APT_Central_Command/.planning/phases/15-gas-migration-phase-a-dead-code-cleanup/15-02-SUMMARY.md
    - C:/PTOW/1_APT_Central_Command/.planning/phases/15-gas-migration-phase-a-dead-code-cleanup/15-03-SUMMARY.md
  </read_first>
  <action>
    Confirm branch is `feat/gas-phase-a-dead-code`. Run `cd tech-pwa && npx tsc --noEmit` — must report ZERO errors. Because Phase A touches only GAS files (Code.js, SuggestTechs.js, dashboard-api/DashboardAPI.gs), tsc proves no Next.js scope leak (SC-4). If tsc reports errors, STOP — they indicate accidental scope leak; do not proceed.

    Then `git push` the branch, and capture the full diff: `git diff main...HEAD > artifacts/ag_diff.txt`. Verify the diff contains ONLY deletions/removals in the three GAS files plus the planning artifacts (no additions to live GAS logic, no tech-pwa source changes). Commit the diff artifact. Post a summary to Claude Code (diff path + file list + net lines removed) and STOP for diff review.
  </action>
  <verify>
    <automated>cd C:/PTOW/1_APT_Central_Command/tech-pwa && npx tsc --noEmit && cd C:/PTOW/1_APT_Central_Command && test -s artifacts/ag_diff.txt && ! grep -qE "^\+.*function (checkNewLeadEmails|doPost|validateApiKey)" artifacts/ag_diff.txt && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `cd tech-pwa && npx tsc --noEmit` exits 0 (zero errors).
    - artifacts/ag_diff.txt exists, is non-empty, and `git diff --stat main...HEAD` shows only Code.js, SuggestTechs.js, dashboard-api/DashboardAPI.gs (plus .planning/ + artifacts/). No file under tech-pwa/src is in the diff.
    - The diff is overwhelmingly deletions (leading `-` lines) — no new live GAS function bodies added.
    - Branch pushed: `git ls-remote --heads origin feat/gas-phase-a-dead-code` is non-empty.
  </acceptance_criteria>
  <done>tsc is clean, branch pushed, full diff captured and posted to Claude Code for review.</done>
</task>

<task type="auto">
  <name>Task N-1: Test sprint — record observed verification evidence (CLAUDE.md terminal task)</name>
  <files>artifacts/ag_test_results.txt</files>
  <read_first>
    - C:/PTOW/1_APT_Central_Command/CLAUDE.md (terminal task spec — "I saw X change to Y" evidence standard)
    - C:/PTOW/1_APT_Central_Command/.planning/phases/15-gas-migration-phase-a-dead-code-cleanup/15-02-SUMMARY.md (root deploy version + log result)
    - C:/PTOW/1_APT_Central_Command/.planning/phases/15-gas-migration-phase-a-dead-code-cleanup/15-03-SUMMARY.md (dashboard-api deploy version + smoke test)
  </read_first>
  <action>
    Write artifacts/ag_test_results.txt with concrete observed evidence per Phase 15 success criterion (no activity descriptions — actual observations):
    - SC-1 (all delete-status functions removed): paste the grep verification output from 15-02 and 15-03 showing each targeted function returns no definition, and each EXCLUDE function (daResponse, getDQSheet/TR/TM, doGet, doPost, validateApiKey, getTodayStr) still defined.
    - SC-2 (clasp push succeeds both projects): record both deploy version numbers (root + dashboard-api) and that clasp push exited 0 for each.
    - SC-3 (clean execution log): paste/transcribe the checkNewLeadEmails() execution-log result (root, from 15-02) and the dashboard-action smoke-test log result (dashboard-api, from 15-03) — both showing zero reference errors.
    - SC-4 (no Next.js/n8n changes): record `git diff --stat main...HEAD` confirming zero tech-pwa/src or n8n files changed, and tsc clean from Task N-2.
    Per D-07, no Playwright run is required. Kill any running dev server. Post the results path to Claude Code and STOP for test review.
  </action>
  <verify>
    <automated>cd C:/PTOW/1_APT_Central_Command && test -s artifacts/ag_test_results.txt && grep -q "SC-1" artifacts/ag_test_results.txt && grep -q "SC-3" artifacts/ag_test_results.txt && grep -q "checkNewLeadEmails" artifacts/ag_test_results.txt && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - artifacts/ag_test_results.txt covers SC-1 through SC-4 with observed evidence (grep output, version numbers, execution-log transcripts).
    - Both GAS deploy version numbers are recorded.
    - The execution-log evidence explicitly states "no reference errors" with the function context (checkNewLeadEmails for root, a named dashboard action for dashboard-api).
    - `git diff --stat main...HEAD` confirming no tech-pwa/src changes is included.
  </acceptance_criteria>
  <done>Test evidence artifact records concrete observations for all four success criteria and is posted to Claude Code.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <what-built>Phase A deletions complete, both GAS projects deployed and verified, diff + test artifacts posted. PR ready for the Claude Code merge gate.</what-built>
  <how-to-verify>
    1. Claude Code reviews artifacts/ag_diff.txt and artifacts/ag_test_results.txt.
    2. Claude Code confirms: deletions only, entry points + shared helpers intact, both execution logs clean, no tech-pwa scope leak.
    3. Claude Code issues an explicit "Clear to merge" or "BLOCK".
    4. ONLY after "Clear to merge": merge the PR for feat/gas-phase-a-dead-code into main via `gh pr merge`. Never merge without the explicit clear (co-lead workflow invariant).
  </how-to-verify>
  <resume-signal>Claude Code: type "Clear to merge" to authorize the merge, or "BLOCK: <reason>".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| branch → main (merge gate) | Merging unreviewed deletions could ship a broken GAS project to production. The Claude Code clear is the control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04a | Tampering | Scope leak into Next.js disguised in the diff | mitigate | tsc --noEmit gate + `git diff --stat` asserts only GAS + planning files changed (SC-4). |
| T-15-04b | Repudiation | Merge without review / rubber-stamp | mitigate | Blocking human-action merge gate requires explicit "Clear to merge"; diff + test artifacts are the evidence basis (AG rubber-stamp prevention). |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | None in this phase. `npx tsc` uses repo-owned dependencies only; no new installs. |

**Why risk is zero for this plan:** No GAS code is modified here. tsc is read-only verification; the only writes are two artifact files. The merge gate is a human control, not an automated push.
</threat_model>

<verification>
- `cd tech-pwa && npx tsc --noEmit` exits 0.
- artifacts/ag_diff.txt and artifacts/ag_test_results.txt exist, non-empty, and cover all four success criteria.
- Diff scope is GAS + planning files only — no tech-pwa/src or n8n changes.
- PR merged only after explicit Claude Code "Clear to merge".
</verification>

<success_criteria>
Phase A is closed: tsc clean (no scope leak), both GAS projects deployed and verified error-free, evidence captured, and the PR merged under the co-lead gate.
</success_criteria>

<output>
Create `.planning/phases/15-gas-migration-phase-a-dead-code-cleanup/15-04-SUMMARY.md` when done. Record tsc result, diff stat, both deploy versions, and the merge outcome.
</output>
