---
phase: 28-sentinel-diet
plan: 03
type: execute
wave: 2
depends_on: ["28-02"]
files_modified:
  - tools/neon/railway_delete_sentinels.cjs
  - .planning/phases/28-sentinel-diet/SENTINEL_INVENTORY.md
  - artifacts/phase28_03_diff.txt
autonomous: false
requirements: [SENT-01, SENT-03]
must_haves:
  truths:
    - "All four (or however many discovered) Railway sentinel services are DELETED — no 24/7 polling service remains (SENT-01 fully satisfied)"
    - "Deletion happens ONLY after Wave 1 n8n parity + Neon-idle is confirmed (Pitfall 5 — no premature deletion)"
    - "A pre-ship vs 48h-post-ship Neon consumption delta proves the compute graph flatlines and projected monthly consumption fits the 100 CU-hr allowance (SENT-03 phase gate)"
    - "SENT-03 evidence is recorded in SENTINEL_INVENTORY.md as the durable phase-gate artifact"
  artifacts:
    - path: "tools/neon/railway_delete_sentinels.cjs"
      provides: "Brandon-runnable Railway GraphQL service-deletion script for the discovered sentinel service IDs"
      contains: "serviceDelete"
    - path: ".planning/phases/28-sentinel-diet/SENTINEL_INVENTORY.md"
      provides: "Appended SENT-03 evidence section — pre/post CU-hours delta + monthly projection + deletion confirmation"
      contains: "SENT-03"
  key_links:
    - from: "tools/neon/railway_delete_sentinels.cjs"
      to: "Railway GraphQL v2 serviceDelete mutation"
      via: "fetch with RAILWAY_TOKEN from .env.local; service IDs from SENTINEL_INVENTORY.md"
      pattern: "serviceDelete"
    - from: "tools/neon/neon_diagnostics.cjs output (pre + post)"
      to: "SENT-03 evidence in SENTINEL_INVENTORY.md"
      via: "Claude Code computes CU-hours delta and monthly projection"
      pattern: "CU-hour"
---

<objective>
Retire the four paused Railway sentinel services permanently (delete, not sleep — CONTEXT.md locked decision) and capture the SENT-03 phase-gate evidence: a verified Neon compute flatline between runs plus a monthly projection that fits the 100 CU-hr allowance with headroom.

Deletion runs ONLY after Wave 1 confirmed n8n parity and Neon-idle (RESEARCH.md Pitfall 5 — deleting before parity has no rollback path). This plan is gated on 28-02's confirmation.

Purpose: Complete SENT-01 (no 24/7 polling services remain) and prove SENT-03 (compute graph flatlines, projected month fits allowance) using `neon_diagnostics.cjs` deltas before and 48h after Wave 1 activation.

Output: `tools/neon/railway_delete_sentinels.cjs` (Brandon-runnable deletion script), SENT-03 evidence appended to `SENTINEL_INVENTORY.md`, `artifacts/phase28_03_diff.txt` (diff gate).
</objective>

<execution_context>
@C:/PTOW/1_APT_Central_Command/.claude/gsd-core/workflows/execute-plan.md
@C:/PTOW/1_APT_Central_Command/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-sentinel-diet/28-RESEARCH.md
@.planning/phases/28-sentinel-diet/SENTINEL_INVENTORY.md
@.planning/phases/28-sentinel-diet/28-VALIDATION.md

# Reference patterns — Railway GraphQL Brandon-run script + the existing Neon consumption diagnostics
@tools/n8n/railway_upsert_vars.cjs
@tools/neon/neon_diagnostics.cjs
---
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Railway sentinel deletion script (tools/neon/railway_delete_sentinels.cjs)</name>
  <read_first>
    - tools/n8n/railway_upsert_vars.cjs (Railway GraphQL Brandon-run pattern — env load, fetch, project ID)
    - .planning/phases/28-sentinel-diet/SENTINEL_INVENTORY.md (the discovered sentinel service IDs to delete — read them from the inventory rows)
  </read_first>
  <action>
    Create `tools/neon/railway_delete_sentinels.cjs` following the railway_upsert_vars.cjs pattern: read `RAILWAY_TOKEN` from `tech-pwa/.env.local` in-process via the same fs+regex parse; never echo it; never accept it via argv. Hardcode `projectId = 'c905a353-0927-4eeb-85f1-11c11d392a08'`.

    The script holds an array `SENTINEL_SERVICE_IDS` of the service IDs recovered in SENTINEL_INVENTORY.md (Wave 0). Transcribe those exact IDs into the array as a named const with a comment mapping each id to its service name, so the deletion target list is auditable in the diff. If the inventory has fewer than four (some already gone), include only the ids present.

    SAFETY GUARD (mandatory): the script must NOT run blind. At the top, require an explicit confirmation: only proceed when invoked as `node tools/neon/railway_delete_sentinels.cjs --confirm`. Without `--confirm`, print the target service id+name list and the message "DRY RUN — re-run with --confirm to delete. Only do this AFTER Wave 1 parity + SENT-03 idle are confirmed (Pitfall 5)." and exit 0 without deleting.

    With `--confirm`, for each id run the Railway GraphQL `serviceDelete(id: $id)` mutation (POST to `https://backboard.railway.app/graphql/v2`, Bearer token). Print per service: `<name> (<id>) -> deleted` or `ERROR: <first 200 chars>`. Continue past individual errors. End with a summary count and the line "Re-run neon_diagnostics.cjs 48h post-Wave-1-activation to capture SENT-03 delta."

    Do NOT run the script. It is destructive and Brandon-only.
  </action>
  <verify>
    <automated>node -c tools/neon/railway_delete_sentinels.cjs && node tools/neon/railway_delete_sentinels.cjs 2>&1 | findstr /C:"DRY RUN" && echo GUARD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `node -c tools/neon/railway_delete_sentinels.cjs` exits 0.
    - Running WITHOUT `--confirm` prints "DRY RUN" + the target list and deletes nothing (safety guard present — Pitfall 5).
    - Source contains the literal project ID `c905a353-0927-4eeb-85f1-11c11d392a08` and a `serviceDelete` mutation.
    - `SENTINEL_SERVICE_IDS` array transcribes the exact ids from SENTINEL_INVENTORY.md with name comments.
    - Reads `RAILWAY_TOKEN` from `tech-pwa/.env.local`; never echoes it.
    - NOTE: the dry-run verify command must NOT require RAILWAY_TOKEN to reach the DRY RUN branch — ensure the `--confirm` / dry-run check happens BEFORE the token-required code path so the guard prints even without creds. (If token-load precedes the guard, move the guard above it.)
  </acceptance_criteria>
  <done>Deletion script exists, syntax-valid, dry-run-guarded, targets the discovered sentinel ids, Brandon-runnable, secret-safe.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Brandon deletes services + captures SENT-03 pre/post delta; Claude Code records evidence</name>
  <read_first>
    - tools/neon/railway_delete_sentinels.cjs (the deletion script Brandon runs)
    - tools/neon/neon_diagnostics.cjs (the SENT-03 consumption + autosuspend reporter Brandon runs pre/post)
    - .planning/phases/28-sentinel-diet/28-VALIDATION.md (§"Manual-Only Verifications" — SENT-03 instruction: pre-ship + 48h-post-ship neon_diagnostics delta)
  </read_first>
  <what-built>
    `tools/neon/railway_delete_sentinels.cjs` (dry-run-guarded Railway deletion for the discovered sentinel service ids). Plus the existing `tools/neon/neon_diagnostics.cjs` is the SENT-03 consumption/autosuspend reporter.
  </what-built>
  <how-to-verify>
    PRECONDITION: Wave 1 (28-02) is confirmed — n8n workflow active, manual execution clean, Neon idles between runs. Do NOT delete before this (Pitfall 5).

    1. SENT-03 baseline: Brandon runs `node tools/neon/neon_diagnostics.cjs` AT Wave-1 activation time (or earliest available) and pastes the output — capture CU-hours used + percent of 100 CU-hr.
    2. After ≥48h of the consolidated workflow running on the work-hours schedule, Brandon runs `node tools/neon/neon_diagnostics.cjs` again and pastes the output.
    3. Claude Code computes: CU-hours consumed over the 48h window = (post CU-hours − baseline CU-hours); daily rate = window_delta / 2; projected month = daily_rate × 30. Confirm projected month < 100 CU-hr with headroom (target ≈ research estimate ~2 CU-hr/day → ~60 CU-hr/month). Confirm `neon_diagnostics.cjs` VERDICT shows all endpoints idle + autosuspend ON + min_cu ≤ 0.25 (graph flatlines between runs).
    4. Once the projection passes, Brandon runs `node tools/neon/railway_delete_sentinels.cjs` (dry run first to confirm targets), then `node tools/neon/railway_delete_sentinels.cjs --confirm` to delete the four services. Brandon pastes the deletion output.
    5. Claude Code appends a `## SENT-03 Evidence` section to `SENTINEL_INVENTORY.md`: baseline CU-hours + timestamp, 48h-post CU-hours + timestamp, computed daily rate, projected monthly CU-hr vs 100 allowance, neon_diagnostics VERDICT line (idle/autosuspend), and the Railway deletion confirmation (which service ids were deleted).
  </how-to-verify>
  <acceptance_criteria>
    - Two `neon_diagnostics.cjs` outputs captured (baseline + ≥48h post) with timestamps.
    - Projected monthly CU-hr computed and recorded as < 100 with headroom (SENT-03 gate met). If projection ≥ 100, Wave 2 deletion is HELD and the inventory records the overage + next step (per CONTEXT deferred: Neon plan upgrade only if diet insufficient) — do NOT delete services in that case.
    - neon_diagnostics VERDICT confirms all endpoints idle + autosuspend ON + min_cu ≤ 0.25 (compute flatlines between runs).
    - Railway deletion output confirms each discovered sentinel service deleted (or documents any that failed).
    - `SENTINEL_INVENTORY.md` has a `## SENT-03 Evidence` section with all the above.
  </acceptance_criteria>
  <resume-signal>Type "approved" once SENT-03 evidence is recorded and deletions confirmed (or held with rationale), or describe issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Terminal gate — commit SENT-03 evidence + diff artifact + STOP</name>
  <read_first>
    - .planning/phases/28-sentinel-diet/SENTINEL_INVENTORY.md (the SENT-03 evidence being committed)
    - tools/neon/railway_delete_sentinels.cjs (the deletion script being committed)
  </read_first>
  <action>
    Verify branch is not `main` (branch gate). Stage `tools/neon/railway_delete_sentinels.cjs` and the updated `.planning/phases/28-sentinel-diet/SENTINEL_INVENTORY.md`. Write `git diff --staged > artifacts/phase28_03_diff.txt`. Commit `feat(phase-28): Wave 2 retire Railway sentinels + SENT-03 evidence`. Push.

    STOP. Post to Claude Code: "Wave 2 complete — Railway sentinels deleted, SENT-03 evidence in SENTINEL_INVENTORY.md (projected <X> CU-hr/month vs 100 allowance). Diff in artifacts/phase28_03_diff.txt. Phase 28 ready for /gsd-verify-work 28." Do NOT merge.
  </action>
  <verify>
    <automated>git rev-parse --abbrev-ref HEAD | findstr /v "^main$" && test -f artifacts/phase28_03_diff.txt && findstr /C:"SENT-03" .planning/phases/28-sentinel-diet/SENTINEL_INVENTORY.md && echo GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - Branch is not `main`.
    - `artifacts/phase28_03_diff.txt` exists and is non-empty.
    - `SENTINEL_INVENTORY.md` contains a `SENT-03` evidence section.
    - Commit touches both `railway_delete_sentinels.cjs` and `SENTINEL_INVENTORY.md`.
    - STOP posted; no merge performed (merge is Claude Code's gate per project protocol).
  </acceptance_criteria>
  <done>SENT-03 evidence committed, diff artifact written, STOP posted. Phase 28 ready for verification + merge gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deletion script → Railway GraphQL API | RAILWAY_TOKEN crosses here; destructive serviceDelete mutation |
| diagnostics script → Neon API | NEON_API_KEY (from .ops/credentials.env) crosses here; read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05 | Tampering / Destruction | railway_delete_sentinels.cjs | mitigate | Dry-run guard requires explicit `--confirm`; deletion gated behind confirmed Wave 1 parity + SENT-03 idle (Pitfall 5); target ids transcribed + auditable in diff before run |
| T-28-06 | Information Disclosure | RAILWAY_TOKEN / NEON_API_KEY | mitigate | Both read from local cred files in-process (railway_upsert_vars.cjs + neon_diagnostics.cjs patterns); never echoed/argv |
| T-28-07 | Repudiation | which services were deleted | mitigate | Deletion script prints per-service result; SENT-03 evidence section records deleted ids — auditable record in committed inventory |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | No package installs — existing Node + repo scripts only (RESEARCH.md Package Legitimacy Audit) |
</threat_model>

<verification>
- Deletion script: dry-run-guarded, targets discovered ids, Brandon-only, secret-safe.
- SENT-03: pre/post neon_diagnostics delta → projected monthly < 100 CU-hr with headroom; VERDICT confirms idle + autosuspend.
- Services deleted only after parity confirmed (Pitfall 5); deletion + evidence committed.
</verification>

<success_criteria>
- All discovered Railway sentinel services deleted — no 24/7 polling service remains (SENT-01 complete).
- SENT-03 evidence recorded: compute flatlines between runs, projected month fits 100 CU-hr allowance with headroom.
- Deletion gated behind Wave 1 parity (Pitfall 5 honored — no premature deletion).
- Diff artifact committed; STOP posted; phase ready for /gsd-verify-work 28 + merge gate.
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Consumed by |
|----------|------|-------------|
| Railway deletion script | `tools/neon/railway_delete_sentinels.cjs` | Brandon (run after parity) |
| SENT-03 evidence | `.planning/phases/28-sentinel-diet/SENTINEL_INVENTORY.md` (## SENT-03 Evidence) | /gsd-verify-work 28; phase gate |
| Diff gate artifact | `artifacts/phase28_03_diff.txt` | Claude Code diff review (PASS/BLOCK) |

<output>
Create `.planning/phases/28-sentinel-diet/28-03-SUMMARY.md` when done.
</output>
