---
phase: 28-sentinel-diet
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tools/n8n/railway_read_sentinels.cjs
  - .planning/phases/28-sentinel-diet/SENTINEL_INVENTORY.md
  - artifacts/phase28_01_diff.txt
autonomous: false
requirements: [SENT-01, SENT-02]
must_haves:
  truths:
    - "All four Railway sentinel services (wc-scanner, time-anomaly, stale-job, spec-architect) are enumerated with their service IDs"
    - "For each sentinel, DATABASE_URL presence/absence is recorded — this determines whether it is a Neon poller"
    - "For each Neon-touching sentinel, the SQL/check logic and polling cadence are recovered and written down"
    - "SENTINEL_INVENTORY.md is the single source of truth that Wave 1 builds the n8n workflow from"
  artifacts:
    - path: "tools/n8n/railway_read_sentinels.cjs"
      provides: "Brandon-runnable Railway GraphQL discovery script — lists services, reads env vars, fetches logs/source per sentinel"
      contains: "c905a353-0927-4eeb-85f1-11c11d392a08"
    - path: ".planning/phases/28-sentinel-diet/SENTINEL_INVENTORY.md"
      provides: "Recovered sentinel SQL, cadence, Neon tables, DATABASE_URL presence per service"
      contains: "DATABASE_URL"
  key_links:
    - from: "tools/n8n/railway_read_sentinels.cjs"
      to: "Railway GraphQL v2 API"
      via: "fetch with RAILWAY_TOKEN from tech-pwa/.env.local"
      pattern: "backboard\\.railway\\.app/graphql/v2"
    - from: "SENTINEL_INVENTORY.md"
      to: "Wave 1 (28-02) workflow build"
      via: "recovered SQL becomes the n8n Postgres node query source"
      pattern: "SENTINEL_INVENTORY"
---

<objective>
Recover the live sentinel check logic that exists ONLY inside the four paused Railway containers (sentinel-wc-scanner, sentinel-time-anomaly, sentinel-stale-job, sentinel-spec-architect). None of this logic is committed to the repo — the `Sentinels/` directory is a Cloudflare Worker proxy, NOT the sentinel source (RESEARCH.md Critical Discovery + Pitfall 1). Without this discovery, Wave 1 has no SQL to port.

This plan writes a Brandon-runnable Railway GraphQL discovery script, Brandon runs it, and Claude Code transcribes the output into SENTINEL_INVENTORY.md — the spec input for Wave 1.

Purpose: Eliminate the sentinel-source gap (RESEARCH.md Pitfall 1) before any porting. Determine per-service whether DATABASE_URL is present so spec-architect (RESEARCH.md Pitfall 4 / Open Question 2) is correctly included or excluded from SENT-01/SENT-02 scope.

Output: `tools/n8n/railway_read_sentinels.cjs` (discovery script), `.planning/phases/28-sentinel-diet/SENTINEL_INVENTORY.md` (filled inventory), `artifacts/phase28_01_diff.txt` (diff gate artifact).
</objective>

<execution_context>
@C:/PTOW/1_APT_Central_Command/.claude/gsd-core/workflows/execute-plan.md
@C:/PTOW/1_APT_Central_Command/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/28-sentinel-diet/28-CONTEXT.md
@.planning/phases/28-sentinel-diet/28-RESEARCH.md
@.planning/phases/28-sentinel-diet/28-VALIDATION.md

# Reference pattern — the Brandon-runnable Railway GraphQL script this plan mirrors
@tools/n8n/railway_upsert_vars.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Railway sentinel discovery script (tools/n8n/railway_read_sentinels.cjs)</name>
  <read_first>
    - tools/n8n/railway_upsert_vars.cjs (the exact Brandon-runnable Railway GraphQL pattern — copy its env-loading + fetch structure verbatim)
    - .planning/phases/28-sentinel-diet/28-RESEARCH.md (§"Railway API: list service env vars" code example; §"Wave 0 Discovery Task" question table)
  </read_first>
  <action>
    Create `tools/n8n/railway_read_sentinels.cjs` following the railway_upsert_vars.cjs pattern EXACTLY:
    read `RAILWAY_TOKEN` from `tech-pwa/.env.local` in-process via the same `fs.readFileSync` + regex parse used in railway_upsert_vars.cjs (lines 7-12). NEVER echo the token; never accept it via argv.

    The script queries Railway GraphQL v2 at `https://backboard.railway.app/graphql/v2` with `Authorization: Bearer ${token}`. Hardcode `projectId = 'c905a353-0927-4eeb-85f1-11c11d392a08'` (verified in railway_upsert_vars.cjs line 22). Do NOT hardcode service IDs — they are unknown and the script must discover them.

    Step 1 — enumerate services: run a `project(id:)` query that returns `services { edges { node { id name } } }` and `environments { edges { node { id name } } }`. Print each service name + id and each environment name + id so Brandon (and Claude Code) can see the full topology. Identify the four sentinel services by name match on `sentinel` (case-insensitive substring).

    Step 2 — per sentinel service, for the production/default environment, run the `variables(projectId, environmentId, serviceId)` query (Railway returns a name→value map). Print, for each sentinel: service name, the list of variable NAMES only, and a boolean `hasDatabaseUrl` (true if a `DATABASE_URL` key exists). Do NOT print variable VALUES — values may contain the Neon connection secret. Only print key names + the DATABASE_URL presence boolean.

    Step 3 — per sentinel service, fetch recent deploy logs to recover the SQL/check behavior the container runs at runtime: query `deployments(input:{projectId, serviceId, environmentId}, first:1)` to get the latest deployment id, then call the deployment-logs query (`deploymentLogs(deploymentId:, limit:200)`) and print the log lines. Sentinel containers typically log the queries they run and the findings they emit — this is the recovery surface for the SQL.

    Wrap each GraphQL call in try/catch; on any GraphQL `errors` field, print `ERROR: <first 200 chars>` and continue to the next service rather than aborting — a partial inventory is better than none. End with a `console.log('STOP — paste this entire output back to Claude Code for SENTINEL_INVENTORY.md')` line.

    Do NOT run the script — it requires RAILWAY_TOKEN which only Brandon's environment has. The script is the deliverable; Brandon runs it in Task 2.
  </action>
  <verify>
    <automated>node -c tools/n8n/railway_read_sentinels.cjs && echo SYNTAX_OK</automated>
  </verify>
  <acceptance_criteria>
    - `node -c tools/n8n/railway_read_sentinels.cjs` exits 0 (valid JS, parses).
    - Source contains the literal project ID `c905a353-0927-4eeb-85f1-11c11d392a08`.
    - Source contains `backboard.railway.app/graphql/v2` and reads `RAILWAY_TOKEN` from `tech-pwa/.env.local` (grep: `RAILWAY_TOKEN` and `.env.local` both present).
    - Source prints variable KEY NAMES + a `DATABASE_URL` presence boolean but does NOT print variable values (no line that logs the full variables map value).
    - Final printed line instructs Brandon to STOP and paste output back to Claude Code.
  </acceptance_criteria>
  <done>Script exists, syntax-valid, mirrors railway_upsert_vars.cjs credential handling, enumerates services + reads per-sentinel env-var names + fetches deploy logs, never logs secret values.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Brandon runs discovery script + Claude Code writes SENTINEL_INVENTORY.md</name>
  <read_first>
    - .planning/phases/28-sentinel-diet/28-RESEARCH.md (§"Wave 0 Discovery Task" — the 6-question table that the inventory must answer per service)
  </read_first>
  <what-built>
    `tools/n8n/railway_read_sentinels.cjs` — a read-only Railway discovery script that lists all services in project c905a353…, identifies the four sentinels, prints each sentinel's env-var key names + DATABASE_URL presence, and dumps recent deploy logs (where the SQL/check logic surfaces).
  </what-built>
  <how-to-verify>
    1. Brandon runs: `node tools/n8n/railway_read_sentinels.cjs`
    2. Brandon pastes the ENTIRE script output back into the chat.
    3. Claude Code reads the output and writes `.planning/phases/28-sentinel-diet/SENTINEL_INVENTORY.md` with one row per sentinel service answering: service name + Railway service id | DATABASE_URL present? (yes/no) | Neon tables read | SQL / check logic recovered (from logs) | polling cadence | read-only or writes to Neon | verdict (PORT to n8n / RETIRE without porting).
    4. Spec-architect decision (RESEARCH.md Pitfall 4): if its output shows NO DATABASE_URL, mark it RETIRE-without-porting and note "SENT-01 compliant by deletion alone — not a Neon poller". If it DOES have DATABASE_URL, mark PORT and capture its SQL.
    5. If deploy logs did not surface SQL for a Neon-touching sentinel, record what IS known (tables, cadence) and add an explicit `[GAP — SQL not recovered from logs; recover during Wave 1 build via additional log pull or container inspection]` note for that row rather than inventing SQL.
    6. Set the inventory header: `Source: railway_read_sentinels.cjs output, run <date> by Brandon`.
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/28-sentinel-diet/SENTINEL_INVENTORY.md` exists.
    - Inventory has exactly one row per Railway service named `sentinel-*` discovered in the output (minimum the four named in CONTEXT.md, or fewer if some were already deleted — note any discrepancy).
    - Every row has an explicit `DATABASE_URL present? yes/no` cell — no blanks.
    - Every row has a verdict cell: `PORT` or `RETIRE-without-porting`.
    - spec-architect row carries the Neon-poller determination (DATABASE_URL presence drives PORT vs RETIRE).
    - Any Neon-touching sentinel whose SQL was not recovered carries an explicit `[GAP …]` marker, not fabricated SQL.
  </acceptance_criteria>
  <resume-signal>Type "approved" once SENTINEL_INVENTORY.md is written and reviewed, or describe corrections.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Terminal gate — diff artifact + STOP for Claude Code review</name>
  <read_first>
    - .planning/phases/28-sentinel-diet/SENTINEL_INVENTORY.md (the artifact being gated)
    - tools/n8n/railway_read_sentinels.cjs (the script being gated)
  </read_first>
  <action>
    Verify the working branch is `feat/phase-28-sentinel-diet` (branch gate per CLAUDE.md AG SPEC REQUIREMENTS Task 1 pattern): run `git branch --show-current` — if it is `main`, STOP and create/switch to `feat/phase-28-sentinel-diet` before committing.

    Stage `tools/n8n/railway_read_sentinels.cjs` and `.planning/phases/28-sentinel-diet/SENTINEL_INVENTORY.md`. Write the diff to the artifact: `git diff --staged > artifacts/phase28_01_diff.txt` (create `artifacts/` if absent). Commit with message `feat(phase-28): Wave 0 sentinel discovery — script + SENTINEL_INVENTORY.md`. Push the branch.

    STOP. Post to Claude Code: "Wave 0 complete — SENTINEL_INVENTORY.md filled from Railway discovery. Diff in artifacts/phase28_01_diff.txt. Awaiting PASS/BLOCK before Wave 1 (28-02) build." Do NOT proceed to Wave 1.
  </action>
  <verify>
    <automated>git rev-parse --abbrev-ref HEAD | findstr /v "^main$" && test -f artifacts/phase28_01_diff.txt && echo GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - Current git branch is NOT `main` (is `feat/phase-28-sentinel-diet` or the phase branch named in the spec).
    - `artifacts/phase28_01_diff.txt` exists and is non-empty.
    - A commit exists touching both `railway_read_sentinels.cjs` and `SENTINEL_INVENTORY.md`.
    - Execution STOPPED after posting the gate message — Wave 1 not started.
  </acceptance_criteria>
  <done>Diff artifact written, branch verified non-main, commit pushed, STOP posted to Claude Code. Wave 1 blocked on PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local script → Railway GraphQL API | RAILWAY_TOKEN crosses here; read from .env.local in-process |
| script stdout → chat (Brandon paste) | discovery output is pasted into chat — must contain no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Information Disclosure | railway_read_sentinels.cjs output | mitigate | Print env-var KEY NAMES + DATABASE_URL boolean only — never variable VALUES; RAILWAY_TOKEN read from .env.local, never echoed/argv (RESEARCH.md Security Domain — same pattern as railway_upsert_vars.cjs) |
| T-28-02 | Information Disclosure | RAILWAY_TOKEN in transit | accept | Token only travels to backboard.railway.app over HTTPS; Brandon-run, local-only; no token in logs |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — existing Node + repo scripts only (RESEARCH.md Package Legitimacy Audit: no new packages) |
</threat_model>

<verification>
- Discovery script is syntax-valid and never logs secret values.
- SENTINEL_INVENTORY.md answers all 6 Wave 0 questions per service (RESEARCH.md Wave 0 Discovery Task table).
- spec-architect's Neon-poller status is resolved by DATABASE_URL presence (Pitfall 4 closed).
- Diff artifact written; branch is non-main; Wave 1 gated behind Claude Code PASS.
</verification>

<success_criteria>
- `tools/n8n/railway_read_sentinels.cjs` exists, syntax-valid, Brandon-runnable, secret-safe.
- `SENTINEL_INVENTORY.md` populated with per-service SQL/cadence/Neon-tables/DATABASE_URL/verdict — no blanks, no fabricated SQL.
- SENT-01/SENT-02 scope is now knowable: each sentinel marked PORT or RETIRE-without-porting.
- Diff artifact committed; STOP posted; Wave 1 not started.
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Consumed by |
|----------|------|-------------|
| Railway discovery script | `tools/n8n/railway_read_sentinels.cjs` | Brandon (run), Claude Code (transcribe output) |
| Sentinel inventory | `.planning/phases/28-sentinel-diet/SENTINEL_INVENTORY.md` | Wave 1 (28-02) — SQL source; Wave 2 (28-03) — service IDs for deletion + SENT-03 evidence |
| Diff gate artifact | `artifacts/phase28_01_diff.txt` | Claude Code diff review (PASS/BLOCK) |

<output>
Create `.planning/phases/28-sentinel-diet/28-01-SUMMARY.md` when done.
</output>
