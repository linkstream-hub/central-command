---
phase: 27-dashboard-remainder
plan: "05"
type: tdd
wave: 2
depends_on: ["27-01"]
files_modified:
  - tech-pwa/src/app/api/feedback/route.ts
  - tech-pwa/src/app/api/feedback/[feedbackId]/route.ts
  - tech-pwa/src/lib/feedback-mapper.ts
  - tech-pwa/src/lib/__tests__/feedback.test.ts
  - artifacts/phase27_05_diff.txt
autonomous: true
requirements: [DASH-04]
tags: [feedback, neon, drizzle, schema-mapping, auth, tdd, vitest]

must_haves:
  truths:
    - "POST /api/feedback inserts a dispatcher_feedback row populating the migrated columns (category, subject, details, status, relatedJobId, submittedBy)"
    - "GET /api/feedback returns FeedbackItem[] with rowIndex = Neon serial id (so the frontend sends id back, not a Sheets row number)"
    - "PATCH /api/feedback/[feedbackId] updates status/adminNotes/response by Neon id"
    - "All feedback routes are staff-auth + org-scoped; user-supplied feedback fields are validated before insert"
  artifacts:
    - path: "tech-pwa/src/app/api/feedback/route.ts"
      provides: "submitFeedback (POST) + getFeedback (GET)"
      exports: ["GET", "POST"]
    - path: "tech-pwa/src/app/api/feedback/[feedbackId]/route.ts"
      provides: "updateFeedbackStatus (PATCH)"
      exports: ["PATCH"]
    - path: "tech-pwa/src/lib/feedback-mapper.ts"
      provides: "pure row↔FeedbackItem mapping (rowIndex=id)"
  key_links:
    - from: "feedback/route.ts"
      to: "dispatcher_feedback table"
      via: "Drizzle insert + select on migrated columns"
      pattern: "dispatcher_feedback|dispatcherFeedback"
---

<objective>
Port submitFeedback / getFeedback / updateFeedbackStatus (DASH-04 feedback half) to Neon-backed Next.js routes against the migrated `dispatcher_feedback` table.

Purpose: Research Finding 5 — the GAS Sheets "Dispatcher Feedback" schema and the frontend FeedbackItem type both diverge from the old Neon table. Plan 01 migrated the table to match FeedbackItem. This plan builds the CRUD routes. Pitfall 6 — `rowIndex` in the frontend must map to the Neon serial `id`, not a Sheets row number, so getFeedback returns `rowIndex: row.id`.

**Gated on Plan 01:** the route reads/writes columns (category, subject, details, status, adminNotes, relatedJobId) that only exist after Plan 01's migration is applied to Neon by Brandon. Do not start until 27-01-SUMMARY confirms "migrated".

Output: feedback collection route (GET+POST) + item route (PATCH) + a pure mapper with unit tests.
</objective>

<execution_context>
@C:/PTOW/1_APT_Central_Command/.claude/get-shit-done/workflows/execute-plan.md
@C:/PTOW/1_APT_Central_Command/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-dashboard-remainder/27-RESEARCH.md
@.planning/phases/27-dashboard-remainder/27-01-SUMMARY.md

<interfaces>
Migrated dispatcher_feedback table (after Plan 01 — confirm column names in 27-01-SUMMARY + schema.ts):
```typescript
id: serial (PK), orgId: text, jobId: text (nullable), employeeId: integer,
category: text, subject: text, details: text, status: text (default 'Needs Review'),
adminNotes: text, relatedJobId: text, managerName: text, response: text,
content: text (legacy — dual-written), feedbackType: text (legacy — dual-written), submittedBy: text, createdAt: timestamp
```

Frontend FeedbackItem (dashboard-api.ts lines 54-66) — the API contract:
```typescript
{ rowIndex: number;  // = Neon serial id
  timestamp: string;  // = createdAt ISO
  category: 'Suggestion'|'Bug Report'|'Workflow Note'|'Question';
  subject: string; details: string; relatedJobId?: string;
  status: 'Needs Review'|'Reviewed'|'In Progress'|'Done';
  response?: string; managerName?: string; adminNotes?: string; submittedBy: string; }
```

Frontend call sites (tech-pwa/src/app/feedback/page.tsx):
- submitFeedback → dashboardRequest('submitFeedback', { category, subject, details, relatedJobId?, submittedBy })
- getFeedback → reads res.items (FeedbackItem[])
- updateFeedbackStatus → dashboardRequest('updateFeedbackStatus', { rowIndex, status, adminNotes?, response?, managerName? })  (rowIndex = Neon id)

Dual-write decision (from Plan 01 SUMMARY): also write `content`=details and `feedbackType`=category so legacy readers stay valid. Confirm the exact decision in 27-01-SUMMARY before implementing.

Auth: dual-auth (session OR x-api-key) + force-dynamic.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — pure feedback-mapper (row↔FeedbackItem) RED→GREEN</name>
  <files>tech-pwa/src/lib/feedback-mapper.ts, tech-pwa/src/lib/__tests__/feedback.test.ts</files>
  <behavior>
    Pure, DB-free helpers:
    - `rowToFeedbackItem(row)` → FeedbackItem with `rowIndex: row.id`, `timestamp: row.createdAt.toISOString()`, category/subject/details/status/relatedJobId/response/managerName/adminNotes/submittedBy mapped from the migrated columns. Test: id→rowIndex; createdAt→ISO timestamp; nullable relatedJobId omitted/undefined when null; status defaults surfaced.
    - `feedbackInsertFromBody(body, orgId)` → the Drizzle insert object with dual-write: `{ orgId, category, subject, details, status: body.status ?? 'Needs Review', relatedJobId, submittedBy, content: body.details, feedbackType: body.category }`. Test: details mirrored to content; category mirrored to feedbackType; default status applied when absent.
    - `validateFeedbackBody(body)` → returns { ok, error? }: rejects empty subject or empty details or invalid category (V5 input validation). Test: missing subject→error; invalid category→error; valid→ok.
    Write the test file FIRST (RED — module not found), commit `test(27-05): ...`; implement feedback-mapper.ts (GREEN), commit `feat(27-05): ...`. (coverage.include for this file is registered by Plan 02 — do not edit vitest.config.ts.)
  </behavior>
  <action>Three named exports, no DB imports. Use a row type inferred from the schema where convenient, or a minimal local interface.</action>
  <read_first>
    - .planning/phases/27-dashboard-remainder/27-01-SUMMARY.md (migrated columns + dual-write decision)
    - .planning/phases/27-dashboard-remainder/27-RESEARCH.md (section 9, Finding 5, Pitfall 6)
    - tech-pwa/src/lib/dashboard-api.ts (FeedbackItem, lines 54-66)
    - tech-pwa/src/lib/__tests__/access-codes.test.ts (test style)
  </read_first>
  <verify>
    <automated>cd tech-pwa && npm run test:unit -- -t "feedback"</automated>
  </verify>
  <acceptance_criteria>RED shows missing-module; GREEN shows mapper tests passing; rowIndex=id verified; dual-write (content/feedbackType) verified; validation rejects empty subject/details + bad category.</acceptance_criteria>
  <done>feedback-mapper.ts exports three helpers; tests green; zero regressions.</done>
</task>

<task type="auto">
  <name>Task 2: feedback routes (collection GET+POST, item PATCH)</name>
  <files>tech-pwa/src/app/api/feedback/route.ts, tech-pwa/src/app/api/feedback/[feedbackId]/route.ts</files>
  <action>
    Dual-auth guard + force-dynamic on both files; org_id on every query.
    - feedback/route.ts GET: select dispatcher_feedback where `eq(orgId, ...)` ordered by createdAt desc; map each via `rowToFeedbackItem`; return `{ success:true, items }`.
    - feedback/route.ts POST: parse body; `validateFeedbackBody` → 400 on failure; insert via `feedbackInsertFromBody(body, orgId)` using `.returning({ id: dispatcherFeedback.id })`; return `{ success:true, rowIndex: inserted.id }`.
    - [feedbackId]/route.ts PATCH: read feedbackId from path param (the Neon id), parse `{ status?, adminNotes?, response?, managerName? }`, build a partial update of only provided fields, update where `eq(id, Number(feedbackId)) AND eq(orgId, ...)`. 404 if no row matched. Return `{ success:true }`. Reject non-numeric feedbackId with 400.
    Note: the existing `jobId NOT NULL` column is now nullable (Plan 01) — do NOT set jobId on insert unless body provides one; relatedJobId carries the optional job reference.
  </action>
  <read_first>
    - tech-pwa/src/app/api/jobs/[jobId]/route.ts (auth + path param + Drizzle update/insert style — read in FULL)
    - tech-pwa/src/lib/schema.ts (migrated dispatcherFeedback)
  </read_first>
  <verify>
    <automated>cd tech-pwa && npx tsc --noEmit && npm run test:unit -- -t "feedback"</automated>
  </verify>
  <acceptance_criteria>Both files compile; GET returns rowIndex=id items; POST validates + inserts + returns new id; PATCH updates by numeric id, 404 on no-match, 400 on non-numeric id; org-scoped throughout; jobId not forced on insert.</acceptance_criteria>
  <done>Feedback CRUD routes complete against the migrated table.</done>
</task>

<task type="auto">
  <name>Task 3: Terminal gate — tsc + eslint + diff artifact + STOP</name>
  <files>artifacts/phase27_05_diff.txt</files>
  <action>`npx tsc --noEmit` + `npm run lint` + full `npm run test:unit` (zero regressions). Push, `git diff main...HEAD > artifacts/phase27_05_diff.txt`, commit, post to Claude Code. STOP.</action>
  <verify>
    <automated>cd tech-pwa && npx tsc --noEmit && npm run test:unit && test -f ../artifacts/phase27_05_diff.txt && echo "gate ready"</automated>
  </verify>
  <done>tsc + lint clean, full suite green, diff artifact committed, posted, stopped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard browser → /api/feedback | User-typed feedback fields (subject/details) enter Neon |
| Frontend rowIndex → PATCH /api/feedback/[id] | Client supplies the record identifier for status updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-11 | Tampering | feedback insert with malformed/oversized input | mitigate | validateFeedbackBody rejects empty subject/details + invalid category; Drizzle parameterizes the insert (no SQL injection) |
| T-27-12 | Tampering | updateFeedbackStatus targeting wrong/foreign record | mitigate | PATCH scoped by Neon serial id (not Sheets row number — Pitfall 6) AND org_id WHERE clause; non-numeric id rejected 400 |
| T-27-13 | Information Disclosure | cross-tenant feedback read | mitigate | `eq(orgId, session.user.orgId ?? 'APT-CA')` on GET; dual-auth 401 guard |
</threat_model>

<verification>
- POST inserts migrated columns + dual-writes content/feedbackType; returns new id
- GET returns FeedbackItem[] with rowIndex=id
- PATCH updates by numeric id, org-scoped, 404/400 guards
- Input validation on user fields
- Full unit suite green; gated correctly on Plan 01 migration
</verification>

<success_criteria>
DASH-04 feedback half served by Neon CRUD routes against the migrated table, with rowIndex=id semantics and validated input. Combined with Plan 04, DASH-04 is fully ported. Wiring into dashboard-api.ts happens in Plan 06.
</success_criteria>

<artifacts_this_phase_produces>
- /api/feedback (GET+POST) + /api/feedback/[feedbackId] (PATCH) routes
- tech-pwa/src/lib/feedback-mapper.ts pure helpers + unit tests
- artifacts/phase27_05_diff.txt
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/27-dashboard-remainder/27-05-SUMMARY.md`. Document the exact submit/get/update request+response contracts (esp. rowIndex=id) for Plan 06 wiring.
</output>
