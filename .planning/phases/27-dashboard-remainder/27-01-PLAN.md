---
phase: 27-dashboard-remainder
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tech-pwa/src/lib/schema.ts
  - tech-pwa/drizzle/
  - artifacts/phase27_01_diff.txt
autonomous: false
requirements: [DASH-04]
tags: [drizzle, neon, schema-migration, dispatcher-feedback]

must_haves:
  truths:
    - "dispatcher_feedback table can store a feedback row matching the frontend FeedbackItem contract (category, subject, details, status, adminNotes, relatedJobId, submittedBy)"
    - "jobId column is nullable so non-job-specific feedback can be inserted"
    - "A drizzle-kit-generated migration file is committed and applies cleanly via npm run db:migrate"
    - "Migration is additive — no column drops, no NOT NULL tightening on existing populated columns without a verified zero/safe row count"
  artifacts:
    - path: "tech-pwa/src/lib/schema.ts"
      provides: "dispatcherFeedback table with frontend-aligned columns"
      contains: "dispatcherFeedback"
    - path: "tech-pwa/drizzle/"
      provides: "generated SQL migration adding feedback columns + relaxing jobId"
  key_links:
    - from: "tech-pwa/src/lib/schema.ts"
      to: "tech-pwa/drizzle/ migration"
      via: "drizzle-kit generate"
      pattern: "dispatcher_feedback"
---

<objective>
Schema-migrate the Neon `dispatcher_feedback` table so it matches the frontend `FeedbackItem` contract, unblocking the feedback route ports in Plan 05.

Purpose: The existing table (`jobId NOT NULL`, `content NOT NULL`, no category/subject/details/status/adminNotes) cannot serve the frontend's FeedbackItem type. Research Finding 5 marks this a blocking prerequisite — submitFeedback/getFeedback/updateFeedbackStatus cannot be ported until the schema is correct.

Output: Updated `dispatcherFeedback` table definition in schema.ts + a committed drizzle migration file + applied migration on the dev Neon branch. ADDITIVE ONLY.
</objective>

<execution_context>
@C:/PTOW/1_APT_Central_Command/.claude/get-shit-done/workflows/execute-plan.md
@C:/PTOW/1_APT_Central_Command/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-dashboard-remainder/27-RESEARCH.md
@.planning/phases/27-dashboard-remainder/27-CONTEXT.md

<interfaces>
Frontend FeedbackItem contract (tech-pwa/src/lib/dashboard-api.ts lines 54-66) — the schema target:
```typescript
export interface FeedbackItem {
  rowIndex: number;        // maps to Neon serial id
  timestamp: string;       // maps to createdAt
  category: 'Suggestion' | 'Bug Report' | 'Workflow Note' | 'Question';
  subject: string;
  details: string;
  relatedJobId?: string;   // nullable
  status: 'Needs Review' | 'Reviewed' | 'In Progress' | 'Done';
  response?: string;
  managerName?: string;
  adminNotes?: string;
  submittedBy: string;
}
```

Current Neon table (tech-pwa/src/lib/schema.ts lines 421-430):
```typescript
export const dispatcherFeedback = pgTable('dispatcher_feedback', {
  id: serial('id').primaryKey(),
  orgId: text('org_id').notNull().default('APT-CA'),
  jobId: text('job_id').notNull(),               // must become nullable → relatedJobId
  employeeId: integer('employee_id'),
  feedbackType: text('feedback_type'),           // maps to category
  content: text('content').notNull(),            // maps to details
  submittedBy: text('submitted_by'),
  createdAt: timestamp('created_at').defaultNow(),
});
```

Migration mechanism (verified from package.json):
- `npm run db:generate` → `drizzle-kit generate` (produces SQL + journal)
- `npm run db:migrate` → `tsx scripts/migrate.ts` (applies — same mechanism used since Foundation F6)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: BLOCKING — Brandon row-count check on dispatcher_feedback before any NOT NULL relaxation</name>
  <what-built>Nothing yet — this gate runs BEFORE the schema edit. Research Assumption A1 says dispatcher_feedback likely has no production rows, but this is unverified. Relaxing `jobId NOT NULL` and adding NOT NULL-defaulted columns is only safe if existing rows are compatible. Claude cannot read .env / cannot connect to Neon directly.</what-built>
  <how-to-verify>
    Brandon runs the row-count against the dev Neon branch:
    1. `SELECT COUNT(*) FROM dispatcher_feedback;`
    2. If COUNT = 0 → migration is unconditionally safe (no existing rows to violate constraints). Reply "0 rows — proceed."
    3. If COUNT > 0 → also run `SELECT COUNT(*) FROM dispatcher_feedback WHERE job_id IS NULL;` and report both numbers. Relaxing jobId to nullable is always safe (loosening). New columns will be added nullable (no default-NOT-NULL), so existing rows remain valid. Reply with both counts + "proceed."
  </how-to-verify>
  <read_first>
    - tech-pwa/src/lib/schema.ts (lines 421-430 — current dispatcherFeedback)
    - .planning/phases/27-dashboard-remainder/27-RESEARCH.md (Finding 5 + Assumptions A1)
  </read_first>
  <acceptance_criteria>Brandon has reported the row count and explicitly cleared "proceed". The executor records the count in the SUMMARY. No schema edit happens before this reply.</acceptance_criteria>
  <resume-signal>Brandon types the row count(s) + "proceed"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Extend dispatcherFeedback schema + generate migration (ADDITIVE ONLY)</name>
  <files>tech-pwa/src/lib/schema.ts, tech-pwa/drizzle/</files>
  <action>
    Edit the `dispatcherFeedback` table in schema.ts to match the FeedbackItem contract. ADDITIVE ONLY — do not drop `feedbackType` or `content` (keep them for backward compat; the route will read/write the new columns). Changes:
    - Relax `jobId`: `text('job_id')` (drop `.notNull()`) — loosening is always safe.
    - Add nullable columns (NO `.notNull()` on any new column): `category text('category')`, `subject text('subject')`, `details text('details')`, `status text('status').default('Needs Review')`, `adminNotes text('admin_notes')`, `relatedJobId text('related_job_id')`, `managerName text('manager_name')`, `response text('response')`.
    - Keep `content` and `feedbackType` as-is (the Plan 05 route writes BOTH `details` and `content`, BOTH `category` and `feedbackType`, so legacy readers and the new contract both work). Note this dual-write decision in the SUMMARY.
    Then run `npm run db:generate` from tech-pwa/ to produce the migration SQL + journal entry. Inspect the generated SQL: it MUST be only `ADD COLUMN` statements + the `ALTER COLUMN job_id DROP NOT NULL`. If `drizzle-kit` emits any `DROP COLUMN` or `SET NOT NULL`, STOP — that violates the additive-only constraint; report it instead of committing.
  </action>
  <verify>
    <automated>cd tech-pwa && npm run db:generate && node -e "const fs=require('fs');const f=fs.readdirSync('drizzle').filter(n=>n.endsWith('.sql')).sort().pop();const sql=fs.readFileSync('drizzle/'+f,'utf8');if(/DROP COLUMN/i.test(sql)||/SET NOT NULL/i.test(sql)){console.error('NON-ADDITIVE migration detected');process.exit(1)}console.log('additive OK:',f)"</automated>
  </verify>
  <done>schema.ts dispatcherFeedback has all FeedbackItem-aligned nullable columns, jobId is nullable, and the newest generated drizzle SQL contains only ADD COLUMN + DROP NOT NULL (no drops, no SET NOT NULL). `npx tsc --noEmit` passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: [BLOCKING] Apply migration to Neon (npm run db:migrate) — Brandon runs (Claude blocked from .env)</name>
  <what-built>The generated additive migration from Task 2. It must be applied to the dev Neon branch before Plan 05 feedback routes can read/write the new columns. Claude cannot run db:migrate — it needs DATABASE_URL from .env which Claude is denied.</what-built>
  <how-to-verify>
    Brandon, from tech-pwa/:
    1. `npm run db:migrate` — applies the new migration via scripts/migrate.ts.
    2. Confirm no errors; the new columns now exist.
    3. Optional spot check: `\d dispatcher_feedback` (or `SELECT column_name FROM information_schema.columns WHERE table_name='dispatcher_feedback';`) shows category, subject, details, status, admin_notes, related_job_id and job_id nullable.
    4. Reply "migrated" (or paste the error if it fails).
  </how-to-verify>
  <read_first>
    - tech-pwa/scripts/migrate.ts (the apply mechanism)
    - the newest tech-pwa/drizzle/*.sql from Task 2
  </read_first>
  <acceptance_criteria>Brandon confirms the migration applied to the dev Neon branch with the new columns present. Plan 05 is unblocked only after this reply.</acceptance_criteria>
  <resume-signal>Brandon types "migrated"</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Terminal gate — tsc + eslint + diff artifact + STOP</name>
  <files>artifacts/phase27_01_diff.txt</files>
  <action>Run `npx tsc --noEmit` (zero errors) and `npm run lint` (no new errors) from tech-pwa/. Push the branch, then `git diff main...HEAD > artifacts/phase27_01_diff.txt` from repo root, commit the diff artifact. Post to Claude Code for diff review. STOP — do not merge.</action>
  <verify>
    <automated>cd tech-pwa && npx tsc --noEmit && test -f ../artifacts/phase27_01_diff.txt && echo "gate ready"</automated>
  </verify>
  <done>tsc clean, lint clean, artifacts/phase27_01_diff.txt committed, posted to Claude Code, execution stopped pending PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drizzle migration → Neon production-adjacent dev branch | Schema DDL crosses into the live data store; a destructive migration could drop data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | dispatcher_feedback DDL migration | mitigate | Additive-only constraint enforced by automated grep gate (Task 2 verify rejects DROP COLUMN / SET NOT NULL); Brandon row-count gate (Task 1) before any constraint change |
| T-27-02 | Denial of Service | migration apply on populated table | accept | Row-count gate confirms safety; jobId relaxation is loosening (cannot fail existing rows); new columns nullable |
</threat_model>

<verification>
- schema.ts dispatcherFeedback exposes category/subject/details/status/adminNotes/relatedJobId/managerName/response columns, jobId nullable
- Newest drizzle/*.sql is additive only (automated grep gate)
- Migration applied to Neon by Brandon (Task 3 checkpoint)
- tsc + lint clean
</verification>

<success_criteria>
The Neon dispatcher_feedback table can store a full FeedbackItem after Brandon applies the migration, with no destructive DDL. Plan 05 feedback routes are unblocked.
</success_criteria>

<artifacts_this_phase_produces>
- Migrated `dispatcher_feedback` Neon table (frontend-aligned columns, jobId nullable)
- Committed additive drizzle migration SQL + journal entry
- artifacts/phase27_01_diff.txt
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/27-dashboard-remainder/27-01-SUMMARY.md` when done. Record the Brandon row-count, the dual-write decision (content+details, feedbackType+category), and the migration filename.
</output>
