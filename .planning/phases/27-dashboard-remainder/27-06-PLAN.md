---
phase: 27-dashboard-remainder
plan: "06"
type: execute
wave: 3
depends_on: ["27-02", "27-03", "27-04", "27-05"]
files_modified:
  - tech-pwa/src/lib/dashboard-api.ts
  - artifacts/phase27_06_diff.txt
  - artifacts/phase27_playwright.txt
autonomous: true
requirements: [DASH-05]
tags: [dashboard-api, wiring, zero-fallthrough, playwright, gas-exit]

must_haves:
  truths:
    - "Every in-scope action (getTimecardApprovalQueue, approveTimecard, disputeTimecard, getTechAvailability, expandScope, generateTenantScheduleLink, submitFeedback, getFeedback, updateFeedbackStatus) sets `data` from a Neon route BEFORE the /api/gas fallthrough block. getCalendarData gets an explicit not-implemented response (calendar deferred to post-sr-01 — dispatch truth is the assignment sheet/Buildertrend today). markPTEGranted dropped (zero callers)."
    - "On a Neon route failure, each ported action returns an explicit error object — never falls through to /api/gas"
    - "markPTEGranted, expandScope, generateTenantScheduleLink, submitFeedback are removed from DEV_BLOCKED_WRITES"
    - "No non-Gmail action reaches the /api/gas POST block (DASH-05) — verified by a static audit"
    - "Playwright suite passes with 0 failures"
  artifacts:
    - path: "tech-pwa/src/lib/dashboard-api.ts"
      provides: "all 11 in-scope actions wired to Neon routes as special cases"
      contains: "getTimecardApprovalQueue"
  key_links:
    - from: "dashboard-api.ts special cases"
      to: "Plan 02/03/04/05 routes"
      via: "fetch to /api/timecards, /api/schedule/availability, /api/calendar, /api/jobs/[id], /api/feedback"
      pattern: "/api/timecards|/api/calendar|/api/feedback|schedule-link"
---

<objective>
Wire all 11 in-scope actions into `dashboard-api.ts` as special-case blocks pointing at the Plan 02–05 Neon routes, remove ported writes from DEV_BLOCKED_WRITES, and prove DASH-05: zero non-Gmail actions reach the `/api/gas` fallthrough.

Purpose: This is the closing wave. Once wired, the GAS DashboardAPI receives only Gmail-domain actions (Phase 23) — all other actions die as caller-less code (Phase 24 deletes). Each ported action must set `data` before the `/api/gas` POST block (lines ~645-657 today) and return explicitly on failure.

Output: Updated dashboard-api.ts + a static fallthrough audit + a green Playwright run.
</objective>

<execution_context>
@C:/PTOW/1_APT_Central_Command/.claude/get-shit-done/workflows/execute-plan.md
@C:/PTOW/1_APT_Central_Command/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-dashboard-remainder/27-RESEARCH.md
@.planning/phases/27-dashboard-remainder/27-02-SUMMARY.md
@.planning/phases/27-dashboard-remainder/27-03-SUMMARY.md
@.planning/phases/27-dashboard-remainder/27-04-SUMMARY.md
@.planning/phases/27-dashboard-remainder/27-05-SUMMARY.md

<interfaces>
dashboard-api.ts current structure (tech-pwa/src/lib/dashboard-api.ts — read in FULL before editing):
- DEV_BLOCKED_WRITES set (lines ~470-478) — contains expandScope, generateTenantScheduleLink, submitFeedback, markPTEGranted (remove the four as they become Neon-only, per Pitfall 5 / archiveJob precedent)
- MIGRATED_ACTIONS map (lines ~525-532) — simple GET-only actions
- Special-case blocks (lines ~534-643) — the pattern to copy (set `data = await response.json()`)
- /api/gas fallthrough POST (lines ~645-657) — guarded by `if (!data)`. DASH-05 = no in-scope action reaches here.

Route contracts come from the Plan 02–05 SUMMARYs. Expected wiring (confirm exact paths/bodies against SUMMARYs):
- getTimecardApprovalQueue → GET /api/timecards/queue (weekStart query optional)
- approveTimecard → POST /api/timecards/{recordId}/approve { supervisorName, supervisorId }
- disputeTimecard → POST /api/timecards/{recordId}/dispute { supervisorName, supervisorId, reason }
- getTechAvailability → GET /api/schedule/availability?weekStart=...
- getCalendarData → NOT IMPLEMENTED special case: `data = { success:false, message:'Calendar arrives with the schedule redesign' }` — never reaches /api/gas (scope amendment 2026-06-11)
- expandScope → BINDING (Plan 04 contract): GET /api/jobs/{jobId} first to read current estHours+notes; compute newEstHours = current + hoursToAdd and appended notes; PATCH the FINAL computed values { estHours: newEstHours, notes: newNotes, assignedTech?: reassignTech }. NEVER PATCH hoursToAdd directly — the route stores estHours verbatim and the delta would overwrite the total.
- generateTenantScheduleLink → POST /api/jobs/{jobId}/schedule-link → returns { url }
- submitFeedback → POST /api/feedback
- getFeedback → GET /api/feedback (returns items)
- updateFeedbackStatus → PATCH /api/feedback/{rowIndex}

Explicit-failure pattern (from createManualJob, lines ~604-616): on fetch throw, `return { success:false, message:'...' } as unknown as T` — never fall through.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire all 11 actions as special cases + clean DEV_BLOCKED_WRITES</name>
  <files>tech-pwa/src/lib/dashboard-api.ts</files>
  <action>
    Read dashboard-api.ts in FULL first. Add special-case blocks (mirroring the existing getWeekSchedule / createManualJob / archiveJob style) for the 9 in-scope actions (+ the getCalendarData not-implemented stub), each setting `data = await response.json()` from the correct Neon route per the SUMMARY contracts above. For the mutating writes (approveTimecard, disputeTimecard, generateTenantScheduleLink, submitFeedback, updateFeedbackStatus, expandScope), wrap the fetch in try/catch and `return { success:false, message:'<action> failed' } as unknown as T` on throw — NEVER let them reach the /api/gas block.
    Place GET-only reads that need no body transform (none here need MIGRATED_ACTIONS — all have query params or path params) as special cases with query-string construction (copy the getWeekSchedule encodeURIComponent pattern).
    Remove `expandScope`, `generateTenantScheduleLink`, `submitFeedback`, `markPTEGranted` from DEV_BLOCKED_WRITES (Pitfall 5) — add the archiveJob-style comment ("removed — now writes to dev Neon branch, not GAS Sheets"). Leave `replyToThread` (Gmail-domain, Phase 23).
    For expandScope use the existing PATCH /api/jobs/{jobId} per the Plan 04 SUMMARY contract. Match the frontend field names sent by JobDetailModal (jobId/leadId) — confirm which id the PATCH path uses against Plan 04 SUMMARY.
    Do NOT touch the Gmail-domain action handling or the /api/gas block itself — only ensure in-scope actions set `data` before it.
  </action>
  <read_first>
    - tech-pwa/src/lib/dashboard-api.ts (FULL — DEV_BLOCKED_WRITES, MIGRATED_ACTIONS, all special cases, /api/gas block)
    - .planning/phases/27-dashboard-remainder/27-02-SUMMARY.md, 27-03-SUMMARY.md, 27-04-SUMMARY.md, 27-05-SUMMARY.md (exact route contracts)
  </read_first>
  <verify>
    <automated>cd tech-pwa && npx tsc --noEmit && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>All 11 actions have special-case blocks setting data from Neon routes; four writes removed from DEV_BLOCKED_WRITES with explanatory comments; mutating actions return explicit error on failure; tsc clean; full unit suite green (zero regressions).</acceptance_criteria>
  <done>dashboard-api.ts routes every in-scope action to Neon; no Gmail-domain action touched.</done>
</task>

<task type="auto">
  <name>Task 2: DASH-05 static fallthrough audit</name>
  <files>tech-pwa/src/lib/dashboard-api.ts</files>
  <action>
    Prove no in-scope action can reach the /api/gas POST block. Build a static audit: for each of the 11 in-scope action strings, confirm dashboard-api.ts contains a special case (or MIGRATED_ACTIONS entry) that assigns `data` for that action. Use git grep (NOT the Grep tool — hook-guarded) or a node script reading the file. The verify command below asserts all 11 action strings appear in a special-case/`data =` assignment context. Record the audit result (the 11 actions + the line where each sets data, plus confirmation the only remaining /api/gas callers are Gmail-domain actions: getGmailThread, getDraftReply, replyToThread) in the SUMMARY.
  </action>
  <read_first>
    - tech-pwa/src/lib/dashboard-api.ts (the wired version from Task 1)
  </read_first>
  <verify>
    <automated>cd tech-pwa && node -e "const fs=require('fs');const s=fs.readFileSync('src/lib/dashboard-api.ts','utf8');const acts=['getTimecardApprovalQueue','approveTimecard','disputeTimecard','getTechAvailability','expandScope','generateTenantScheduleLink','submitFeedback','getFeedback','updateFeedbackStatus'];if(!s.includes(\"action === 'getCalendarData'\")){console.error('getCalendarData not-implemented stub missing');process.exit(1)}const dbw=(s.match(/DEV_BLOCKED_WRITES = new Set\(\[([\s\S]*?)\]\)/)||[,''])[1];const stillBlocked=['markPTEGranted','expandScope','generateTenantScheduleLink','submitFeedback'].filter(a=>dbw.includes(a));if(stillBlocked.length){console.error('STILL IN DEV_BLOCKED_WRITES:',stillBlocked.join(','));process.exit(1)}const unhandled=acts.filter(a=>{const hasIf=s.includes(\"action === '\"+a+\"'\");const hasMap=new RegExp(a+':\\s*[\'\\\"]/api').test(s);return !hasIf && !hasMap});if(unhandled.length){console.error('NO HANDLING BRANCH (would fall through to /api/gas):',unhandled.join(','));process.exit(1)}console.log('all 11 actions have handling branches; 4 writes cleared from DEV_BLOCKED_WRITES')"</automated>
  </verify>
  <acceptance_criteria>Audit confirms all 11 in-scope action strings are handled by a Neon special case; the SUMMARY lists each action → its data-assignment line; only Gmail-domain actions remain on the /api/gas path.</acceptance_criteria>
  <done>DASH-05 satisfied: /api/gas fallthrough serves zero non-Gmail actions.</done>
</task>

<task type="auto">
  <name>Task 3: Terminal gate — tsc + eslint + Playwright (0 failures) + diff artifact + STOP</name>
  <files>artifacts/phase27_06_diff.txt, artifacts/phase27_playwright.txt</files>
  <action>`npx tsc --noEmit` + `npm run lint` + full `npm run test:unit` (zero regressions) from tech-pwa/. Run the Playwright suite (`npx playwright test`) — capture output to `artifacts/phase27_playwright.txt`. The phase merge ceiling is 0 Playwright failures; if any fail, STOP and report (do not paper over). Push branch, `git diff main...HEAD > artifacts/phase27_06_diff.txt`, commit both artifacts, post to Claude Code. STOP — phase merge only on Claude Code clear.</action>
  <verify>
    <automated>cd tech-pwa && npx tsc --noEmit && npm run test:unit && bash -c 'set -o pipefail; npx playwright test --reporter=line | tee ../artifacts/phase27_playwright.txt' && echo "gate GREEN (0 playwright failures — baseline 22 passed / 67 skipped / 0 failed local)"</automated>
  </verify>
  <done>tsc + lint clean, unit suite green, Playwright captured with 0 failures, both artifacts committed, posted to Claude Code, stopped pending merge clear.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard-api.ts routing → Neon routes vs /api/gas | A mis-wired action could silently fall through to GAS, writing a Sheets row instead of Neon |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-14 | Tampering | ported write silently falling through to /api/gas (dual write to Sheets) | mitigate | Explicit-failure return on every mutating special case; Task 2 static audit asserts all 11 actions set data before the /api/gas block |
| T-27-15 | Repudiation | DEV_BLOCKED_WRITES left stale → dev writes silently no-op'd | mitigate | Remove the four ported writes from DEV_BLOCKED_WRITES with documented comments (Pitfall 5) |
</threat_model>

<verification>
- All 11 in-scope actions wired to Neon routes (static audit, Task 2)
- Four ported writes removed from DEV_BLOCKED_WRITES
- Mutating actions return explicit error on failure (no /api/gas fallthrough)
- tsc + lint + full unit suite green
- Playwright 0 failures (phase merge ceiling)
</verification>

<success_criteria>
DASH-05 satisfied: dashboard-api.ts /api/gas fallthrough serves only Gmail-domain actions. All of Phase 27's ported actions hit Neon. Playwright green. Phase ready to merge on Claude Code clear; GAS DashboardAPI becomes caller-less (Phase 24 deletes).
</success_criteria>

<artifacts_this_phase_produces>
- Fully-wired dashboard-api.ts (11 actions → Neon, 4 writes un-blocked)
- DASH-05 static fallthrough audit (in SUMMARY)
- artifacts/phase27_06_diff.txt + artifacts/phase27_playwright.txt
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/27-dashboard-remainder/27-06-SUMMARY.md`. Include the DASH-05 audit table (each action → its Neon route + data-assignment line) and the Playwright pass count.
</output>
