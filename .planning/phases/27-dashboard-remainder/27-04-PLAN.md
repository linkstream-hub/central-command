---
phase: 27-dashboard-remainder
plan: "04"
type: tdd
wave: 2
depends_on: ["27-02"]
files_modified:
  - tech-pwa/src/app/api/jobs/[jobId]/schedule-link/route.ts
  - tech-pwa/src/lib/schedule-link.ts
  - tech-pwa/src/lib/__tests__/schedule-link.test.ts
  - artifacts/phase27_04_diff.txt
autonomous: true
requirements: [DASH-04]
tags: [jobs, tracking-token, schedule-link, neon, drizzle, crypto, tdd]

must_haves:
  truths:
    - "POST /api/jobs/[jobId]/schedule-link writes a crypto.randomUUID() to jobs.trackingToken and returns { success, url } where url = `${base}/schedule/${token}`"
    - "The response field is named `url` (frontend reads res.url) — NOT `link` (GAS returned link)"
    - "expandScope requires NO new route — it delegates to the existing PATCH /api/jobs/[jobId] (estHours, notes already mapped there). markPTEGranted DROPPED (scope amendment 2026-06-11): zero callers — PTE granting already flows via updateJob from the JobDetailModal checkbox."
    - "schedule-link route is staff-auth + org-scoped"
  artifacts:
    - path: "tech-pwa/src/app/api/jobs/[jobId]/schedule-link/route.ts"
      provides: "generateTenantScheduleLink (DASH-04)"
      exports: ["POST"]
    - path: "tech-pwa/src/lib/schedule-link.ts"
      provides: "pure buildScheduleUrl + scope-note append helpers"
  key_links:
    - from: "jobs/[jobId]/schedule-link/route.ts"
      to: "jobs.trackingToken"
      via: "Drizzle update set trackingToken = randomUUID"
      pattern: "trackingToken"
---

<objective>
Implement the dispatcher job-write actions for DASH-04 that operate on the `jobs` table: expandScope and generateTenantScheduleLink. (markPTEGranted dropped — dead action string with zero frontend callers; PTE flows via updateJob.)

Purpose: Research Finding 4 confirms markPTEGranted and expandScope have NO GAS implementation (they only appear in DEV_BLOCKED_WRITES). Both are thin job-field updates the existing PATCH /api/jobs/[jobId] route already handles (it maps `pteGranted→pte`, `estHours`, `notes`). So expandScope needs NO new route — only wiring (Plan 06); markPTEGranted needs NOTHING (no caller exists). generateTenantScheduleLink needs a real new route: GAS used a Sheets rowIndex and returned `link`; the frontend sends `jobId` and reads `url`. Finding 3 — must return `url`, not `link`.

Output: One new route (schedule-link) + a pure helper module (URL builder + scope-note append) with unit tests. The expandScope route decision is documented here for Plan 06 to wire as a PATCH special case.

BINDING CONTRACT for Plan 06 (expandScope): the special case MUST (1) GET /api/jobs/{jobId} to read current estHours and notes, (2) compute newEstHours = current + hoursToAdd and newNotes = notes + appended additionalWork, (3) PATCH the computed FINAL values. The PATCH route stores estHours verbatim — PATCHing hoursToAdd directly would overwrite total hours with the delta and corrupt the job. No alternative path.
</objective>

<execution_context>
@C:/PTOW/1_APT_Central_Command/.claude/get-shit-done/workflows/execute-plan.md
@C:/PTOW/1_APT_Central_Command/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-dashboard-remainder/27-RESEARCH.md

<interfaces>
jobs table (schema.ts lines 337-375) — relevant columns:
```typescript
jobId: text (unique), orgId: text, pte: text, estHours: real, notes: text,
tech: text, trackingToken: text  // existing column — no migration needed
```

Existing PATCH /api/jobs/[jobId] (tech-pwa/src/app/api/jobs/[jobId]/route.ts) ALREADY maps:
- body.pteGranted → updates.pte
- body.estHours → updates.estHours
- body.notes → updates.notes
- body.assignedTech → updates.tech
- body.status → updates.status
Dual-auth (session OR x-api-key) already enforced. expandScope reuses this — confirmed, no new route.

Frontend call sites (tech-pwa/src/components/dashboard/JobDetailModal.tsx):
- handleMarkPTEGranted (line ~403): already calls updateJob → PATCH works today.
- expandScope (line ~500): dashboardRequest('expandScope', { jobId, leadId, rowIndex, additionalWork, hoursToAdd, reassignTech }). Sandbox behavior (sandbox-store.ts ~314): estHours += hoursToAdd; notes appends additionalWork.
- generateTenantScheduleLink (line ~488): dashboardRequest('generateTenantScheduleLink', { jobId, leadId }). Expects { success, url? }. Reads res.url.

Env for URL: NEXT_PUBLIC_BASE_URL (verify on Vercel — Assumption A2); fallback to process.env.VERCEL_URL prefixed with https://. GAS used DASHBOARD_BASE_URL script property → /schedule/{token}.

Auth: dual-auth (session OR x-api-key) + force-dynamic — same as the existing [jobId]/route.ts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — pure schedule-link + scope-note helpers (RED→GREEN)</name>
  <files>tech-pwa/src/lib/schedule-link.ts, tech-pwa/src/lib/__tests__/schedule-link.test.ts</files>
  <behavior>
    Pure, DB-free helpers:
    - `buildScheduleUrl(token, base)` → `${base}/schedule/${token}` with no double slashes (trim trailing slash on base). Test: base with trailing slash and without both yield a single-slash URL; token is appended verbatim.
    - `resolveBaseUrl(env)` → prefers NEXT_PUBLIC_BASE_URL; else `https://${VERCEL_URL}`; else throws/returns null with a clear reason (so the route can 500 explicitly rather than emit a broken URL — Assumption A2 guard). Test: all three branches.
    - `appendScopeNote(currentNotes, additionalWork)` → currentNotes with additionalWork appended on a new line (e.g. `\n[Scope+] ${additionalWork}`); empty currentNotes → just the note. Test: existing notes preserved + appended; null/empty current → note only. Mirrors sandbox-store behavior.
    Write the test file FIRST (RED — module not found), commit `test(27-04): ...`; implement schedule-link.ts (GREEN), commit `feat(27-04): ...`. (coverage.include for this file is registered by Plan 02 — do not edit vitest.config.ts.) Do NOT generate the UUID inside these helpers (the route calls crypto.randomUUID() — keep helpers deterministic/testable).
  </behavior>
  <action>Three named exports, no DB/crypto imports inside helpers.</action>
  <read_first>
    - .planning/phases/27-dashboard-remainder/27-RESEARCH.md (sections 7 + 8, Finding 3, Pitfall 4, Assumption A2)
    - tech-pwa/src/lib/sandbox-store.ts (expandScope behavior ~314-324)
    - tech-pwa/src/lib/__tests__/access-codes.test.ts (test style)
  </read_first>
  <verify>
    <automated>cd tech-pwa && npm run test:unit -- -t "scheduleLink"</automated>
  </verify>
  <acceptance_criteria>RED shows missing-module; GREEN shows URL builder + base resolver + scope-note tests passing; no double-slash URLs; base-missing path returns an explicit null/throw (no broken URL).</acceptance_criteria>
  <done>schedule-link.ts exports all three helpers; tests green; zero regressions.</done>
</task>

<task type="auto">
  <name>Task 2: schedule-link route (POST) — writes trackingToken, returns url</name>
  <files>tech-pwa/src/app/api/jobs/[jobId]/schedule-link/route.ts</files>
  <action>
    POST handler: dual-auth guard (session OR x-api-key; 401), `export const dynamic = 'force-dynamic'`. Read `jobId` from the path param. Generate `const token = crypto.randomUUID()`. Resolve base via `resolveBaseUrl(process.env)` — if null, return `{ success:false, message:'BASE_URL not configured' }` with 500 (explicit failure, never a broken URL). Update jobs `set({ trackingToken: token })` where `eq(jobs.jobId, jobId) AND eq(jobs.orgId, session.user.orgId ?? 'APT-CA')`. If zero rows matched, return 404 `{ success:false, error:'JOB_NOT_FOUND' }`. On success return `{ success:true, url: buildScheduleUrl(token, base) }` — field MUST be `url` (Finding 3). Validate jobId non-empty (V5).
    Do NOT create a route for expandScope — record in the SUMMARY that it wires to the existing PATCH /api/jobs/[jobId] (expandScope → `{ estHours: current + hoursToAdd, notes: appendScopeNote(...), assignedTech: reassignTech || unchanged }`). Plan 06 implements the wiring; this plan provides the helper + contract.
  </action>
  <read_first>
    - tech-pwa/src/app/api/jobs/[jobId]/route.ts (auth + param + Drizzle update pattern — read in FULL)
    - tech-pwa/src/lib/schema.ts (jobs 337-375)
  </read_first>
  <verify>
    <automated>cd tech-pwa && npx tsc --noEmit && npm run test:unit -- -t "scheduleLink"</automated>
  </verify>
  <acceptance_criteria>Route compiles, returns `url` (not `link`), writes trackingToken via crypto.randomUUID, org-scoped update, 404 on no-match, explicit 500 when base URL missing. No new route files for PTE/scope. NEXT_PUBLIC_BASE_URL confirmed present in Vercel env (or VERCEL_URL fallback documented) before terminal gate.</acceptance_criteria>
  <done>schedule-link route done; PTE/scope delegation contract documented for Plan 06.</done>
</task>

<task type="auto">
  <name>Task 3: Terminal gate — tsc + eslint + diff artifact + STOP</name>
  <files>artifacts/phase27_04_diff.txt</files>
  <action>`npx tsc --noEmit` + `npm run lint` + full `npm run test:unit` (zero regressions). Push, `git diff main...HEAD > artifacts/phase27_04_diff.txt`, commit, post to Claude Code. STOP.</action>
  <verify>
    <automated>cd tech-pwa && npx tsc --noEmit && npm run test:unit && test -f ../artifacts/phase27_04_diff.txt && echo "gate ready"</automated>
  </verify>
  <done>tsc + lint clean, full suite green, diff artifact committed, posted, stopped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard → /api/jobs/[jobId]/schedule-link | Generates a public tokened URL that a tenant later visits unauthenticated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-08 | Information Disclosure | schedule-link token enumeration | mitigate | `crypto.randomUUID()` (128-bit, non-sequential) — never hand-rolled; the public /schedule/{token} consumer is out of scope this phase |
| T-27-09 | Tampering | schedule-link write to arbitrary job | mitigate | org_id WHERE on update; jobId validated; staff dual-auth 401 guard |
| T-27-10 | Information Disclosure | broken URL leaking internal host via VERCEL_URL fallback | accept | resolveBaseUrl prefers NEXT_PUBLIC_BASE_URL; VERCEL_URL fallback is the platform-provided public host — acceptable |
</threat_model>

<verification>
- schedule-link writes trackingToken (uuid) and returns `url`
- markPTEGranted/expandScope contract documented (no new route)
- Route dual-auth + org-scoped + explicit failure when base URL missing
- Full unit suite green
</verification>

<success_criteria>
generateTenantScheduleLink served by a Neon-backed route returning `url`; markPTEGranted + expandScope delegation contract ready for Plan 06 wiring. DASH-04 (job-write half) complete; submitFeedback half is Plan 05.
</success_criteria>

<artifacts_this_phase_produces>
- /api/jobs/[jobId]/schedule-link route
- tech-pwa/src/lib/schedule-link.ts pure helpers + unit tests
- markPTEGranted/expandScope → existing-PATCH delegation contract (documented for Plan 06)
- artifacts/phase27_04_diff.txt
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/27-dashboard-remainder/27-04-SUMMARY.md`. Document the exact PATCH bodies Plan 06 must send for markPTEGranted and expandScope, and the schedule-link request/response contract.
</output>
