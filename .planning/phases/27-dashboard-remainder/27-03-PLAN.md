---
phase: 27-dashboard-remainder
plan: "03"
type: tdd
wave: 1
depends_on: []
files_modified:
  - tech-pwa/src/app/api/schedule/availability/route.ts
  - tech-pwa/src/lib/availability-data.ts
  - tech-pwa/src/lib/__tests__/tech-availability.test.ts
  - artifacts/phase27_03_diff.txt
autonomous: true
requirements: [DASH-02]
tags: [availability, neon, drizzle, date-expansion, tdd, vitest]

must_haves:
  truths:
    - "GET /api/schedule/availability returns outDates: { 'Tech Name': ['YYYY-MM-DD', ...] } for approved leave overlapping the requested week"
    - "Leave ranges are expanded across every date they cover, not just the start date"
    - "Route is staff-auth (auth() session OR x-api-key) and org-scoped"
  artifacts:
    - path: "tech-pwa/src/app/api/schedule/availability/route.ts"
      provides: "getTechAvailability (DASH-02)"
      exports: ["GET"]
    - path: "tech-pwa/src/lib/availability-data.ts"
      provides: "pure week-date + leave-expansion helpers"
  key_links:
    - from: "schedule/availability/route.ts"
      to: "time_off_requests JOIN employees"
      via: "Drizzle select status=Approved, week overlap, join for name"
      pattern: "time_off|timeOffRequests"
---

<objective>
Port getTechAvailability (DASH-02) as a Neon-backed Next.js read.

SCOPE AMENDMENT (2026-06-11, COO + lead dev): getCalendarData (DASH-03) is REMOVED from this plan and DEFERRED. Dispatch's real scheduling truth today is the daily-assignment Google Sheet + a Buildertrend calendar feed — NOT Neon. A Neon-backed calendar would render false/empty data. The calendar ships after the schedule redesign (sr-01 Lock-and-Send) makes CC the scheduling surface. Plan 27-06 gives getCalendarData an explicit not-implemented response so it never falls through to /api/gas.

Purpose: getTechAvailability moves off the separate TOM spreadsheet onto the Neon `time_off_requests` table — this data IS in Neon (research-verified), unlike calendar scheduling data.

Output: One route + a pure date-expansion helper module with unit tests (week-overlap logic).
</objective>

<execution_context>
@C:/PTOW/1_APT_Central_Command/.claude/get-shit-done/workflows/execute-plan.md
@C:/PTOW/1_APT_Central_Command/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-dashboard-remainder/27-RESEARCH.md

<interfaces>
time_off_requests table (schema.ts lines 175-187):
```typescript
employeeId: integer (notNull), requestDate: text (startDate 'YYYY-MM-DD'),
returnDate: text (endDate 'YYYY-MM-DD'), leaveType: text, status: text (default 'Pending'),
orgId: text
```
employees table (schema.ts lines 125-159): id, orgId, name (notNull) — JOIN on time_off_requests.employeeId = employees.id for the tech name key.

getTechAvailability response (dashboard-api.ts line 683-686):
```typescript
{ outDates: Record<string, string[]> }   // { "Tech Name": ["YYYY-MM-DD", ...] }
```
Frontend sends { weekStart: 'YYYY-MM-DD' }.

Auth pattern: dual-auth (auth() session OR x-api-key === DASHBOARD_API_KEY), force-dynamic — same as Plan 02 / live-status.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — pure week-date helpers RED→GREEN</name>
  <files>tech-pwa/src/lib/availability-data.ts, tech-pwa/src/lib/__tests__/tech-availability.test.ts</files>
  <behavior>
    Pure, DB-free helpers (the logic the route layer feeds rows into):
    - `weekDates(weekStart)` → 5 weekday 'YYYY-MM-DD' strings Mon–Fri from weekStart (port GAS getTechAvailabilityWeekDA: builds 5 weekday dates).
    - `expandLeaveOverWeek(startDate, endDate, weekDates)` → subset of weekDates that fall within [startDate, endDate] inclusive. Test: leave Mon–Wed over a Mon-start week → [Mon,Tue,Wed]; leave entirely before the week → []; single-day leave → [that day].
    Write the test file FIRST (RED — module not found), commit `test(27-03): ...`, implement availability-data.ts (GREEN), commit `feat(27-03): ...`. Do NOT touch vitest.config.ts — Plan 27-02 is the sole owner and registers availability-data.ts in coverage.include. Use string date comparison (ISO 'YYYY-MM-DD' sorts lexically) — do not hand-roll Date math where string compare suffices.
  </behavior>
  <action>Implement both helpers as named exports. No Drizzle/DB imports.</action>
  <read_first>
    - .planning/phases/27-dashboard-remainder/27-RESEARCH.md (sections 4, Pitfall 3)
    - tech-pwa/src/lib/__tests__/access-codes.test.ts (test style)
  </read_first>
  <verify>
    <automated>cd tech-pwa && npm run test:unit -- -t "availability"</automated>
  </verify>
  <acceptance_criteria>RED commit shows missing-module failure; GREEN shows all availability helper tests passing; leave-range expansion covers all in-window dates (not just start).</acceptance_criteria>
  <done>availability-data.ts exports both helpers, tests green, zero regressions vs baseline.</done>
</task>

<task type="auto">
  <name>Task 2: availability route</name>
  <files>tech-pwa/src/app/api/schedule/availability/route.ts</files>
  <action>
    Dual-auth guard (auth() session OR x-api-key), `export const dynamic = 'force-dynamic'`, org_id WHERE on every query.
    GET: read `weekStart` from query (`new URL(req.url).searchParams`). Validate /^\d{4}-\d{2}-\d{2}$/ — 400 on malformed (V5). Compute `weekDates(weekStart)`. Select time_off_requests where `eq(orgId,...) AND eq(status,'Approved')` JOIN employees (innerJoin on employeeId=id) for the name. For each row, `expandLeaveOverWeek(requestDate, returnDate, weekDates)` and accumulate into `outDates[employeeName]`. Return `{ success:true, outDates }`. (Verify Assumption A3: requestDate=start, returnDate=end.)
  </action>
  <read_first>
    - tech-pwa/src/lib/schema.ts (timeOffRequests 175-187, employees 125-159)
    - tech-pwa/src/app/api/dashboard/live-status/route.ts (auth + Drizzle join style)
  </read_first>
  <verify>
    <automated>cd tech-pwa && npx tsc --noEmit && npm run test:unit -- -t "availability"</automated>
  </verify>
  <acceptance_criteria>Route compiles, dual-auth + org_id scoping present, outDates keyed by employees.name, malformed-date 400 guard present.</acceptance_criteria>
  <done>Route returns correctly-shaped response; date logic delegated to the tested helpers.</done>
</task>

<task type="auto">
  <name>Task 3: Terminal gate — tsc + eslint + diff artifact + STOP</name>
  <files>artifacts/phase27_03_diff.txt</files>
  <action>`npx tsc --noEmit` + `npm run lint` + full `npm run test:unit` (zero regressions) from tech-pwa/. Push, `git diff main...HEAD > artifacts/phase27_03_diff.txt`, commit, post to Claude Code. STOP.</action>
  <verify>
    <automated>cd tech-pwa && npx tsc --noEmit && npm run test:unit && test -f ../artifacts/phase27_03_diff.txt && echo "gate ready"</automated>
  </verify>
  <done>tsc + lint clean, full suite green, diff artifact committed, posted, stopped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard browser → /api/schedule/availability | Reads of employee leave data must be staff-scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06 | Information Disclosure | cross-tenant leave read | mitigate | `eq(orgId, ...)` on every select; dual-auth 401 guard |
| T-27-07 | Tampering | malformed weekStart param | mitigate | Regex validation, 400 on malformed; queries parameterized via Drizzle |
</threat_model>

<verification>
- availability returns outDates keyed by employee name, only approved leave, week-overlap correct
- Route dual-auth + org-scoped + input-validated
- Full unit suite green, zero regressions
</verification>

<success_criteria>
DASH-02 served by a Neon-backed Next.js read with correct date-expansion. DASH-03 deferred per scope amendment. Wiring into dashboard-api.ts happens in Plan 06.
</success_criteria>

<artifacts_this_phase_produces>
- /api/schedule/availability route
- tech-pwa/src/lib/availability-data.ts pure helpers + unit tests
- artifacts/phase27_03_diff.txt
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/27-dashboard-remainder/27-03-SUMMARY.md`. Document exact query-param + response contracts for Plan 06 wiring.
</output>
