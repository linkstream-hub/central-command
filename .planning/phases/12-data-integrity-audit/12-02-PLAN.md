---
phase: "12"
plan: "02"
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - tools/orchestrator/neon_audit.py
autonomous: true
requirements:
  - DINT-01
  - DINT-02

must_haves:
  truths:
    - "neon_audit.py imports succeed and --help prints usage without connectivity to Sheets or Neon"
    - "fetch_sheets_jobs returns a set of job_id strings filtered to the 21-day window"
    - "fetch_sheets_time_records returns a set of record_id strings filtered to the 21-day window"
    - "fetch_sheets_job_comments returns a count (not a set) of rows in the 21-day window"
    - "fetch_neon_jobs returns a set of job_id strings for org_id=APT-CA in the window"
    - "fetch_neon_time_records returns a set of record_id strings for org_id=APT-CA in the window"
    - "fetch_neon_job_comments returns a dict with total count and count where sheets_id IS NOT NULL"
    - "env var validation exits with code 2 and prints actionable error message when vars are missing"
    - "Neon connection failure exits with code 2 with psycopg2.OperationalError details printed"
  artifacts:
    - path: "tools/orchestrator/neon_audit.py"
      provides: "Core audit script — data fetch layer + CLI scaffolding"
      min_lines: 150
      exports: ["fetch_sheets_jobs", "fetch_sheets_time_records", "fetch_sheets_job_comments", "fetch_neon_jobs", "fetch_neon_time_records", "fetch_neon_job_comments", "parse_args", "get_cutoff"]
  key_links:
    - from: "neon_audit.py fetch_sheets_jobs"
      to: "Dispatch Queue tab, row[0]=Timestamp, row[1]=Lead ID"
      via: "gspread worksheet('Dispatch Queue').get_all_values()"
      pattern: "Dispatch Queue"
    - from: "neon_audit.py fetch_neon_jobs"
      to: "jobs table, job_id column, timestamp::date >= cutoff"
      via: "psycopg2 cursor.execute"
      pattern: "SELECT job_id FROM jobs"
    - from: "neon_audit.py fetch_neon_time_records"
      to: "time_records table, record_id column, date >= cutoff (text comparison)"
      via: "psycopg2 cursor.execute"
      pattern: "SELECT record_id FROM time_records"
---

<objective>
Build the data fetch layer of neon_audit.py: CLI scaffolding, environment validation, connection setup, and the six fetch functions (three from Sheets, three from Neon). The diff and report engine comes in Plan 03.

Purpose: By the end of this plan, running "python tools/orchestrator/neon_audit.py --help" works without any connectivity. All fetch functions are importable and independently testable with mocks. The script structure is finalized so Plan 03 can wire diff + output without restructuring.

Output: tools/orchestrator/neon_audit.py (~160 lines), complete fetch layer, argparse CLI, env validation.
</objective>

<execution_context>
@A:/PTOW/1_APT_Central_Command/.claude/get-shit-done/workflows/execute-plan.md
@A:/PTOW/1_APT_Central_Command/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@A:/PTOW/1_APT_Central_Command/.planning/phases/12-data-integrity-audit/12-CONTEXT.md
@A:/PTOW/1_APT_Central_Command/.planning/phases/12-data-integrity-audit/12-RESEARCH.md
@A:/PTOW/1_APT_Central_Command/tools/orchestrator/ptow_adw.py
@A:/PTOW/1_APT_Central_Command/docs/SHEETS_SCHEMA.md

<interfaces>
<!-- Column indices — 0-indexed Python row access -->
SPREADSHEET_ID = "1eCSHpj5381R7Hhloe718r1n8fEfesY2tjYOuOLxTSd4"

Dispatch Queue tab:
  row[0] = Timestamp   (date filter)  format: "yyyy-MM-dd HH:mm"
  row[1] = Lead ID     (key column)   matches jobs.job_id

Time Records tab:
  row[0] = RECORD_ID   (key column)   matches time_records.record_id
  row[6] = DATE        (date filter)  format: "YYYY-MM-DD"
  NOTE: Index 6 is ASSUMED from SHEETS_SCHEMA.md narrative ordering
  ("0-15: RECORD_ID, JOB_ID, TECH_ID, TECH_NAME, CLOCK_IN, STATUS, DATE...")
  Add a comment in the code: "# DATE column — index 6 ASSUMED from SHEETS_SCHEMA.md; verify on first run"

JobComments tab:
  row[1] = Lead ID     (not a key — not used for diff)
  row[5] = Timestamp   (date filter)  format: "yyyy-MM-ddTHH:mm:ss"
  Count-only: no key column.

<!-- Neon SQL contracts -->
jobs query:
  SELECT job_id FROM jobs
  WHERE org_id = 'APT-CA'
    AND timestamp::date >= %s
  Parameters: (cutoff_date_str,)  -- cutoff as 'YYYY-MM-DD' string

time_records query:
  SELECT record_id FROM time_records
  WHERE org_id = 'APT-CA'
    AND date >= %s
  Parameters: (cutoff_date_str,)
  NOTE: date is text type — lexicographic comparison works for ISO dates

job_comments query:
  SELECT COUNT(*) AS total, COUNT(sheets_id) AS with_sheets_id
  FROM job_comments
  WHERE org_id = 'APT-CA'
    AND created_at >= %s
  Parameters: (cutoff_datetime_str,)  -- 'YYYY-MM-DD 00:00:00'

<!-- ptow_adw.py patterns to follow -->
- Module-level REPO_ROOT = Path(__file__).parent.parent.parent
- def log(msg: str): ts = datetime.now().strftime("%H:%M:%S"); print(f"[{ts}] {msg}", flush=True)
- def banner(section: str): print(f"\n{'='*60}", flush=True); log(f"AUDIT: {section}"); print(f"{'='*60}\n", flush=True)
- argparse with parse_args() function
- sys.exit(0/1/2) — exit codes defined at top of file
- stdout reconfigure for UTF-8 (copy from ptow_adw.py lines 37-40)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold neon_audit.py — CLI, env validation, connection setup, and Sheets fetch functions</name>
  <read_first>
    - A:/PTOW/1_APT_Central_Command/tools/orchestrator/ptow_adw.py (lines 1–80: imports, stdout reconfigure, REPO_ROOT, log/banner, argparse pattern)
    - A:/PTOW/1_APT_Central_Command/.planning/phases/12-data-integrity-audit/12-RESEARCH.md (Pattern 1: gspread auth, Pattern 4: date filtering, Pattern 5: argparse)
    - A:/PTOW/1_APT_Central_Command/docs/SHEETS_SCHEMA.md (Dispatch Queue columns, Time Records columns, JobComments columns — just updated in Plan 01)
  </read_first>
  <behavior>
    - parse_args() with --days 21, --table choices=["jobs","time_records","job_comments","all"], default="all"
    - Missing GOOGLE_SERVICE_ACCOUNT_JSON → print "ERROR: GOOGLE_SERVICE_ACCOUNT_JSON not set — set it to the path of your GCP service account key JSON file" → sys.exit(2)
    - Missing DATABASE_URL_UNPOOLED → print "ERROR: DATABASE_URL_UNPOOLED not set — source tech-pwa/.env.local or export manually" → sys.exit(2)
    - psycopg2.OperationalError on connect → print "ERROR: Cannot connect to Neon: {e}" → sys.exit(2)
    - gspread exception on auth → print "ERROR: Cannot authenticate with Google Sheets: {e}" → sys.exit(2)
    - get_cutoff(days=21) → returns date object for today - 21 days using ZoneInfo("America/Los_Angeles")
    - parse_sheets_date(val) → tries multiple formats: "%Y-%m-%d %H:%M:%S", "%Y-%m-%d %H:%M", "%Y-%m-%dT%H:%M:%S", "%Y-%m-%d" → returns date or None for empty/unparseable
    - fetch_sheets_jobs(spreadsheet, cutoff) → worksheet("Dispatch Queue"), get_all_values(), skip row[0] (header), filter rows where parse_sheets_date(row[0]) >= cutoff and row[1] non-empty → return set of row[1] values
    - fetch_sheets_time_records(spreadsheet, cutoff) → worksheet("Time Records"), filter rows where parse_sheets_date(row[6]) >= cutoff and row[0] non-empty → return set of row[0] values
    - fetch_sheets_job_comments(spreadsheet, cutoff) → worksheet("JobComments"), count rows where parse_sheets_date(row[5]) >= cutoff → return int (count only)
  </behavior>
  <action>
    Create A:/PTOW/1_APT_Central_Command/tools/orchestrator/neon_audit.py.

    File structure (top to bottom):
    1. Shebang: #!/usr/bin/env python3
    2. Module docstring: triple-quoted block with USAGE comment (how to run, env vars required, exit codes 0/1/2). Per D-05: document that DATABASE_URL_UNPOOLED must include sslmode=require (copy from Neon dashboard).
    3. UTF-8 stdout reconfigure block (copy from ptow_adw.py lines 37-40).
    4. Imports: argparse, json, os, sys, psycopg2, gspread, google.oauth2.service_account.Credentials, datetime/timedelta/date, ZoneInfo from zoneinfo, pathlib.Path.
    5. Constants: SPREADSHEET_ID, SCOPES list (spreadsheets.readonly + drive.readonly), EXIT_PASS=0, EXIT_FAIL=1, EXIT_CONN=2.
    6. log(msg) and banner(section) functions — exact pattern from ptow_adw.py.
    7. parse_args() — argparse with --days and --table.
    8. get_cutoff(days: int) -> date.
    9. parse_sheets_date(val: str) -> date | None — try four formats in order.
    10. build_sheets_client() -> gspread.Spreadsheet — reads GOOGLE_SERVICE_ACCOUNT_JSON from env, validates, opens spreadsheet by SPREADSHEET_ID. sys.exit(EXIT_CONN) on error.
    11. build_neon_conn() -> psycopg2.connection — reads DATABASE_URL_UNPOOLED from env, validates, connects. sys.exit(EXIT_CONN) on OperationalError.
    12. fetch_sheets_jobs(spreadsheet, cutoff: date) -> set[str]
    13. fetch_sheets_time_records(spreadsheet, cutoff: date) -> set[str]
    14. fetch_sheets_job_comments(spreadsheet, cutoff: date) -> int (count only)
    15. Placeholder stubs for fetch_neon_jobs, fetch_neon_time_records, fetch_neon_job_comments, run_audit, and main — each with "raise NotImplementedError" so Plan 03 can implement them cleanly. main() must call parse_args() and print a "not yet implemented" banner so --help works.
    16. if __name__ == "__main__": main()

    Do NOT inline fenced code blocks or complete file contents in this action description — implement the file directly.

    Key constraints:
    - Never print the DSN value — only print actionable "not set" error messages.
    - Comment on the TIME_RECORDS DATE column index: "# DATE column — index 6 ASSUMED from SHEETS_SCHEMA.md; verify on first run"
    - Use context manager pattern for psycopg2: with build_neon_conn() as conn: with conn.cursor() as cur: ...
    - gspread.authorize(creds) pattern (not gspread.service_account() shortcut — see RESEARCH.md Pitfall 2).
  </action>
  <verify>
    <automated>
      cd A:/PTOW/1_APT_Central_Command && python -m py_compile tools/orchestrator/neon_audit.py && python tools/orchestrator/neon_audit.py --help
    </automated>
  </verify>
  <acceptance_criteria>
    - "python -m py_compile tools/orchestrator/neon_audit.py" exits with code 0 (no syntax errors)
    - "python tools/orchestrator/neon_audit.py --help" prints usage including --days and --table options without requiring GOOGLE_SERVICE_ACCOUNT_JSON or DATABASE_URL_UNPOOLED to be set
    - "python -c 'from tools.orchestrator import neon_audit'" — wait, this is a script not a module; instead: "python -c 'import importlib.util; spec = importlib.util.spec_from_file_location(\"neon_audit\", \"tools/orchestrator/neon_audit.py\"); mod = importlib.util.module_from_spec(spec)'" runs without ImportError
    - The functions fetch_sheets_jobs, fetch_sheets_time_records, fetch_sheets_job_comments, build_sheets_client, build_neon_conn, parse_args, get_cutoff, parse_sheets_date are all defined in the file
    - Running with no env vars set and no --help flag exits with code 2 and prints "ERROR: GOOGLE_SERVICE_ACCOUNT_JSON not set"
  </acceptance_criteria>
  <done>neon_audit.py scaffolded: --help works, all fetch functions defined, env validation exits with code 2 on missing vars</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Neon fetch functions (fetch_neon_jobs, fetch_neon_time_records, fetch_neon_job_comments)</name>
  <read_first>
    - A:/PTOW/1_APT_Central_Command/tools/orchestrator/neon_audit.py (just created in Task 1 — read current state before modifying)
    - A:/PTOW/1_APT_Central_Command/.planning/phases/12-data-integrity-audit/12-RESEARCH.md (Pattern 2: psycopg2 connection, Code Examples: Neon Query with Window Filter, Pitfall 4: text date column)
    - A:/PTOW/1_APT_Central_Command/tech-pwa/src/lib/schema.ts (lines 216–255: time_records schema; lines 337–374: jobs schema; lines 376–388: job_comments schema — confirm column names and types)
  </read_first>
  <behavior>
    - fetch_neon_jobs(conn, cutoff_date: date) -> set[str]: cursor executes SELECT job_id FROM jobs WHERE org_id = %s AND timestamp::date >= %s; ("APT-CA", str(cutoff_date)); returns {row[0] for row in cur.fetchall()}
    - fetch_neon_time_records(conn, cutoff_date: date) -> set[str]: cursor executes SELECT record_id FROM time_records WHERE org_id = %s AND date >= %s; ("APT-CA", str(cutoff_date)); returns set of record_id strings (text column — lexicographic comparison is correct for ISO dates)
    - fetch_neon_job_comments(conn, cutoff_date: date) -> dict: cursor executes SELECT COUNT(*) AS total, COUNT(sheets_id) AS with_sheets_id FROM job_comments WHERE org_id = %s AND created_at >= %s; ("APT-CA", datetime.combine(cutoff_date, datetime.min.time()).strftime("%Y-%m-%d 00:00:00")); returns {"total": int(rows[0][0]), "with_sheets_id": int(rows[0][1]), "unverified": int(rows[0][0]) - int(rows[0][1])}
    - All three functions accept an open psycopg2 connection (not a URL) — caller manages connection lifecycle
    - Each function uses "with conn.cursor() as cur:" context manager
    - job_comments unverified count = total - with_sheets_id (rows where sheets_id IS NULL, i.e. direct POST inserts without Sheets backfill)
  </behavior>
  <action>
    In tools/orchestrator/neon_audit.py, replace the three NotImplementedError stubs for fetch_neon_jobs, fetch_neon_time_records, and fetch_neon_job_comments with their implementations.

    SQL for fetch_neon_jobs:
      SELECT job_id FROM jobs WHERE org_id = %s AND timestamp::date >= %s
      Params: ("APT-CA", str(cutoff_date))
      Add inline comment: "# timestamp::date cast handles timezone-naive timestamp column (confirmed schema.ts)"

    SQL for fetch_neon_time_records:
      SELECT record_id FROM time_records WHERE org_id = %s AND date >= %s
      Params: ("APT-CA", str(cutoff_date))
      Add inline comment: "# date is text type (schema.ts line 239) — ISO YYYY-MM-DD lexicographic comparison is correct"

    SQL for fetch_neon_job_comments:
      SELECT COUNT(*) AS total, COUNT(sheets_id) AS with_sheets_id
      FROM job_comments
      WHERE org_id = %s AND created_at >= %s
      Params: ("APT-CA", f"{str(cutoff_date)} 00:00:00")
      Return dict: {"total": total, "with_sheets_id": with_sheets_id, "unverified": total - with_sheets_id}

    Do not modify any other function in the file.
  </action>
  <verify>
    <automated>
      cd A:/PTOW/1_APT_Central_Command && python -m py_compile tools/orchestrator/neon_audit.py && python -c "
import pathlib, ast
src = pathlib.Path('tools/orchestrator/neon_audit.py').read_text()
assert 'fetch_neon_jobs' in src
assert 'fetch_neon_time_records' in src
assert 'fetch_neon_job_comments' in src
assert 'timestamp::date' in src
assert 'NotImplementedError' not in src.split('fetch_neon_jobs')[1].split('def ')[0]
print('PASS: all three Neon fetch functions implemented')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - "python -m py_compile tools/orchestrator/neon_audit.py" exits with code 0
    - python -c "src=open('tools/orchestrator/neon_audit.py').read(); assert 'def fetch_neon_jobs' in src; print('PASS')" exits 0
    - python -c "src=open('tools/orchestrator/neon_audit.py').read(); assert 'def fetch_neon_time_records' in src; print('PASS')" exits 0
    - python -c "src=open('tools/orchestrator/neon_audit.py').read(); assert 'def fetch_neon_job_comments' in src; print('PASS')" exits 0
    - python -c "src=open('tools/orchestrator/neon_audit.py').read(); jobs_body=src.split('def fetch_neon_jobs')[1].split('def ')[0]; assert 'NotImplementedError' not in jobs_body; print('PASS')" exits 0
    - python -c "src=open('tools/orchestrator/neon_audit.py').read(); assert 'timestamp::date' in src; print('PASS')" exits 0 (confirms correct Postgres timezone cast)
  </acceptance_criteria>
  <done>All three Neon fetch functions implemented and syntax-valid; fetch layer is complete and importable</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → psycopg2.connect() | DATABASE_URL_UNPOOLED contains credentials; must never appear in stdout |
| Google Sheets API → script | gspread returns untrusted string data; date parsing must handle malformed values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Information Disclosure | build_neon_conn() error path | mitigate | Print psycopg2.OperationalError message but never print the DSN; error branch only prints str(e) which contains host/port, not the full connection string |
| T-12-04 | Tampering | Sheets row data | accept | Audit is read-only; malformed Sheets data returns None from parse_sheets_date and is skipped — no code injection surface |
| T-12-05 | Denial of Service | gspread 429 rate limit | accept | One-shot script with three sequential tab fetches; well under 60 reads/min quota; retry logic not needed |
| T-12-SC | Tampering | pip install from requirements.txt | accept | Both packages rated [OK] by slopcheck 0.6.1 |
</threat_model>

<verification>
After both tasks complete:
- "python tools/orchestrator/neon_audit.py --help" prints usage
- "python -m py_compile tools/orchestrator/neon_audit.py" exits 0
- All six fetch functions (three Sheets, three Neon) defined in neon_audit.py
- No NotImplementedError in fetch_neon_* bodies
- grep "timestamp::date" confirms jobs query uses correct Postgres cast
</verification>

<success_criteria>
Plan 02 is complete when:
1. neon_audit.py exists with --help working and all six fetch functions implemented
2. Env var validation: missing GOOGLE_SERVICE_ACCOUNT_JSON → exit 2; missing DATABASE_URL_UNPOOLED → exit 2
3. Neon queries use correct SQL: jobs (timestamp::date cast), time_records (text date), job_comments (created_at with COUNT/COUNT(sheets_id))
4. Python syntax check passes: "python -m py_compile" exits 0
</success_criteria>

<output>
Create `.planning/phases/12-data-integrity-audit/12-02-SUMMARY.md` when done.
</output>
