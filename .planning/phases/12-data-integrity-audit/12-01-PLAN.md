---
phase: "12"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/orchestrator/requirements.txt
  - docs/SHEETS_SCHEMA.md
  - tools/orchestrator/tests/test_neon_audit.py
autonomous: false
requirements:
  - DINT-01
  - DINT-02
  - DINT-03

user_setup:
  - service: Google Cloud Platform
    why: "Service account for Google Sheets API access — gspread requires a service account JSON key"
    env_vars:
      - name: GOOGLE_SERVICE_ACCOUNT_JSON
        source: >
          GCP Console → IAM & Admin → Service Accounts → Create Service Account →
          name: 'neon-audit-reader', role: none (no project role needed) →
          Keys tab → Add Key → JSON → download the file to a safe local path (e.g.
          C:/secrets/neon-audit-sa.json) → set env var:
          $env:GOOGLE_SERVICE_ACCOUNT_JSON = "C:/secrets/neon-audit-sa.json"
    dashboard_config:
      - task: "Share three Sheets tabs with the service account email at Viewer level"
        location: >
          Open APT Lead Intake Master (ID: 1eCSHpj5381R7Hhloe718r1n8fEfesY2tjYOuOLxTSd4)
          → Share button → paste the service account email (format: name@project.iam.gserviceaccount.com)
          → Viewer → Send. This grants access to all tabs including Dispatch Queue, Time Records,
          and JobComments.

must_haves:
  truths:
    - "tools/orchestrator/requirements.txt exists and lists psycopg2-binary==2.9.12 and gspread==6.2.1"
    - "docs/SHEETS_SCHEMA.md JobComments tab section has a documented 6-column schema with Timestamp at index 5"
    - "tools/orchestrator/tests/test_neon_audit.py exists with stub test functions for DINT-01, DINT-02, DINT-03"
    - "Brandon has a service account key JSON file and GOOGLE_SERVICE_ACCOUNT_JSON env var set"
    - "Brandon has shared the APT Lead Intake Master spreadsheet with the service account email at Viewer level"
  artifacts:
    - path: "tools/orchestrator/requirements.txt"
      provides: "Python dependency declarations for the orchestrator toolchain"
      contains: "psycopg2-binary"
    - path: "docs/SHEETS_SCHEMA.md"
      provides: "JobComments tab column map (authoritative — derived from DashboardAPI.gs)"
      contains: "JobComments"
    - path: "tools/orchestrator/tests/test_neon_audit.py"
      provides: "Pytest stub file that Wave 2 will fill in"
      contains: "test_jobs_diff"
  key_links:
    - from: "neon_audit.py"
      to: "tools/orchestrator/requirements.txt"
      via: "pip install -r"
      pattern: "psycopg2-binary"
    - from: "neon_audit.py"
      to: "docs/SHEETS_SCHEMA.md"
      via: "executor reads column indices from this document"
      pattern: "JobComments"
---

<objective>
Lay the groundwork for the neon_audit.py script before any code is written. This plan has three parts:

1. Brandon prerequisite gate: Brandon must create a GCP service account, download the key JSON, set GOOGLE_SERVICE_ACCOUNT_JSON, and share the spreadsheet. The script cannot authenticate with Google Sheets until this is done.
2. AG documentation tasks: Update SHEETS_SCHEMA.md with the JobComments tab column map (derived from DashboardAPI.gs — confirmed in this session), and create tools/orchestrator/requirements.txt.
3. Test scaffold: Create the pytest stub file that Wave 2 will populate with real test logic.

Purpose: The Brandon action cannot be automated. It must complete before Wave 2 (script implementation). The documentation and scaffold tasks can run in parallel with Brandon's action.

Output: requirements.txt, updated SHEETS_SCHEMA.md, test stub file, Brandon's service account + env var.
</objective>

<execution_context>
@A:/PTOW/1_APT_Central_Command/.claude/get-shit-done/workflows/execute-plan.md
@A:/PTOW/1_APT_Central_Command/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@A:/PTOW/1_APT_Central_Command/.planning/ROADMAP.md
@A:/PTOW/1_APT_Central_Command/.planning/phases/12-data-integrity-audit/12-CONTEXT.md
@A:/PTOW/1_APT_Central_Command/.planning/phases/12-data-integrity-audit/12-RESEARCH.md
@A:/PTOW/1_APT_Central_Command/docs/SHEETS_SCHEMA.md
@A:/PTOW/1_APT_Central_Command/tools/orchestrator/tests/conftest.py

<interfaces>
<!-- JobComments tab column layout — confirmed from DashboardAPI.gs addJobCommentDA / getJobCommentsDA -->
JobComments tab (Spreadsheet ID: 1eCSHpj5381R7Hhloe718r1n8fEfesY2tjYOuOLxTSd4):
  Col 0: Comment ID   — string, format: "c_{epoch}_{4-char-random}"
  Col 1: Lead ID      — string, matches jobs.job_id (e.g. "WO-12345")
  Col 2: Author       — string, dispatcher name
  Col 3: Role         — string, e.g. "dispatch"
  Col 4: Body         — string, comment text (max 2000 chars)
  Col 5: Timestamp    — string, format: "yyyy-MM-dd'T'HH:mm:ss" in America/Los_Angeles

Header row (row 0): ["Comment ID", "Lead ID", "Author", "Role", "Body", "Timestamp"]
Auto-provisioned on first use (insertSheet + setValues header).

Date filter column for 21-day window: Col 5 (Timestamp), format "2026-05-31T14:30:00"
Parsing: datetime.strptime(val, "%Y-%m-%dT%H:%M:%S").date()

<!-- Time Records tab column layout — from SHEETS_SCHEMA.md narrative -->
Time Records tab:
  Col 0: RECORD_ID   — natural key, matches time_records.record_id
  Col 1: JOB_ID
  Col 2: TECH_ID
  Col 3: TECH_NAME
  Col 4: CLOCK_IN
  Col 5: STATUS
  Col 6: DATE        — ASSUMED index 6 per "0-15: RECORD_ID, JOB_ID, TECH_ID, TECH_NAME, CLOCK_IN, STATUS, DATE..."
                       format: "YYYY-MM-DD" (ISO date string)
  Cols 7-15: additional clock data
  Cols 16-19: GPS (Lat/Lng In/Out)
  Col 20: Entity ID
  Cols 21-27: attestation, supervisor, dispute fields

<!-- Dispatch Queue tab column layout — from SHEETS_SCHEMA.md -->
Dispatch Queue tab:
  Col 1:  Timestamp  — date filter, format "yyyy-MM-dd HH:mm" (0-indexed: col 0)
  Col 2:  Lead ID    — natural key, matches jobs.job_id (0-indexed: col 1)
  NOTE: SHEETS_SCHEMA.md uses 1-indexed column numbers. In Python row[index], use 0-indexed:
    row[0] = Timestamp
    row[1] = Lead ID (job_id key)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Brandon — create GCP service account and share spreadsheet</name>
  <what-built>
    AG cannot do this — it requires Brandon's personal GCP account login and Google Sheets share dialog.
    AG has documented exactly what Brandon needs to do below.
  </what-built>
  <how-to-verify>
    Step 1 — Create service account:
    1. Go to: https://console.cloud.google.com/iam-admin/serviceaccounts
    2. Select the APT project (the same one with the "APT Tech PWA" OAuth client).
    3. Click "Create Service Account".
    4. Name: neon-audit-reader | Description: Read-only Sheets access for neon_audit.py
    5. Click "Create and Continue" → skip role assignment (no project role needed) → Done.
    6. Click the new service account email (format: neon-audit-reader@YOUR-PROJECT.iam.gserviceaccount.com).
    7. Go to the "Keys" tab → "Add Key" → "Create new key" → JSON → Create.
    8. A JSON file downloads automatically. Move it somewhere safe, e.g.: C:\secrets\neon-audit-sa.json
    9. Do NOT commit this file. It is a secret.

    Step 2 — Set the env var (PowerShell):
    $env:GOOGLE_SERVICE_ACCOUNT_JSON = "C:\secrets\neon-audit-sa.json"
    (Add to your PowerShell profile if you want it to persist across sessions.)

    Step 3 — Share the spreadsheet:
    1. Open: https://docs.google.com/spreadsheets/d/1eCSHpj5381R7Hhloe718r1n8fEfesY2tjYOuOLxTSd4
    2. Click the Share button (top right).
    3. Paste the service account email from Step 1 (neon-audit-reader@YOUR-PROJECT.iam.gserviceaccount.com).
    4. Set permission to "Viewer".
    5. Uncheck "Notify people" (service accounts don't have inboxes).
    6. Click Share.

    Step 4 — Verify env var is set:
    echo $env:GOOGLE_SERVICE_ACCOUNT_JSON
    Expected: the path to your JSON file (not empty).
  </how-to-verify>
  <resume-signal>
    Type "service account ready" after completing all four steps above.
    Paste the service account email here so AG can document it: ______
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create requirements.txt and update SHEETS_SCHEMA.md with JobComments column map</name>
  <read_first>
    - A:/PTOW/1_APT_Central_Command/docs/SHEETS_SCHEMA.md (section "Tab: JobComments" — currently has no column schema)
    - A:/PTOW/1_APT_Central_Command/dashboard-api/DashboardAPI.gs (functions getJobCommentsSheet, addJobCommentDA, getJobCommentsDA — lines 3380–3481 approximately — to confirm column layout)
  </read_first>
  <action>
    Part A — Create tools/orchestrator/requirements.txt:

    Create the file at A:/PTOW/1_APT_Central_Command/tools/orchestrator/requirements.txt with these exact contents (pinned versions):

      psycopg2-binary==2.9.12
      gspread==6.2.1

    No other packages — all other dependencies (google-auth, zoneinfo, argparse, etc.) are either already installed as gspread transitive deps or are stdlib in Python 3.9+.

    Part B — Update docs/SHEETS_SCHEMA.md JobComments section:

    Find the "Tab: JobComments" section (currently: "Internal job comment threads. Auto-provisioned on first use.").
    Replace it with a documented column schema derived from DashboardAPI.gs getJobCommentsSheet() which sets the header: ["Comment ID", "Lead ID", "Author", "Role", "Body", "Timestamp"].

    The updated section must read:

      ### Tab: `JobComments`
      Internal job comment threads. Auto-provisioned on first use by `getJobCommentsSheet()` in DashboardAPI.gs.
      6 columns (0–5).

      | Col (0-indexed) | Field | Format | Notes |
      |---|---|---|---|
      | 0 | Comment ID | `c_{epoch}_{4-char-random}` | Generated by `addJobCommentDA` |
      | 1 | Lead ID | String (e.g. `WO-12345`) | Matches `jobs.job_id` in Neon |
      | 2 | Author | String | Dispatcher name |
      | 3 | Role | String (e.g. `dispatch`) | Comment author role |
      | 4 | Body | String (max 2000 chars) | Comment text |
      | 5 | Timestamp | `yyyy-MM-dd'T'HH:mm:ss` (America/Los_Angeles) | Written by `addJobCommentDA` using `Utilities.formatDate` |

      **Date filter column for audit:** Col 5 (Timestamp). Parse with `datetime.strptime(val, "%Y-%m-%dT%H:%M:%S").date()`.
      **No natural key linkable to Neon** — `neon_audit.py` performs count-only comparison for this tab.
      **Spreadsheet ID:** `1eCSHpj5381R7Hhloe718r1n8fEfesY2tjYOuOLxTSd4`
  </action>
  <verify>
    <automated>
      python -c "
      import pathlib
      r = pathlib.Path('A:/PTOW/1_APT_Central_Command/tools/orchestrator/requirements.txt').read_text()
      assert 'psycopg2-binary==2.9.12' in r, 'psycopg2-binary missing'
      assert 'gspread==6.2.1' in r, 'gspread missing'
      s = pathlib.Path('A:/PTOW/1_APT_Central_Command/docs/SHEETS_SCHEMA.md').read_text()
      assert 'Comment ID' in s, 'JobComments column map missing'
      assert 'Timestamp' in s and \"yyyy-MM-dd'T'HH:mm:ss\" in s, 'Timestamp format missing'
      assert 'Date filter column for audit' in s, 'audit note missing'
      print('PASS')
      "
    </automated>
  </verify>
  <acceptance_criteria>
    - tools/orchestrator/requirements.txt exists and contains exactly two lines: psycopg2-binary==2.9.12 and gspread==6.2.1
    - docs/SHEETS_SCHEMA.md JobComments section now has a 6-column table (Col 0–5) with field names matching DashboardAPI.gs header: ["Comment ID", "Lead ID", "Author", "Role", "Body", "Timestamp"]
    - The Timestamp format string "yyyy-MM-dd'T'HH:mm:ss" appears in the JobComments section
    - The audit note stating "count-only comparison" appears in the JobComments section
    - No other sections of SHEETS_SCHEMA.md are modified
  </acceptance_criteria>
  <done>requirements.txt pinned; SHEETS_SCHEMA.md JobComments section documented with 6-column map and audit note</done>
</task>

<task type="auto">
  <name>Task 3: Create test stub file tools/orchestrator/tests/test_neon_audit.py</name>
  <read_first>
    - A:/PTOW/1_APT_Central_Command/tools/orchestrator/tests/conftest.py (fixture patterns to reuse)
    - A:/PTOW/1_APT_Central_Command/.planning/phases/12-data-integrity-audit/12-RESEARCH.md (Validation Architecture section — test map for DINT-01, DINT-02, DINT-03)
  </read_first>
  <action>
    Create A:/PTOW/1_APT_Central_Command/tools/orchestrator/tests/test_neon_audit.py as a pytest stub file.

    The stub must define the following test functions — each with a pytest.mark.skip and a docstring describing what it will test. Wave 2 (12-03-PLAN.md) will fill in real implementations.

    Test functions required:
    - test_jobs_diff: Covers DINT-01 — key-by-key diff for jobs table. Mock gspread to return rows; mock psycopg2 cursor to return job_ids. Assert missing_from_neon and neon_only sets are correctly computed.
    - test_window_filter: Covers DINT-01 — rows older than --days are excluded from both Sheets and Neon sets.
    - test_time_records_neon_only_informational: Covers DINT-01 — Neon-only time_records keys (from direct clock-in path) do NOT cause exit code 1.
    - test_job_comments_count_only: Covers DINT-01 — job_comments comparison returns sheets_count, neon_count, and unverified_neon (rows with sheets_id IS NULL); no pass/fail boolean.
    - test_exit_code_pass: Covers DINT-02 — exit code 0 when no Sheets-to-Neon gaps exist.
    - test_exit_code_fail: Covers DINT-02 — exit code 1 when jobs or time_records have Sheets keys absent from Neon.
    - test_exit_code_connectivity: Covers DINT-02 — exit code 2 when DATABASE_URL_UNPOOLED is missing from env.
    - test_artifact_schema: Covers DINT-02 — JSON artifact written to artifacts/ has keys: run_at, window_days, overall, tables (with jobs, time_records, job_comments sub-keys).
    - test_terminal_output: Covers DINT-03 — stdout contains "[PASS]" or "[FAIL]" prefix for each table, followed by "Sheets:" and "Neon:" counts.

    Each function body: raise NotImplementedError("stub — implement in 12-03-PLAN.md")
    Each function: decorate with @pytest.mark.skip(reason="stub — implement in 12-03-PLAN.md")

    Add a module-level docstring: "Test suite for neon_audit.py — DINT-01, DINT-02, DINT-03."
    Import: import pytest (only — no import of neon_audit yet; that comes in Wave 2).
  </action>
  <verify>
    <automated>
      cd A:/PTOW/1_APT_Central_Command && pytest tools/orchestrator/tests/test_neon_audit.py --collect-only -q 2>&amp;1 | findstr /C:"test_jobs_diff" /C:"test_exit_code_pass" /C:"test_artifact_schema"
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at tools/orchestrator/tests/test_neon_audit.py
    - Running "pytest tools/orchestrator/tests/test_neon_audit.py --collect-only -q" collects exactly 9 test items: test_jobs_diff, test_window_filter, test_time_records_neon_only_informational, test_job_comments_count_only, test_exit_code_pass, test_exit_code_fail, test_exit_code_connectivity, test_artifact_schema, test_terminal_output
    - Running "pytest tools/orchestrator/tests/test_neon_audit.py -v" shows all 9 tests as SKIPPED (not FAILED or ERRORED)
    - No import errors on collection
  </acceptance_criteria>
  <done>test_neon_audit.py collected by pytest with 9 skipped stubs; Wave 2 can implement them</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → script | GOOGLE_SERVICE_ACCOUNT_JSON is a file path; script reads it from disk — file must not be in git |
| env → script | DATABASE_URL_UNPOOLED contains Neon credentials — must never be logged or printed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | GOOGLE_SERVICE_ACCOUNT_JSON file | mitigate | File path only in env var; .json key file must be in .gitignore; never echo the file contents |
| T-12-02 | Information Disclosure | DATABASE_URL_UNPOOLED | mitigate | Error message only prints "not set" — never the DSN value; documented in neon_audit.py usage comment |
| T-12-SC | Tampering | pip install psycopg2-binary gspread | accept | Both packages rated [OK] by slopcheck 0.6.1; PyPI age ~12 yrs, very high downloads; no checkpoint needed |
</threat_model>

<verification>
After all three tasks complete (including Brandon's action):
- tools/orchestrator/requirements.txt exists with correct pinned versions
- docs/SHEETS_SCHEMA.md JobComments section has 6-column documented schema
- test_neon_audit.py collected by pytest with 9 SKIPPED stubs
- Brandon confirms: GOOGLE_SERVICE_ACCOUNT_JSON set, spreadsheet shared with service account at Viewer
</verification>

<success_criteria>
Wave 1 is complete when:
1. requirements.txt pinned: psycopg2-binary==2.9.12, gspread==6.2.1
2. SHEETS_SCHEMA.md JobComments section has documented column schema (6 cols, Timestamp at index 5)
3. test_neon_audit.py collects 9 skipped stub tests — no errors
4. Brandon has set GOOGLE_SERVICE_ACCOUNT_JSON and shared the spreadsheet
</success_criteria>

<output>
Create `.planning/phases/12-data-integrity-audit/12-01-SUMMARY.md` when done.
</output>
