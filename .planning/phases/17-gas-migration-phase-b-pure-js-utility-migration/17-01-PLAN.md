---
phase: 17-gas-migration-phase-b-pure-js-utility-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tech-pwa/src/app/api/field/auth/change-pin/route.ts, tech-pwa/src/app/api/gas/validate-token/route.ts, TechPWA.gs]
autonomous: true
requirements: ["17-cutover"]
must_haves:
  truths:
    - "TechPWA.gs validateToken fetches from Next.js instead of Sheets using secure internal key"
    - "TechPWA.gs no longer writes clock events to Sheets"
  artifacts:
    - path: "tech-pwa/src/app/api/field/auth/change-pin/route.ts"
      provides: "Change PIN endpoint"
    - path: "tech-pwa/src/app/api/gas/validate-token/route.ts"
      provides: "GAS internal validate-token endpoint"
  key_links:
    - from: "TechPWA.gs"
      to: "/api/gas/validate-token"
      via: "UrlFetchApp"
---

<objective>
Complete the Phase 17 TechPWA Cutover.

Purpose: Remove major Sheets read/write bottlenecks for token validation, clock events, and job status.
Output: Next.js API routes for change-pin and validate-token, updated TechPWA.gs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gas-migration-phase-b-pure-js-utility-migration/17-RESEARCH.md
</context>

<threat_model>
- **Threat**: The `/api/gas/validate-token` route could be exposed to the public.
  - **Mitigation**: Route requires a shared secret (`GAS_INTERNAL_SECRET`). GAS will send `X-GAS-Internal-Key: <secret>`. Next.js validates it against the `GAS_INTERNAL_SECRET` env var, returning 401 if missing/wrong.
- **Threat**: Stopping clock event writes in GAS breaks clock functionality.
  - **Mitigation**: We have verified the PWA uses the `/api/field/clock/*` Next.js endpoints correctly. Thus, old GAS routes can safely be stubbed (Option A).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Next.js Change PIN Route</name>
  <files>tech-pwa/src/app/api/field/auth/change-pin/route.ts</files>
  <read_first>tech-pwa/src/lib/fieldAuth.ts</read_first>
  <action>Create POST route for `/api/field/auth/change-pin`. Parse session token, verify old PIN, hash the new PIN using `crypto.createHash('sha256')`, and update the DB `pinHash` for the tech in the Neon `employees` table. Return success payload.</action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - DB update uses Neon Drizzle ORM
    - Correctly hashes PIN with SHA-256
  </acceptance_criteria>
  <done>Next.js can change PIN directly</done>
</task>

<task type="auto">
  <name>Task 2: Next.js Validate Token Route (Internal)</name>
  <files>tech-pwa/src/app/api/gas/validate-token/route.ts</files>
  <read_first>tech-pwa/src/lib/fieldAuth.ts</read_first>
  <action>Create a specialized internal POST endpoint for GAS. Accept `{"token": "..."}`, but BEFORE processing, check the `X-GAS-Internal-Key` header against `process.env.GAS_INTERNAL_SECRET`. Return 401 if missing/wrong. If valid, use `verifyFieldSession(token)` and return the tech object payload matching the GAS format (`badge`, `name`, `status`, etc.).</action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - Requires `X-GAS-Internal-Key` header
    - Endpoint returns a tech object matching GAS's expected format
  </acceptance_criteria>
  <done>GAS has a secure endpoint to call for token validation</done>
</task>

<task type="auto">
  <name>Task 3: Cutover TechPWA.gs</name>
  <files>TechPWA.gs</files>
  <read_first>TechPWA.gs</read_first>
  <action>
1. Add `GAS_INTERNAL_SECRET` retrieval: `const gasSecret = PropertiesService.getScriptProperties().getProperty('GAS_INTERNAL_SECRET');`
2. Update `handleChangePin` to return a `DEPRECATED` payload.
3. Update `validateToken` to call `/api/gas/validate-token` using `UrlFetchApp.fetch()`, passing `X-GAS-Internal-Key: gasSecret`. Remove the `Tech Roster` sheet loop entirely.
4. Stub clock events (`handleClockIn`, `handleClockOut`, `handleStartBreak`, `handleEndBreak`) to return `DEPRECATED` since PWA handles these natively now.
5. Stub `updateJobStatus` to return `DEPRECATED` (no more sheets writes).
  </action>
  <verify>Code is syntactically valid in GAS</verify>
  <acceptance_criteria>
    - `validateToken` uses `UrlFetchApp` with the `X-GAS-Internal-Key` header.
    - No `tmSheet.appendRow` in clock events.
  </acceptance_criteria>
  <done>TechPWA.gs no longer relies on Sheets for these actions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes for Next.js
- [ ] TechPWA.gs syntax is valid
- [ ] Next.js routes correctly respond to requests
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/17-gas-migration-phase-b-pure-js-utility-migration/17-01-SUMMARY.md`
</output>
