---
phase: 10-gas-migration-scope
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/GAS_MIGRATION_SCOPE.md
autonomous: true
requirements: [SC-1, SC-2]
must_haves:
  truths:
    - "A reader can find the migration status (keep/migrate/delete) for every GAS function across Code.js, SuggestTechs.js, TechPWA.gs, and DashboardAPI.gs"
    - "A reader can identify which migration phase (A through M) each migration belongs to, ordered lowest risk to highest"
    - "A reader can identify the blocked migrations (Gmail OAuth, TOM redesign) and their specific blockers"
    - "A reader can identify the open security gap (TechPWA.gs Anyone access) as a Phase K prerequisite"
  artifacts:
    - path: "docs/GAS_MIGRATION_SCOPE.md"
      provides: "Complete function-by-function GAS migration catalog with risk-ordered phases"
      min_lines: 250
      contains: "## Code.js"
  key_links:
    - from: "docs/GAS_MIGRATION_SCOPE.md"
      to: ".planning/phases/10-gas-migration-scope/10-RESEARCH.md"
      via: "every catalog row sourced from the research Full Function Inventory"
      pattern: "Migration Status"
---

<objective>
Author the complete `docs/GAS_MIGRATION_SCOPE.md` — a function-by-function catalog of every Google Apps Script function across all three GAS projects (Code.js, SuggestTechs.js, TechPWA.gs, DashboardAPI.gs), each with a migration status (keep / migrate / delete), a risk level, and a target tier. The document organizes the migrations into risk-ordered phases (A through M) and records blocked migrations with their blockers.

This phase is DOCUMENTATION-ONLY. No source code changes. The deliverable is one Markdown file that becomes the execution roadmap for the CC3.0 GAS migration phases.

Purpose: Phase 13 (Write Path Flip) already references `docs/GAS_MIGRATION_SCOPE.md` as a canonical ref. A complete, authoritative catalog is required before any CC3.0 GAS migration phase can be planned or executed.
Output: `docs/GAS_MIGRATION_SCOPE.md` (overwrites the partial 2026-05-31 version with the complete 2026-06-07 inventory).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-gas-migration-scope/10-RESEARCH.md
@docs/GAS_MIGRATION_SCOPE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write document header, overview, constraints, and Architectural Responsibility Map</name>
  <files>docs/GAS_MIGRATION_SCOPE.md</files>
  <read_first>
    - .planning/phases/10-gas-migration-scope/10-RESEARCH.md (Summary, Project Constraints, Architectural Responsibility Map sections — the authoritative source)
    - docs/GAS_MIGRATION_SCOPE.md (existing 2026-05-31 partial version being replaced — read so nothing useful is lost)
    - CLAUDE.md (clasp project structure, deployment workflow, NEVER RUN list)
  </read_first>
  <action>
    Overwrite docs/GAS_MIGRATION_SCOPE.md. Begin with an H1 title "GAS Migration Scope" and a generated-date line noting this supersedes the 2026-05-31 version and reflects the 2026-06-07 research inventory. Add a "## Migration Overview" section summarizing: three GAS projects (Code.js root manual-deploy, TechPWA.gs CI-deploy, dashboard-api/DashboardAPI.gs CI-deploy) plus the SuggestTechs.js support module; the fact that WRITE_PATH_NEON_ONLY=true is set and the Dispatch Queue Sheets tab is a read-only archive as of 2026-06-01. Add a "## Project Constraints" section transcribing the constraints from RESEARCH.md (dev write guard, NEVER RUN functions catchUpMissedEmails/resetBackfill/setupBackfillTrigger/archiveOldJobsConfirmed/mineScheduleSheet, three separate clasp projects, manual deploy only for Code.js, WRITE_PATH_NEON_ONLY behavior). Add a "## Architectural Responsibility Map" section reproducing the RESEARCH.md table with columns: Capability | Primary Tier | Secondary Tier | Rationale. Do not abbreviate the rows. Use absolute reasoning per RESEARCH.md; do not invent new capabilities.
  </action>
  <verify>
    <automated>grep -c "^## " docs/GAS_MIGRATION_SCOPE.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/GAS_MIGRATION_SCOPE.md contains the heading "## Migration Overview"
    - docs/GAS_MIGRATION_SCOPE.md contains the heading "## Project Constraints"
    - docs/GAS_MIGRATION_SCOPE.md contains the heading "## Architectural Responsibility Map"
    - File contains the string "WRITE_PATH_NEON_ONLY"
    - File contains the string "catchUpMissedEmails" (NEVER RUN list transcribed)
    - File contains all four project file names: "Code.js", "SuggestTechs.js", "TechPWA.gs", "DashboardAPI.gs"
  </acceptance_criteria>
  <done>Header, overview, constraints, and responsibility map sections exist and reproduce the RESEARCH.md content without omission.</done>
</task>

<task type="auto">
  <name>Task 2: Write the full function catalog tables for all four GAS files</name>
  <files>docs/GAS_MIGRATION_SCOPE.md</files>
  <read_first>
    - .planning/phases/10-gas-migration-scope/10-RESEARCH.md (Full Function Inventory — File 1 Code.js, File 2 SuggestTechs.js, File 3 TechPWA.gs, File 4 DashboardAPI.gs, including the "Already migrated to Next.js" DELETE table and the "Commented-out / dead functions" lists)
  </read_first>
  <action>
    Append four catalog sections to docs/GAS_MIGRATION_SCOPE.md, one per file: "## Code.js Functions", "## SuggestTechs.js Functions", "## TechPWA.gs Functions", "## DashboardAPI.gs Functions". Each section is a Markdown table with these exact columns: Function | Responsibility | Migration Status | Risk | Notes. Transcribe EVERY row from the corresponding RESEARCH.md File inventory table — do not summarize, sample, or drop rows. For Code.js also append the "Commented-out / dead functions" entries (backfillDispatchMsgIds, catchUpMissedEmails, draftTenantContact, sendUrgentAlert, sendTurnoverFlag) as DELETE rows. For DashboardAPI.gs include both the "Already migrated to Next.js (DELETE)" function rows (getDispatchDataDA, getJobByIdDA, getTodaySchedule, getWeekSchedule, getLiveFieldStatus, getComplianceStatus, getTechListDA, getJobHistory, getNotificationsDA, updateJobDA, archiveJobDA) and the "Still active" function rows. Write each Migration Status cell in lowercase keyword form usable by a grep gate: use "keep", "migrate", or "delete" as the leading token of the status cell (the parenthetical qualifiers from RESEARCH.md such as "(long-term: migrate)" or "(partial complete)" may follow, but the leading word must be one of keep/migrate/delete). Preserve the Risk values (LOW/MEDIUM/HIGH/VERY HIGH) and the Notes verbatim from RESEARCH.md.
  </action>
  <verify>
    <automated>grep -E -ci '\| (keep|migrate|delete)' docs/GAS_MIGRATION_SCOPE.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains all four headings: "## Code.js Functions", "## SuggestTechs.js Functions", "## TechPWA.gs Functions", "## DashboardAPI.gs Functions"
    - grep -E -ci '\| (keep|migrate|delete)' docs/GAS_MIGRATION_SCOPE.md returns a count >= 120 (every catalog row carries a keep/migrate/delete status token)
    - File contains the column header string "Migration Status"
    - File contains "checkNewLeadEmails" (Code.js core function present)
    - File contains "handleLogin" and "validateToken" (TechPWA.gs auth functions present)
    - File contains "getGmailThreadDA" (DashboardAPI.gs Gmail action present)
    - File contains "suggestTechsForJob" (SuggestTechs.js entry point present)
  </acceptance_criteria>
  <done>All four function catalog tables exist, every RESEARCH.md inventory row is transcribed, and at least 120 rows carry a keep/migrate/delete status token.</done>
</task>

<task type="auto">
  <name>Task 3: Write risk-ordered migration phases, blocked migrations, and open security gap</name>
  <files>docs/GAS_MIGRATION_SCOPE.md</files>
  <read_first>
    - .planning/phases/10-gas-migration-scope/10-RESEARCH.md (Migration Phases section A through M, Common Pitfalls, Security Domain / Open Security Gap, Assumptions Log, Open Questions)
  </read_first>
  <action>
    Append a "## Migration Phases" section to docs/GAS_MIGRATION_SCOPE.md. Reproduce each phase from RESEARCH.md as a subsection with an H3 heading naming the phase letter and risk level: "### Phase A: Dead Code Cleanup (Zero Risk)" through "### Phase M: Time Off Manager (BLOCKED on TOM Redesign)". For each phase include its files-touched / examples / risk text and, where RESEARCH.md provides them, the function candidates. Order the phases ascending by risk exactly as in RESEARCH.md (A=zero risk first, L=very high and M=blocked last). Then append a "## Blocked Migrations" section listing the two hard blockers with their specific cause: (1) Phase J Gmail actions blocked on Gmail API OAuth not being available in the Node context (workorder@ credentials only exist in GAS) — cite the Pitfall 2 remediation options (service account with domain-wide delegation, or OAuth2 refresh token); (2) Phase M Time Off Manager blocked on AppSheet TOM reading Sheets directly until TOM is redesigned or replaced. Then append a "## Open Security Gap" section noting TechPWA.gs is "Anyone" access with no Cloudflare Worker in front, and that adding a Cloudflare Worker is a prerequisite for Phase K. Then append a "## Migration Prerequisites and Pitfalls" section transcribing the five Common Pitfalls from RESEARCH.md, and a "## Assumptions and Open Questions" section transcribing the Assumptions Log (A1-A4) and Open Questions (1-3). End the document with a one-line acceptance note that this document is the CC3.0 GAS migration execution roadmap pending developer sign-off.
  </action>
  <verify>
    <automated>grep -E -c '^### Phase [A-M]:' docs/GAS_MIGRATION_SCOPE.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains the heading "## Migration Phases"
    - grep -E -c '^### Phase [A-M]:' docs/GAS_MIGRATION_SCOPE.md returns >= 13 (phases A through M each have a subsection)
    - File contains "### Phase A: Dead Code Cleanup" as the first phase subsection
    - File contains "### Phase M: Time Off Manager" and the string "BLOCKED"
    - File contains the heading "## Blocked Migrations"
    - File contains the string "domain-wide delegation" (Gmail OAuth remediation cited)
    - File contains the heading "## Open Security Gap" and the string "Cloudflare Worker"
    - File contains the heading "## Assumptions and Open Questions" and the string "WRITE_PATH_NEON_ONLY" (assumption A1)
  </acceptance_criteria>
  <done>All 13 risk-ordered phase subsections, the blocked-migrations section, the open-security-gap section, and the assumptions/open-questions section exist and reproduce the RESEARCH.md content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only phase — no executable code paths, no inputs crossing a trust boundary, no new endpoints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | docs/GAS_MIGRATION_SCOPE.md content | accept | Document is internal architecture notes. It must NOT contain secrets — no API keys, deployment IDs are already public-internal in CLAUDE.md, no credential values. Author copies function names and dispositions only, never secret values (per project "Never Share Secrets" rule). |

ASVS L1: No authentication, session, access-control, input-validation, or cryptography surface is introduced by this phase (RESEARCH.md Security Domain confirms all categories N/A). The one substantive security item — TechPWA.gs "Anyone" access — is DOCUMENTED in this phase as a Phase K prerequisite, not fixed here.
</threat_model>

<verification>
After all three tasks, the document must satisfy every Success Criterion:
- SC-1 (function catalog with keep/migrate/delete): `grep -E -ci '\| (keep|migrate|delete)' docs/GAS_MIGRATION_SCOPE.md` >= 120
- SC-2 (risk-ordered phases): `grep -E -c '^### Phase [A-M]:' docs/GAS_MIGRATION_SCOPE.md` >= 13
- Document references all four GAS files: `grep -c "Code.js\|SuggestTechs.js\|TechPWA.gs\|DashboardAPI.gs" docs/GAS_MIGRATION_SCOPE.md` > 0
</verification>

<success_criteria>
- docs/GAS_MIGRATION_SCOPE.md exists with >= 250 lines
- Every GAS function from RESEARCH.md Full Function Inventory appears as a catalog row with a keep/migrate/delete status (SC-1)
- 13 risk-ordered migration phase subsections (A-M) exist, lowest risk first (SC-2)
- Blocked migrations (Gmail OAuth, TOM) and the TechPWA.gs open security gap are documented
- No secret values appear anywhere in the document
</success_criteria>

<artifacts_this_phase_produces>
New file: `docs/GAS_MIGRATION_SCOPE.md` (overwrites the partial 2026-05-31 version)

Document sections this phase creates:
- `## Migration Overview`
- `## Project Constraints`
- `## Architectural Responsibility Map`
- `## Code.js Functions` (catalog table: Function | Responsibility | Migration Status | Risk | Notes)
- `## SuggestTechs.js Functions` (catalog table)
- `## TechPWA.gs Functions` (catalog table)
- `## DashboardAPI.gs Functions` (catalog table, including the already-migrated DELETE rows)
- `## Migration Phases` with subsections `### Phase A` through `### Phase M` (risk-ordered, lowest first)
- `## Blocked Migrations` (Gmail OAuth, AppSheet TOM)
- `## Open Security Gap` (TechPWA.gs "Anyone" access — Phase K prerequisite)
- `## Migration Prerequisites and Pitfalls`
- `## Assumptions and Open Questions`

No source code symbols are created — this phase produces documentation only.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/10-gas-migration-scope/10-01-SUMMARY.md` when done.
</output>
