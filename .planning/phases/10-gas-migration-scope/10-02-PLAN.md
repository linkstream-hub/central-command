---
phase: 10-gas-migration-scope
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - docs/GAS_MIGRATION_SCOPE.md
autonomous: false
requirements: [SC-3]
must_haves:
  truths:
    - "Every function defined in the live GAS source files appears in the catalog (no silent omissions)"
    - "A developer has reviewed the document and accepted it as the CC3.0 GAS migration execution roadmap"
  artifacts:
    - path: "docs/GAS_MIGRATION_SCOPE.md"
      provides: "Verified, accepted GAS migration roadmap"
      contains: "## Migration Phases"
  key_links:
    - from: "docs/GAS_MIGRATION_SCOPE.md"
      to: "Code.js, TechPWA.gs, dashboard-api/DashboardAPI.gs, SuggestTechs.js"
      via: "every live function() definition has a catalog row"
      pattern: "function "
---

<objective>
Verify that `docs/GAS_MIGRATION_SCOPE.md` (authored in plan 10-01) is complete — every function defined in the live GAS source files has a catalog row — then present the document to the developer for the explicit acceptance gate (Success Criterion 3).

Purpose: SC-3 is a human go/no-go. The document only becomes the CC3.0 GAS migration execution roadmap once a developer reads it and accepts it. This plan first runs an automated completeness cross-check against the actual source files so the reviewer is not approving a document with silent gaps, then pauses for sign-off.
Output: An updated acceptance line in docs/GAS_MIGRATION_SCOPE.md and developer sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-gas-migration-scope/10-RESEARCH.md
@docs/GAS_MIGRATION_SCOPE.md
@.planning/phases/10-gas-migration-scope/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-check catalog completeness against live GAS source files</name>
  <files>docs/GAS_MIGRATION_SCOPE.md</files>
  <read_first>
    - docs/GAS_MIGRATION_SCOPE.md (the catalog authored in 10-01)
    - .planning/phases/10-gas-migration-scope/10-RESEARCH.md (Full Function Inventory + Sources section noting Code.js v96 2103 lines, TechPWA.gs v90 1835 lines, DashboardAPI.gs 4225 lines)
  </read_first>
  <action>
    Extract the list of top-level function names defined in each live GAS file and confirm each appears in docs/GAS_MIGRATION_SCOPE.md. Run, from the repo root C:/PTOW/1_APT_Central_Command, a grep for function definitions in each source file: search Code.js, SuggestTechs.js, TechPWA.gs, and dashboard-api/DashboardAPI.gs for lines matching the pattern "function NAME(" to produce the authoritative defined-function set. For each defined function name, confirm the same name appears in docs/GAS_MIGRATION_SCOPE.md. Produce a gap list of any function defined in source but absent from the catalog. If the gap list is non-empty, add the missing functions as catalog rows in the appropriate "## <File> Functions" table, assigning a keep/migrate/delete status by analogy to similar functions already cataloged and citing RESEARCH.md reasoning; if a missing function cannot be confidently classified, add it with status "migrate" and a Note "uncatalogued in research — confirm disposition during CC3.0 planning". Do NOT remove or reclassify existing rows. The DashboardAPI.gs supporting helpers beyond research line 2530 (noted in RESEARCH.md Sources as not individually cataloged) are the most likely gap source — pay particular attention to that file.
  </action>
  <verify>
    <automated>grep -E -ci '\| (keep|migrate|delete)' docs/GAS_MIGRATION_SCOPE.md</automated>
  </verify>
  <acceptance_criteria>
    - For each of Code.js, SuggestTechs.js, TechPWA.gs, dashboard-api/DashboardAPI.gs, every name matched by `grep -oE 'function [A-Za-z0-9_]+' <file>` (stripped to the bare function name) is present as a substring somewhere in docs/GAS_MIGRATION_SCOPE.md
    - The gap list produced during this task is recorded in the plan SUMMARY (empty list is an acceptable result and must be stated explicitly)
    - grep -E -ci '\| (keep|migrate|delete)' docs/GAS_MIGRATION_SCOPE.md returns a count >= the count after 10-01 (rows only added, never removed)
  </acceptance_criteria>
  <done>Every top-level function defined in the four live GAS source files has a corresponding catalog row in docs/GAS_MIGRATION_SCOPE.md; any gaps found were added; the gap list is recorded in the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Developer acceptance gate (SC-3)</name>
  <files>docs/GAS_MIGRATION_SCOPE.md</files>
  <action>
    Pause for the developer acceptance gate. Present docs/GAS_MIGRATION_SCOPE.md for review using the verification steps below. Do not auto-approve — this is a blocking human gate (SC-3 is a human go/no-go) and is not subject to auto_advance. On the developer typing "accepted", update the final acceptance line in docs/GAS_MIGRATION_SCOPE.md to read that the document is accepted as the CC3.0 GAS migration execution roadmap with the accepted date filled in. If the developer lists required changes instead, apply them and re-present.
  </action>
  <what-built>
    docs/GAS_MIGRATION_SCOPE.md — a complete function-by-function GAS migration catalog (all functions across Code.js, SuggestTechs.js, TechPWA.gs, DashboardAPI.gs each tagged keep/migrate/delete with a risk level), 13 risk-ordered migration phases (A through M), documented blockers (Gmail OAuth, AppSheet TOM), and the TechPWA.gs open security gap. Task 1 cross-checked the catalog against the live source files and closed any gaps.
  </what-built>
  <how-to-verify>
    1. Open docs/GAS_MIGRATION_SCOPE.md.
    2. Confirm the function catalog covers all three GAS projects plus SuggestTechs.js and that each function has a keep/migrate/delete status and a risk level.
    3. Confirm the migration phases run lowest risk (Phase A: Dead Code Cleanup) to highest (Phase L email polling / Phase M blocked TOM).
    4. Confirm the blocked migrations and the TechPWA.gs "Anyone" access security gap are documented as prerequisites.
    5. Spot-check 3-5 functions against RESEARCH.md to confirm dispositions match.
    6. Decide: accept this document as the CC3.0 GAS migration execution roadmap, or list required changes.
    7. On acceptance, the executor updates the final acceptance line in docs/GAS_MIGRATION_SCOPE.md to record the accepted date.
  </how-to-verify>
  <verify>
    <human-check>Developer reviews docs/GAS_MIGRATION_SCOPE.md per the steps above and types "accepted" or lists required changes.</human-check>
  </verify>
  <resume-signal>Type "accepted" to lock the document as the CC3.0 roadmap, or describe required changes.</resume-signal>
  <done>Developer typed "accepted"; the acceptance line in docs/GAS_MIGRATION_SCOPE.md records the accepted date.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only phase — completeness cross-check is read-only grep over source files; no executable code path or input boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Information Disclosure | docs/GAS_MIGRATION_SCOPE.md | accept | Cross-check reads function names only from source; no secret values are copied into the catalog. Reviewer confirms no credentials present during the acceptance gate. |

ASVS L1: No new authentication, session, access-control, input-validation, or cryptography surface. Verification is grep + human review only.
</threat_model>

<verification>
- SC-3 acceptance gate satisfied: developer types "accepted" and the acceptance line in docs/GAS_MIGRATION_SCOPE.md records the accepted date.
- Completeness: every `function NAME(` defined in the four live GAS files has a matching catalog row.
</verification>

<success_criteria>
- Catalog completeness cross-check passed (or gaps closed) against Code.js, SuggestTechs.js, TechPWA.gs, DashboardAPI.gs
- Developer accepted the document as the CC3.0 GAS migration execution roadmap (SC-3)
- Acceptance date recorded in docs/GAS_MIGRATION_SCOPE.md
</success_criteria>

<artifacts_this_phase_produces>
Modifies: `docs/GAS_MIGRATION_SCOPE.md` (adds any missing catalog rows found by the completeness cross-check; records the developer acceptance date).

No new files or source code symbols — verification and acceptance only.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/10-gas-migration-scope/10-02-SUMMARY.md` when done. Include the completeness gap list (even if empty).
</output>
