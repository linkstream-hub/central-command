---
phase: 03-gap-remediation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tech-pwa/src/components/dashboard/JobDetailModal.tsx
  - tech-pwa/src/components/dashboard/AppSidebar.tsx
  - tech-pwa/src/components/dashboard/JobQueueTable.tsx
  - tech-pwa/src/components/dashboard/DashboardLayout.tsx
autonomous: true
requirements:
  - GAP-01
  - GAP-02

must_haves:
  truths:
    - "WO card (JobDetailModal) shows tenantName, tenantPhone, and tenantEmail when any field is non-null"
    - "Searching 'APT-01397' in the dispatch queue returns that WO as the first result"
    - "The header Search button in DashboardLayout is removed from the /live page; Ctrl+K keyboard shortcut still works"
    - "Sidebar shows 'Needs Review' and 'Ready to Schedule' labels (not 'Operations' / 'Schedule Queue')"
    - "Comms tab message bubbles are visually distinct — inbound grey/neutral, outbound accent color"
  artifacts:
    - path: "tech-pwa/src/components/dashboard/JobDetailModal.tsx"
      provides: "Tenant contact section in right panel + expanded left-panel contact card + improved message bubble contrast"
      contains: "tenantName\|tenantPhone\|tenantEmail\|Tenant Contact"
    - path: "tech-pwa/src/components/dashboard/AppSidebar.tsx"
      provides: "Updated NAV_ITEMS labels"
      contains: "Needs Review\|Ready to Schedule"
    - path: "tech-pwa/src/components/dashboard/JobQueueTable.tsx"
      provides: "WO# jobId search field + match-score ranking"
      contains: "jobId\|matchScore"
    - path: "tech-pwa/src/components/dashboard/DashboardLayout.tsx"
      provides: "Header Search button removed"
  key_links:
    - from: "JobQueueTable.tsx filteredAndSortedJobs"
      to: "j.jobId"
      via: "filter + matchScore sort"
      pattern: "jobId.*toLowerCase\|matchScore"
    - from: "JobDetailModal.tsx right panel"
      to: "activeJob.tenantName / tenantPhone / tenantEmail"
      via: "conditional render section"
      pattern: "tenantName\|tenantPhone\|tenantEmail"
---

<objective>
Fix four tech-pwa UX gaps surfaced in Phase 2: (1) WO card missing tenant contact display, (2) search missing WO# field + no ranking, (3) sidebar labels not matching workflow stages, (4) Comms tab message bubble contrast. Also remove the header Search button from DashboardLayout per user decision.

Purpose: Dispatchers cannot see tenant contact info in the WO card, cannot search by WO#, are confused by sidebar labels that don't reflect the workflow, and cannot distinguish inbound vs outbound messages in the Comms tab.

Output: Four modified Next.js files — deployed automatically via Vercel on push to main (via PR).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@A:/PTOW/1_APT_Central_Command/.planning/ROADMAP.md
@A:/PTOW/1_APT_Central_Command/.planning/phases/03-gap-remediation/03-CONTEXT.md
@A:/PTOW/1_APT_Central_Command/.planning/phases/03-gap-remediation/03-RESEARCH.md

<interfaces>
<!-- Extracted from live source files — verified by codebase read -->

From AppSidebar.tsx lines 31–42 (current NAV_ITEMS, target values bolded):
  { id: 'live',     label: 'Operations',    href: '/live',     module: 'dispatch' }  → label: 'Needs Review'
  { id: 'schedule', label: 'Schedule Queue', href: '/schedule', module: 'dispatch' }  → label: 'Ready to Schedule'
  { id: 'weekly-schedule', label: 'Work Schedule', ... }  — NO CHANGE (already correct)
  Only change the `label` string. Never change `id`, `href`, or `module`.

From JobQueueTable.tsx lines 230–238 (current search filter — missing jobId):
  if (searchQuery?.trim()) {
    const q = searchQuery.toLowerCase();
    list = list.filter(j =>
      String(j.address || '').toLowerCase().includes(q) ||
      String(j.description || '').toLowerCase().includes(q) ||
      String(j.rmName || '').toLowerCase().includes(q) ||
      String(j.assignedTech || '').toLowerCase().includes(q)
    );
  }
  jobId is NOT present — add it.

From JobDetailModal.tsx — right panel section pattern to replicate:
  <section className="space-y-3">
    <h4 className="text-[9px] font-black text-[var(--text-muted)] uppercase tracking-[0.3em]">
      Tenant Contact
    </h4>
    <div className="bg-[var(--bg-surface)] rounded-2xl border border-white/10 backdrop-blur-sm p-5 space-y-3">
      {/* render tenantName, tenantPhone, tenantEmail */}
    </div>
  </section>

From JobDetailModal.tsx lines 946–947 (current message bubble classes — low contrast):
  className={`p-5 rounded-2xl border ${msg.isOutbound
    ? "bg-[var(--accent)]/5 border-[var(--accent)]/10 ml-12"
    : "bg-white/[0.03] border-white/5 mr-12"}`}
  Fix: increase both opacity values — inbound to bg-white/[0.08] or bg-zinc-800/60; outbound to bg-[var(--accent)]/20.

From DashboardLayout.tsx lines 99–118 (the header Search button div to REMOVE entirely):
  <div
    onClick={() => { ... dispatch synthetic Ctrl+K KeyboardEvent ... }}
    className="flex items-center space-x-2 px-3 py-1.5 rounded-full bg-white/5 ..."
  >
    <Search size={14} />
    <span>Search</span>
    <span>Ctrl K</span>
  </div>
  Remove this entire div (lines 99–118). The CommandPalette keyboard listener in CommandPalette.tsx listens for Ctrl+K globally — it will still work after removing this button.
</interfaces>
</context>

<tasks>

<task type="auto" id="task-0">
  <name>Task 0: Branch verification (CLAUDE.md BRANCH RULE — NON-NEGOTIABLE)</name>
  <files></files>
  <read_first>
    CLAUDE.md — BRANCH SETUP section.
  </read_first>
  <action>
    Run these three commands and paste each output before proceeding:

    STEP 1:
      git branch --show-current
    Output must be: feat/phase3-gap-remediation
    Mismatch = STOP and report to Claude Code. Do NOT commit to any other branch.

    STEP 2:
      git ls-remote --heads origin feat/phase3-gap-remediation
    Output must be non-empty (branch exists on remote).
    If empty: run `git checkout -b feat/phase3-gap-remediation && git push -u origin feat/phase3-gap-remediation` first.

    STEP 3:
      git log main..HEAD --oneline
    If output is empty on a pre-existing branch, rebase from main:
      git fetch origin main && git rebase origin/main
    Note: If Plan 03-01 already committed Code.js fixes to this branch, this log will be non-empty — that is expected and correct.
  </action>
  <verify>
    <automated>
      git branch --show-current
    </automated>
    Expected: feat/phase3-gap-remediation
  </verify>
  <acceptance_criteria>
    - `git branch --show-current` returns exactly `feat/phase3-gap-remediation`
    - `git ls-remote --heads origin feat/phase3-gap-remediation` returns non-empty output
    - No commits made to any branch other than feat/phase3-gap-remediation
  </acceptance_criteria>
</task>

<task type="auto" id="task-1">
  <name>Task 1: Fix AppSidebar labels + JobQueueTable WO# search + ranking</name>
  <files>
    tech-pwa/src/components/dashboard/AppSidebar.tsx,
    tech-pwa/src/components/dashboard/JobQueueTable.tsx
  </files>
  <read_first>
    tech-pwa/src/components/dashboard/AppSidebar.tsx lines 31–42 (NAV_ITEMS array — current labels).
    tech-pwa/src/components/dashboard/JobQueueTable.tsx lines 155–265 (filteredAndSortedJobs useMemo, search filter block at lines 230–238, sort block at lines 244–259).
  </read_first>
  <action>
    CHANGE 1 — AppSidebar.tsx NAV_ITEMS (lines 31–42):
    Update two label strings only. Do NOT change id, href, module, icon, or status fields on any item:
      - `label: 'Operations'` → `label: 'Needs Review'`
      - `label: 'Schedule Queue'` → `label: 'Ready to Schedule'`
    The `weekly-schedule` item with `label: 'Work Schedule'` is already correct — leave it unchanged.

    CHANGE 2 — JobQueueTable.tsx search filter (lines 230–238):
    Add `String(j.jobId || '').toLowerCase().includes(q) ||` as the FIRST condition in the filter (before address). jobId is the most specific field — placing it first is correct even though filter is boolean; the sort in CHANGE 3 will handle ranking.

    Updated filter block:
      if (searchQuery?.trim()) {
        const q = searchQuery.toLowerCase();
        list = list.filter(j =>
          String(j.jobId || '').toLowerCase().includes(q) ||
          String(j.address || '').toLowerCase().includes(q) ||
          String(j.description || '').toLowerCase().includes(q) ||
          String(j.rmName || '').toLowerCase().includes(q) ||
          String(j.assignedTech || '').toLowerCase().includes(q)
        );
      }

    CHANGE 3 — JobQueueTable.tsx search ranking: After the filter block and before the primary sort (the `return list.sort(...)` at line 244), add a secondary match-score sort when a search query is active.

    Add a `matchScore` helper function inside the `useMemo` callback (before the sort), or at the top level of the file as a named function:
      function matchScore(job: Job, q: string): number {
        const id = String(job.jobId || '').toLowerCase();
        if (id === q) return 3;           // exact WO# match
        if (id.startsWith(q)) return 2;  // prefix match
        if (id.includes(q)) return 1;    // substring match
        return 0;                         // no jobId match (matched another field)
      }

    Then wrap the existing `list.sort(...)` with a match-score pre-sort when searchQuery is active:
      if (searchQuery?.trim()) {
        const q = searchQuery.toLowerCase();
        list = list.sort((a, b) => matchScore(b, q) - matchScore(a, q));
      }
      // existing primary sort follows (unchanged)
      return list.sort((a, b) => { ... existing sort logic ... });

    The Job type is already imported — use the existing type. Do not import anything new.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project A:/PTOW/1_APT_Central_Command/tech-pwa/tsconfig.json</automated>
    Expected: zero TypeScript errors.

    Also run:
      grep -n "Needs Review\|Ready to Schedule" A:/PTOW/1_APT_Central_Command/tech-pwa/src/components/dashboard/AppSidebar.tsx
      grep -n "jobId" A:/PTOW/1_APT_Central_Command/tech-pwa/src/components/dashboard/JobQueueTable.tsx
      grep -n "matchScore" A:/PTOW/1_APT_Central_Command/tech-pwa/src/components/dashboard/JobQueueTable.tsx
  </verify>
  <acceptance_criteria>
    - AppSidebar.tsx: `grep -n "Needs Review"` returns exactly one match on the `live` NAV_ITEM label
    - AppSidebar.tsx: `grep -n "Ready to Schedule"` returns exactly one match on the `schedule` NAV_ITEM label
    - AppSidebar.tsx: 'Operations' and 'Schedule Queue' strings no longer appear in NAV_ITEMS
    - AppSidebar.tsx: `href` values `/live` and `/schedule` are unchanged
    - JobQueueTable.tsx: `grep -n "jobId" | grep -i "includes\|filter"` returns at least one match (jobId added to filter)
    - JobQueueTable.tsx: `grep -n "matchScore"` returns at least 3 matches (definition + two usage sites)
    - `npx tsc --noEmit` exits with code 0 (zero errors)
  </acceptance_criteria>
</task>

<task type="auto" id="task-2">
  <name>Task 2: Fix JobDetailModal — tenant contact section + message bubble contrast + DashboardLayout search button removal</name>
  <files>
    tech-pwa/src/components/dashboard/JobDetailModal.tsx,
    tech-pwa/src/components/dashboard/DashboardLayout.tsx
  </files>
  <read_first>
    tech-pwa/src/components/dashboard/JobDetailModal.tsx — read two ranges:
      Lines 697–750 (left panel contact card — the commStakeholder card that shows name + one contact line).
      Lines 940–985 (Comms tab message bubble map block — the className with bg-[var(--accent)]/5).
    Also read lines 400–460 (right panel section structure to understand where to insert the new tenant section).
    tech-pwa/src/components/dashboard/DashboardLayout.tsx lines 95–125 (the header div containing the Search button at lines 99–118).
  </read_first>
  <action>
    CHANGE 1 — JobDetailModal.tsx: Add tenant contact section to right panel.

    Find the right panel JSX (the panel that contains Job Details, Job Status sections). After the existing Job Details section (look for the section with job address, unit, property type fields), add a new section that renders when any tenant contact field is non-null:

      {(activeJob.tenantName || activeJob.tenantPhone || activeJob.tenantEmail) && (
        <section className="space-y-3">
          <h4 className="text-[9px] font-black text-[var(--text-muted)] uppercase tracking-[0.3em]">
            Tenant Contact
          </h4>
          <div className="bg-[var(--bg-surface)] rounded-2xl border border-white/10 backdrop-blur-sm p-5 space-y-3">
            {activeJob.tenantName && (
              <div>
                <p className="text-[10px] text-[var(--text-muted)] uppercase tracking-wider mb-0.5">Name</p>
                <p className="text-sm text-[var(--text-primary)]">{activeJob.tenantName}</p>
              </div>
            )}
            {activeJob.tenantPhone && (
              <div>
                <p className="text-[10px] text-[var(--text-muted)] uppercase tracking-wider mb-0.5">Phone</p>
                <p className="text-sm text-[var(--text-primary)]">{activeJob.tenantPhone}</p>
              </div>
            )}
            {activeJob.tenantEmail && (
              <div>
                <p className="text-[10px] text-[var(--text-muted)] uppercase tracking-wider mb-0.5">Email</p>
                <p className="text-sm text-[var(--text-primary)]">{activeJob.tenantEmail}</p>
              </div>
            )}
          </div>
        </section>
      )}

    CHANGE 2 — JobDetailModal.tsx left panel contact card (lines 697–739): Expand the secondary text to show BOTH tenantPhone and tenantEmail when both exist. Currently it shows `activeJob.tenantEmail || activeJob.tenantPhone || "No Contact Info"` (one value). Replace with a combined display:

      <p className="text-xs text-[var(--text-muted)]">
        {[activeJob.tenantEmail, activeJob.tenantPhone].filter(Boolean).join(' · ') || 'No Contact Info'}
      </p>

    This shows both values separated by a bullet when both exist, or whichever is non-null, or the fallback.

    CHANGE 3 — JobDetailModal.tsx message bubbles (lines ~946–947): Increase background opacity for visual contrast.
    Current className string:
      `p-5 rounded-2xl border ${msg.isOutbound ? "bg-[var(--accent)]/5 border-[var(--accent)]/10 ml-12" : "bg-white/[0.03] border-white/5 mr-12"}`

    Replace with:
      `p-5 rounded-2xl border ${msg.isOutbound ? "bg-[var(--accent)]/20 border-[var(--accent)]/30 ml-12" : "bg-white/[0.08] border-white/10 mr-12"}`

    This increases inbound from 3% to 8% white opacity, and outbound from 5% to 20% accent opacity — visually distinct on the #0A0A0A dark background.

    CHANGE 4 — DashboardLayout.tsx: Remove the header Search button div (lines 99–118 per user decision — this is the div that dispatches a synthetic Ctrl+K event, containing Search icon + "Search" label + "Ctrl K" hint).

    Remove the entire div block from line 99 (`<div onClick={() => {`) through line 118 (`</div>`). The CommandPalette Ctrl+K keyboard listener lives in CommandPalette.tsx and is not affected — the global shortcut continues to work.

    Verify the Search import from lucide-react is still used elsewhere in DashboardLayout.tsx before removing it. If Search is only used in the deleted button, remove it from the import line too.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project A:/PTOW/1_APT_Central_Command/tech-pwa/tsconfig.json</automated>
    Expected: zero TypeScript errors.

    Also run:
      grep -n "Tenant Contact" A:/PTOW/1_APT_Central_Command/tech-pwa/src/components/dashboard/JobDetailModal.tsx
      grep -n "accent.*\/20\|white.*0\.08" A:/PTOW/1_APT_Central_Command/tech-pwa/src/components/dashboard/JobDetailModal.tsx
      grep -n "Search\|CommandPalette\|dispatch.*keydown" A:/PTOW/1_APT_Central_Command/tech-pwa/src/components/dashboard/DashboardLayout.tsx
  </verify>
  <acceptance_criteria>
    - JobDetailModal.tsx: `grep -n "Tenant Contact"` returns exactly one match (section heading)
    - JobDetailModal.tsx: `grep -n "tenantPhone"` includes a match inside the new right-panel section
    - JobDetailModal.tsx: `grep -n "accent.*\/20"` or `grep -n "\/20"` returns match in the message bubble className (outbound opacity increased)
    - JobDetailModal.tsx: `grep -n "0\.08"` returns match in message bubble className (inbound opacity increased from 0.03 to 0.08)
    - DashboardLayout.tsx: `grep -n "dispatch.*keydown\|synthetic\|Ctrl.*K.*hint"` returns zero matches (button removed)
    - DashboardLayout.tsx: CommandPalette component reference still present (not removed — only the button div was removed)
    - `npx tsc --noEmit` exits with code 0 (zero errors)
  </acceptance_criteria>
</task>

<task type="auto" id="task-3">
  <name>Task 3: tsc gate + diff artifact + push + post to Claude Code</name>
  <files>
    tech-pwa/src/components/dashboard/JobDetailModal.tsx,
    tech-pwa/src/components/dashboard/AppSidebar.tsx,
    tech-pwa/src/components/dashboard/JobQueueTable.tsx,
    tech-pwa/src/components/dashboard/DashboardLayout.tsx,
    artifacts/ag_diff.txt
  </files>
  <read_first>
    CLAUDE.md — "Terminal task N-2" pattern.
  </read_first>
  <action>
    STEP 1 — TypeScript gate. From tech-pwa directory:
      cd A:/PTOW/1_APT_Central_Command/tech-pwa && npx tsc --noEmit
    Must exit with zero errors. If any errors appear, fix them before continuing. Do not move to STEP 2 until clean.

    STEP 2 — Commit all four changed files:
      git add tech-pwa/src/components/dashboard/JobDetailModal.tsx
      git add tech-pwa/src/components/dashboard/AppSidebar.tsx
      git add tech-pwa/src/components/dashboard/JobQueueTable.tsx
      git add tech-pwa/src/components/dashboard/DashboardLayout.tsx
      git commit -m "fix(pwa): GAP-05/01b tenant contact + GAP-06 WO# search + GAP-07 sidebar labels + GAP-08 comms contrast + remove header search btn"

    STEP 3 — Push to remote:
      git push origin HEAD

    STEP 4 — Generate diff artifact:
      git diff main...HEAD > A:/PTOW/1_APT_Central_Command/artifacts/ag_diff.txt
      git add A:/PTOW/1_APT_Central_Command/artifacts/ag_diff.txt
      git commit -m "chore: update diff artifact — phase 03 plan 02"
      git push origin HEAD

    Post the diff artifact to Claude Code and stop. Wait for PASS before proceeding.
  </action>
  <verify>
    <automated>
      cd A:/PTOW/1_APT_Central_Command/tech-pwa && npx tsc --noEmit
      git -C A:/PTOW/1_APT_Central_Command log --oneline -4
    </automated>
    Expected: tsc exits 0; git log shows the fix commit and the diff artifact commit.
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with code 0 (zero TypeScript errors)
    - `git log --oneline -4` shows the pwa fix commit and the diff artifact commit
    - `artifacts/ag_diff.txt` is non-empty (diff contains all four changed files)
    - Claude Code has issued PASS before continuing to the test sprint
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    AG has committed and pushed all four Next.js component fixes:
    - Tenant contact section visible in JobDetailModal right panel
    - Sidebar shows 'Needs Review' and 'Ready to Schedule' labels
    - WO# search (e.g. APT-01397) returns correct WO first
    - Comms tab message bubbles are visually distinct (inbound grey, outbound accent)
    - Header Search button removed from DashboardLayout (Ctrl+K still works)
  </what-built>
  <how-to-verify>
    Run dev server: cd tech-pwa && npm run dev → http://localhost:3000 (badge 1, PIN 1234)

    1. Sidebar: Confirm left nav shows "Needs Review" and "Ready to Schedule" (not "Operations" / "Schedule Queue"). State which labels you see: ______

    2. WO detail — open any WO that has tenantName and/or tenantPhone set (APT-01397 or APT-01331 if visible). In the right panel, confirm a "Tenant Contact" section shows the tenant name, phone, and/or email. State what you observe: ______

    3. Search — in the dispatch queue search bar, type "APT-01397". Confirm that WO appears as the first result. State the WO number of the first result: ______

    4. Comms tab — open any WO with thread messages. Confirm inbound messages (grey background) and outbound messages (accent background) are visually distinct. State what contrast difference you observe: ______

    5. Header — confirm the "Search Ctrl K" header button is gone from the /live page. Confirm Ctrl+K still opens the CommandPalette overlay. State: Search button visible (yes/no): ______, Ctrl+K opens overlay (yes/no): ______

    Kill dev server after: Get-NetTCPConnection -LocalPort 3000 -State Listen -ErrorAction SilentlyContinue | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force -ErrorAction SilentlyContinue }
  </how-to-verify>
  <resume-signal>Type "approved" and fill in the blanks above, or describe any issues found.</resume-signal>
</task>

<task type="auto" id="task-4">
  <name>Task 4 (separate session): Test sprint + ag_test_results.txt</name>
  <files>artifacts/ag_test_results.txt</files>
  <read_first>
    CLAUDE.md — "Terminal task N-1" pattern.
  </read_first>
  <action>
    Run dev server: cd A:/PTOW/1_APT_Central_Command/tech-pwa && npm run dev

    Browser-verify all five fixes at http://localhost:3000:

    1. SIDEBAR LABELS
       Navigate to /live. Observe the left sidebar.
       Expected label: "Needs Review" on the /live nav item.
       Expected label: "Ready to Schedule" on the /schedule nav item.
       Evidence to record: [exact labels visible in sidebar]: ______

    2. TENANT CONTACT SECTION
       Open a WO that has tenantName set (APT-01397 or APT-01331 if visible in queue).
       Expand the WO detail modal. Look at the right panel.
       Expected: A "TENANT CONTACT" section heading with tenantName, tenantPhone, tenantEmail fields visible.
       Evidence: [section heading visible: yes/no, fields shown: _____]: ______

    3. WO# SEARCH
       In the dispatch queue on /live, type "APT-01397" in the search bar.
       Expected: APT-01397 appears as the first (or only) result.
       Evidence: [first result WO# in search]: ______

    4. COMMS MESSAGE CONTRAST
       Open any WO with thread messages in the Comms tab.
       Expected: Inbound messages have a noticeably different background from outbound messages (grey vs accent color).
       Evidence: [describe the visual difference observed]: ______

    5. HEADER SEARCH BUTTON REMOVED
       On /live, confirm the "Search Ctrl K" button in the top header is NOT present.
       Press Ctrl+K — confirm the CommandPalette overlay still opens.
       Evidence: [Search button visible: yes/no | Ctrl+K works: yes/no]: ______

    Write all evidence to A:/PTOW/1_APT_Central_Command/artifacts/ag_test_results.txt with specific observed values.

    Kill dev server:
      Get-NetTCPConnection -LocalPort 3000,3001,3010 -State Listen -ErrorAction SilentlyContinue | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force -ErrorAction SilentlyContinue }

    Commit and push test results:
      git add A:/PTOW/1_APT_Central_Command/artifacts/ag_test_results.txt
      git commit -m "chore: test results — phase 03 plan 02"
      git push origin HEAD

    Post ag_test_results.txt to Claude Code and stop. Wait for clear-to-merge.
  </action>
  <verify>
    <automated>
      Test-Path A:/PTOW/1_APT_Central_Command/artifacts/ag_test_results.txt
    </automated>
    Expected: file exists and contains specific observed values (not just "PASS").
  </verify>
  <acceptance_criteria>
    - ag_test_results.txt exists with fill-in-the-blank evidence for all 5 items above
    - Each item has a specific observed value (WO#, label text, yes/no) — not just "verified" or "PASS"
    - Dev server killed after testing
    - File committed and pushed
    - Claude Code has issued clear-to-merge before proceeding to merge task
  </acceptance_criteria>
</task>

<task type="auto" id="task-5">
  <name>Task 5: Merge after Claude Code clear-to-merge</name>
  <files></files>
  <read_first>
    CLAUDE.md — "Terminal task N" pattern. Merge only after Claude Code issues "Clear to merge." Not before.
  </read_first>
  <action>
    Merge only after Claude Code has issued "Clear to merge." Do NOT merge before that signal.

    Create PR targeting main:
      gh pr create --title "fix(pwa): GAP-05/01b/06/07/08 — tenant contact + search + labels + comms contrast" \
        --body "Fixes four Phase 2 UX gaps: tenant contact section in WO modal, WO# search with ranking, sidebar labels aligned to workflow stages, Comms tab message bubble contrast. Removes duplicate header Search button (Ctrl+K preserved)."

    After Claude Code clears, merge:
      gh pr merge --squash --auto

    Confirm Vercel auto-deploy triggered (Vercel picks up main push automatically per CLAUDE.md).
  </action>
  <verify>
    <automated>
      gh pr view --json state,mergedAt
    </automated>
    Expected: state = MERGED, mergedAt is non-null.
  </verify>
  <acceptance_criteria>
    - PR merged to main only after Claude Code "Clear to merge" signal
    - Vercel deployment triggered (check Vercel dashboard or GitHub Actions status)
    - No Code.js deploy run during this task (this plan only touches tech-pwa)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User search query → JobQueueTable filter | Untrusted user input processed client-side |
| activeJob data → JobDetailModal render | Data from Neon via API — already validated at API layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | JobQueueTable searchQuery filter | accept | Client-side filter on already-fetched data; no server round-trip; no SQL injection surface. Input never reaches DB. |
| T-03-05 | Information Disclosure | Tenant contact section renders tenantEmail in DOM | accept | This page is behind next-auth session gate (office staff only). Tenant email already accessible via Comms tab. No new exposure. |
| T-03-06 | Spoofing | DashboardLayout Search button removal | accept | Removing the button does not disable Ctrl+K — the keyboard listener in CommandPalette.tsx is unaffected. No auth surface changed. |
</threat_model>

<verification>
All five fixes verified manually by Brandon in dev server before merge (see checkpoint task). Claude Code clears diff and test results before merge gate.
</verification>

<success_criteria>
- Sidebar: "Needs Review" and "Ready to Schedule" labels visible on /live page left nav
- WO detail: tenantName + tenantPhone + tenantEmail visible in right panel when any is non-null
- Search: typing "APT-01397" in dispatch queue search returns that WO first
- Comms tab: inbound messages (grey/neutral) visually distinct from outbound (accent color)
- Header Search button absent from /live page; Ctrl+K still opens CommandPalette
- npx tsc --noEmit exits 0; diff artifact at artifacts/ag_diff.txt committed; Claude Code PASS received; test results at artifacts/ag_test_results.txt with specific evidence; merged to main
</success_criteria>

<output>
After completion, create `.planning/phases/03-gap-remediation/03-02-SUMMARY.md` using the summary template.
</output>
