---
phase: 03-gap-remediation
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
autonomous: false
files_modified: []
requirements:
  - GAP-01
  - GAP-02

must_haves:
  truths:
    - "APT-SEED-0001 test record is absent from the production Neon jobs table"
    - "APT-01331 has a clean tenant_email (no mailto: suffix) for the COORD-03 re-test"
    - "Sam Cooney (cooneysam@gmail.com) is present in the Master Directory Google Sheet"
    - "Dispatcher confirms a Comms tab reply was sent on a real Lapham WO and appeared in the Gmail thread"
  artifacts:
    - path: "artifacts/phase3-verification-results.md"
      provides: "Evidence of all data patches applied + COORD-03 re-test result"
      contains: "APT-SEED-0001\|APT-01331\|Sam Cooney\|COORD-03"
  key_links:
    - from: "Master Directory Google Sheet row (cooneysam@gmail.com)"
      to: "loadLaphamDatabase() in Code.js"
      via: "GAS reads Sheet at runtime on each email ingestion"
      pattern: "COL_RM_EMAIL match on cooneysam@gmail.com"
---

<objective>
Execute three data/operational cleanup steps that require Brandon's direct action (Neon console + Google Sheet access), then re-test the COORD-03 comms reply path on a real Lapham WO after the Wave 1 GAS fixes are deployed. This plan is the final verification gate for Phase 3 and the v1.0 milestone.

Purpose: The data patches clean production state (remove test record, fix malformed tenant_email, add missing contact). The COORD-03 re-test proves the full lead→coordinate loop works on real data.

Output: Phase 3 verified. All v1.0 requirements pass.

DEPENDENCY: This plan executes ONLY after Plan 03-01 (Code.js GAS fixes) is deployed to TechPWA production. GAP-03 re-test requires clean tenant_email data from the GAP-02 fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@A:/PTOW/1_APT_Central_Command/.planning/ROADMAP.md
@A:/PTOW/1_APT_Central_Command/.planning/phases/03-gap-remediation/03-CONTEXT.md
@A:/PTOW/1_APT_Central_Command/artifacts/phase2-verification-results.md

<interfaces>
<!-- No code changes in this plan. Data-only steps. -->

Neon console: https://console.neon.tech — Brandon's login required.
SQL for GAP-04 DELETE: DELETE FROM jobs WHERE job_id = 'APT-SEED-0001';
SQL for GAP-04 verify: SELECT COUNT(*) FROM jobs WHERE job_id = 'APT-SEED-0001';
SQL for APT-01331 cleanup: UPDATE jobs SET tenant_email = 'mwangcp@gmail.com' WHERE job_id = 'APT-01331';
SQL for APT-01331 verify: SELECT job_id, tenant_email FROM jobs WHERE job_id = 'APT-01331';

Master Directory Google Sheet schema (from Code.js constants lines 22–27):
  Column A: Property ID
  Column B: Client
  Column C: Manager Name (RM Name)
  Column D: Property Address
  Column E: Access Email (sender matching key — this MUST be cooneysam@gmail.com)
  Column F: Property Notes

GAP-09 row to add:
  Column E (Access Email): cooneysam@gmail.com
  Column C (RM Name): Sam Cooney
  Columns A, B, D, F: fill with known property info for Sam Cooney, or leave blank if unknown.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: Confirm Plan 03-01 TechPWA deploy is live before proceeding</name>
  <what-to-do>
    This plan executes ONLY after the Code.js GAS fixes from Plan 03-01 are deployed to TechPWA production.

    Confirm by asking Brandon: "Did you run the TechPWA clasp deploy command from Plan 03-01 Task 3?"

    The command was:
      clasp deploy --deploymentId AKfycbySG8tbAaXyIRFXnq7x-Fp5Gvs7uG8RmAyBB_wSFcGmScbhI3SHSq2HoznowBcsi3mM9Q --description "v## — GAP-01 serviceCategory + GAP-02 forwarded parsing"

    Brandon must confirm the deploy ran and show the output (deployment ID + version number).
    Deploy confirmed: yes/no: ______
    Deployment output: ______

    If deploy has NOT been run: STOP. Do not proceed. Remind Brandon to run the clasp deploy command first.
  </what-to-do>
  <resume-signal>Type "confirmed" and paste the clasp deploy output. Only then proceed to Task 1.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Brandon — Neon data cleanup (GAP-04 + APT-01331 fix)</name>
  <what-to-do>
    Brandon opens Neon console (https://console.neon.tech), selects the APT Central Command database, opens the SQL editor, and runs the following two statements in sequence.

    STATEMENT 1 — Delete the APT-SEED-0001 test record (GAP-04):
      DELETE FROM jobs WHERE job_id = 'APT-SEED-0001';

    Verify: Run this query and paste the output:
      SELECT COUNT(*) FROM jobs WHERE job_id = 'APT-SEED-0001';
    Expected result: count = 0.
    Paste the count result here: ______

    STATEMENT 2 — Fix APT-01331 malformed tenant_email (required for COORD-03 re-test):
      UPDATE jobs SET tenant_email = 'mwangcp@gmail.com' WHERE job_id = 'APT-01331';

    Verify: Run this query and paste the output:
      SELECT job_id, tenant_email FROM jobs WHERE job_id = 'APT-01331';
    Expected result: tenant_email = 'mwangcp@gmail.com' (no '<mailto:...>' suffix).
    Paste the full row here: ______
  </what-to-do>
  <resume-signal>Type "done" and paste both query outputs above.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Brandon — Add Sam Cooney to Master Directory Google Sheet (GAP-09)</name>
  <what-to-do>
    Brandon opens the Master Directory Google Sheet (the same spreadsheet that feeds the Lapham contact lookup in GAS — accessible via Google Sheets, sheet tab named "Master Directory").

    Add a new row with the following values:
      Column A (Property ID): [fill with Sam Cooney's property ID if known, or leave blank]
      Column B (Client): Sam Cooney (or the PM company name if known)
      Column C (Manager Name): Sam Cooney
      Column D (Property Address): [fill if known, or leave blank]
      Column E (Access Email): cooneysam@gmail.com   ← CRITICAL — this is the sender-matching key
      Column F (Property Notes): [leave blank or fill if known]

    Column E (Access Email) is mandatory. Without it, loadLaphamDatabase() cannot match incoming emails from cooneysam@gmail.com to a contact record.

    After adding the row:
    - Confirm the row is visible in the sheet
    - State the row number added: ______
    - Confirm the value in Column E: ______
  </what-to-do>
  <resume-signal>Type "done" and paste the row number and Column E value.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: COORD-03 re-test — Comms tab reply on real Lapham WO</name>
  <what-built>
    Wave 1 GAS fixes (Plan 03-01) are deployed. APT-01331 now has a clean tenant_email (mwangcp@gmail.com — from Task 1). The comms reply path can now be tested on a real Lapham WO with a safe internal recipient.
  </what-built>
  <how-to-verify>
    PREREQUISITE: Confirm Plan 03-01 TechPWA deploy is live (Brandon or AG ran the clasp deploy command printed by Plan 03-01 Task 3).

    STEP 1: Open the dispatch dashboard (https://dispatch.aptmaintenanceinc.com).
    STEP 2: Navigate to APT-01331 (or any real Lapham WO with a clean tenant_email).
    STEP 3: Open the Comms tab.
    STEP 4: Confirm the Gmail thread history loads (COORD-01 baseline — already passed in Phase 2, but confirm still working after GAS changes).
    STEP 5: Send a reply. Use a safe internal email as the recipient if the production tenant_email is live (you can edit the To field before sending if needed). Send a real test message (e.g., "Test reply from dashboard — Phase 3 COORD-03 verification. [date]").
    STEP 6: Confirm the sent message appears in the Gmail thread (check Gmail directly or wait for the thread to reload in the Comms tab).

    Evidence to record:
    - WO number used: ______
    - Comms tab loaded with thread history: yes/no: ______
    - Reply sent successfully (no error shown in UI): yes/no: ______
    - Message visible in Gmail thread: yes/no: ______
    - Message text seen in Gmail thread: ______
  </how-to-verify>
  <resume-signal>Type "approved" and fill in all blanks above. If any step fails, describe the failure.</resume-signal>
</task>

<task type="auto" id="task-4">
  <name>Task 4: Record Phase 3 verification results</name>
  <files>artifacts/phase3-verification-results.md</files>
  <read_first>
    artifacts/phase2-verification-results.md — structure to replicate.
    .planning/REQUIREMENTS.md — requirement IDs and descriptions.
  </read_first>
  <action>
    Create artifacts/phase3-verification-results.md recording the outcome of all Phase 3 remediation work. Include:

    1. Header with date, runner, milestone
    2. A requirements table with columns: Requirement, Environment, Result, Evidence — covering all 11 v1.0 requirements (copy Phase 2 pass/fail results for COORD-01, DISP-01, DISP-02, DISP-03, QUEUE-01 and fill in the Phase 3 fixes for LEAD-01, LEAD-02, COORD-02, COORD-03)
    3. Gap remediation summary — for each of the 9 gaps, state: Gap ID, Description, Fix Applied, Evidence of Resolution
    4. Data cleanup section — state the SQL results from Task 1 (APT-SEED-0001 deleted, APT-01331 updated) and the Sam Cooney Sheet row from Task 2
    5. COORD-03 re-test section — paste the evidence from Task 3

    Use the evidence collected in Tasks 1, 2, and 3 above. Do NOT fabricate values — paste exactly what was observed.

    After writing the file:
      git add A:/PTOW/1_APT_Central_Command/artifacts/phase3-verification-results.md
      git commit -m "docs(phase3): record verification results — all gaps remediated"
      git push origin HEAD
  </action>
  <verify>
    <automated>
      Test-Path A:/PTOW/1_APT_Central_Command/artifacts/phase3-verification-results.md
      git -C A:/PTOW/1_APT_Central_Command log --oneline -2
    </automated>
    Expected: file exists; commit visible in log.
  </verify>
  <acceptance_criteria>
    - artifacts/phase3-verification-results.md exists and is non-empty
    - File contains all 9 gap IDs (GAP-01 through GAP-09)
    - File contains COORD-03 re-test evidence with specific WO# and message text
    - File contains Neon SQL result confirming APT-SEED-0001 count = 0
    - File committed and pushed to remote
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Brandon → Neon console SQL | Privileged direct DB access for data cleanup |
| Brandon → Google Sheet | Privileged write to contact lookup data source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | Neon DELETE (GAP-04) | accept | Targeted WHERE clause on specific job_id; no wildcard delete. Brandon verifies count = 0 after deletion. |
| T-03-08 | Tampering | Neon UPDATE (APT-01331 tenant_email) | accept | Targeted WHERE clause on specific job_id; overwrites one field. Verified by SELECT after UPDATE. |
| T-03-09 | Spoofing | Master Directory Sheet row (GAP-09) | accept | Adding a sender→contact mapping; cooneysam@gmail.com must already be a known Lapham PM. No new privilege granted — GAS already trusts all rows in Master Directory. |
</threat_model>

<verification>
Phase 3 complete when:
1. `SELECT COUNT(*) FROM jobs WHERE job_id = 'APT-SEED-0001'` returns 0 (GAP-04)
2. `SELECT tenant_email FROM jobs WHERE job_id = 'APT-01331'` returns 'mwangcp@gmail.com' with no suffix (APT-01331 fix)
3. cooneysam@gmail.com row present in Master Directory Sheet with Sam Cooney in Manager Name column (GAP-09)
4. COORD-03 re-test: dispatcher states WO#, confirms reply sent, message visible in Gmail thread (GAP-03)
5. artifacts/phase3-verification-results.md committed with specific evidence for all items
</verification>

<success_criteria>
- APT-SEED-0001 is absent from production Neon jobs table (SQL count = 0)
- APT-01331 tenant_email = 'mwangcp@gmail.com' (no mailto: suffix)
- Sam Cooney row added to Master Directory Sheet with cooneysam@gmail.com in Access Email column
- COORD-03: dispatcher sends real reply from Comms tab on real Lapham WO, message appears in Gmail thread
- All 11 v1.0 requirements marked pass with specific evidence in phase3-verification-results.md
- Milestone v1.0 CC Core Operational closes
</success_criteria>

<output>
After completion, create `.planning/phases/03-gap-remediation/03-03-SUMMARY.md` using the summary template.
Update .planning/REQUIREMENTS.md — mark all 11 requirements as complete with evidence references.
Update .planning/STATE.md — mark Phase 3 complete, milestone v1.0 closed.
</output>
